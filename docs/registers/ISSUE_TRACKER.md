# RosiFit — Issue Tracker

Companion to `REMEDIATION_WORK_ORDER.md`. One row per underlying defect. Where several audits found the same thing, the row lists every source ID — so this is the de-duplicated table the RV register wanted and could not build. When the consolidated 75/77-item register is found, map its IDs onto these rows; do not create new rows for them.

Lives at `docs/registers/ISSUE_TRACKER.md`. Append-only for rows; the Status column is the only thing that changes.
(Renamed from `RosiFit_Issue_Tracker.md` and `RosiFit_Remediation_Work_Order_v2.md` on 17-Sep-2026 via `git mv`, so the history follows.)

---

## The loop — run this for every row, no exceptions

1. **Take the next open row in the current gate that belongs to your surface (D-9, D-9a).** Never take a row from a later gate while an earlier gate has open rows. Gate 0 rows are reads, not fixes — they can run in parallel with Gate 1. **Claim it first:** write `⏳ A`, `⏳ B` or `⏳ C` into the row's Status cell, `git pull --rebase`, push. A row already carrying another session's claim is skipped. **Work in your own worktree or clone** — `git worktree add ../RosiFit-<X> <branch>`, shared object store, separate HEAD and index. No session checks out another's branch; everyone branches from `origin/main` and pushes `HEAD:<branch>` (D-9b). **Three surfaces (D-9a, 18-Sep-2026).** Session A owns `supabase/migrations`, `supabase/tests`, `db/harness`, any row whose fix is SQL, and every production **write**. Session B owns `app/` and `src/`. Session C owns `supabase/functions/`, `.github/`, `ci/`, `tsconfig*`, `package.json`, the eslint config, `supabase/config.toml`, and read-only production **reads** for Gate 0 rows. No session edits another's files — if a B or C row needs SQL, it is noted in the row and left for A; if a row's Verify needs a production read, the exact query is written into the row for the session that owns that read. The guard specs under `src/data` that police Edge Functions (`edgeFunctionPagedReads`, `requestSize`, `pagedReads`) stay with B: C changes function code and asks B to widen a guard through a tracker note, never by editing `src/`.
2. **Branch** `fix/<row-id>` (e.g. `fix/T-043`).
3. **Write the failing test first.** The row's *Proof* column names it. If the row has no proof named, the first commit on the branch is the test. If a test cannot exist (a doc change), say so in the PR.
4. **Make the change.** One row per PR. If you find a new defect while in there, **add a row** — do not fix it in the same PR.
5. **Run `npm run check` locally.** Push. CI runs `gate` and `db-harness`. Both must be green. (Until Gate 2.2 lands they are not yet *required* on `main` — read them anyway; a merge on red is a Gate 2 violation.)
6. **If the PR carries a migration:** apply per D-3 (`db query --linked -f`, then `migration repair`), never `db push`. Run the row's *Verify* read against production afterwards.
7. **Write the root-cause entry** in `docs/registers/ROOT_CAUSE_REGISTER.md` as the next `RC-nnn` from your session's block (D-9/D-9a: Session A RC-046–RC-069, Session B RC-070–RC-099, Session C RC-100–RC-129; the register stays newest-first by date, not by number; the 0071 incident is RC-046 and is T-013's entry). The template is below. The PR is not mergeable without it — add a CI/spec check that every PR touching `src/`, `supabase/` or `app/` also touches `ROOT_CAUSE_REGISTER.md` or carries a `no-rc:` label with a reason. A fix without a recorded cause is how the same class recurs one file over (RC-039 → RC-043 → RV-05).
8. **Tick the row** — `⏳ A`/`⏳ B` → `☑ PR#, date, RC-nnn`. Every push that touches the three registers is preceded by `git pull --rebase`; register edits are line-local and a conflict is resolved by keeping both sides (D-9). Rows that were `NEEDS VERIFICATION` and turned out not to be defects get `☒ not a defect` with the read that showed it.
   **`RUN_LOG.md` lines are written by `scripts/run-log.mjs`, for gate verdicts only — never by hand.** A Gate 0 row is a read, not a run: it has no gate verdict, so it records its result in its own tracker row and nowhere else.
9. **Gate exit** is the exit condition in the work order, not "all rows ticked" — some rows can legitimately move to a later gate with a written reason in the row.

### RC entry template (one per ticked row)

```
## RC-nnn — <one-line name>                          Tracker: T-nnn · Sources: RV-xx, A:F-xx …
Symptom       What the operator / the data / the log actually showed. Dated if known.
Root cause    The mechanism, not the site. "ON CONFLICT infers an index, not a rule" — not
              "commit_csv_import failed." Name the file:line where the mechanism lives.
Why it shipped  The check that should have caught it and why it did not run or could not see
              it (the test that was never run, the guard scoped to one file, the tsconfig exclude).
Class         Which other sites carry the same mechanism. Enumerate them — this is the sweep.
              If this fix closes only the one site, say so and open tracker rows for the rest.
Fix           What changed, with file:line and migration number. What deliberately did NOT change.
Proof         The test that fails without the fix and passes with it. Its path.
Guard         What now makes the class a build failure rather than a review finding.
Verify        The production read taken afterwards, its result, its timestamp.
```

The two fields that matter most are **Why it shipped** and **Class**. Every audit found the same shape: a rule established, specced, and applied to the file where it was discovered rather than everywhere it holds. If those two fields are honest, the register becomes the sweep list for the next audit instead of a memorial.

Cadence honesty: at ~3–5 h/weekday for two people, Gate 1 is about a week and Gate 2 another one to two. Gates 3–6 are a quarter. Gate 7 is polish that goes through a working pipeline whenever there is slack. Do not compress Gate 2 — it is the gate that makes every later tick mean something.

Status marks: `☐` open · `☑` done (PR#, date) · `☒` not a defect / superseded (reason) · `→G<n>` moved with reason

---

## Gate 0 — Read-only verification (parallel with Gate 1)

| ID | Read | Sources | Settles | Status |
|---|---|---|---|---|
| T-001 | GitHub Actions history for `main` — `gate` and `db-harness` state | RV-40, A:F-09/F-10 | Whether CI is red, green, or not running | ☑ 17-Sep-2026, read-only via the GitHub API. One workflow (`CI`, `.github/workflows/ci.yml`), 80 runs on `main`, **0 green**. #2–#80 (2–11 Sep): no step of either job ever ran — every check-run annotated *"The job was not started because your account is locked due to a billing issue."* #82 (11-Sep 10:03 UTC) onward: both jobs execute. `gate` fails at `npm run check` every run (RV-03). `db-harness` installs psql 16, replays every migration clean, runs all 56 spec files (~4 min), and has been red every run: 16 files at #82, 19 at #99 (`5e361de`, 0071), 18 at #103/#104. `main` is unprotected (`protected:false`, required checks off) — a red run blocks nothing. Step name still says "135 assertions"; there are 776. Job logs were fetched with the credential the machine already holds for `git push`, sent as a header to api.github.com only, never printed. |
| T-002 | `email_batches where status='processing'` | RV-41, C:RF-06 | Killed sends already in prod | ☑ 17-Sep-2026 21:10:43 UTC (18-Sep 02:40 IST), read-only on prod `lhpzhkzbnquwjljmbylo`. **Zero rows.** `select id, created_at, requested_count, excluded_count, sent_count, failed_count, completed_at, client_batch_id from email_batches where status = 'processing' order by created_at` returned `[]`. RV-41 does not fire today. The empty set is meaningful because the table is not empty: 17 batches, every one terminal — 12 `completed`, 5 `completed_with_failures`, oldest `2026-09-05 07:44:12.050618+00`, newest `2026-09-09 20:00:44.906731+00`. **No batch has been created since 09-Sep**, so this reads the whole send history, not a quiet window. Counters summed across all 17: requested 23, excluded 5, sent 13, failed 5 (5+13+5 = 23, internally consistent). All 17 carry a distinct `client_batch_id` — which is T-017/T-115 seen from the data side: the server minted a fresh key every time, so no two of these 17 could ever have collided. **Caveat that outranks the result: see T-119** — only 1 `email_messages` row survives to back those 18 terminal per-recipient outcomes, so `email_batches` counters are currently the only record that any of these sends happened. |
| T-003 | `email_messages group by status` | B:unknown-5, B:F-14 | Rows stuck in `sending` | ☑ 17-Sep-2026 21:10:43 UTC, read-only on prod. `select status, count(*) from email_messages group by 1` returned **exactly one group: `sent` = 1** (its `sent_at` = `2026-09-08 10:05:20.786+00`). **No rows in `sending`, `queued`, `failed` or `excluded`.** B:F-14 does not fire today — but not because the send path is sound: the table is all but empty. `pg_stat_user_tables` for `email_messages`: `n_tup_ins` **23**, `n_tup_upd` 20, `n_tup_del` **32**, `n_live_tup` **1**; `pg_stat_database.stats_reset` = `2026-08-25 20:33:23.891825+00`, which predates the first batch (05-Sep), so these counters cover the entire history of the table. 23 inserted matches `sum(requested_count)` = 23 exactly: **every message row was written, and 22 were deleted afterwards.** The read is not an RLS artefact — it ran as `postgres`, which holds `rolbypassrls = true`, and `n_live_tup` is RLS-independent anyway (`email_messages` is `relrowsecurity` and `relforcerowsecurity` with 1 policy). Two schema facts for T-053's sweeper, recorded here because they change its design: `email_messages` has **no `created_at`** column at all (only `sent_at` and `delivered_at`, both null until a send succeeds), so a stale `queued`/`sending` row carries no timestamp to age out on; and the table has **zero user triggers**, so nothing audits a delete. Filed as **T-119**. |
| T-004 | Is 0072 applied? `fetchMembers` selects `active_again_from` | B:unknown-8 | Member list may fail outright | ☒ not a defect — 17-Sep-2026: `supabase_migrations.schema_migrations` holds `20260916093816 / 0072_member_active_again_from` (read during T-013's ledger check). |
| T-005 | `pg_roles.rolconfig` for `authenticated`, `service_role`, `authenticator` | C:§20-2, A:V-4 | Real `statement_timeout` (import ceiling) | ☑ 17-Sep-2026 21:12:18 UTC, read-only on prod. Verbatim `rolconfig`: `authenticated` — `{statement_timeout=8s}`. `authenticator` — `{session_preload_libraries=supautils, safeupdate, statement_timeout=8s, lock_timeout=8s}`. `service_role` — **null** (`rolbypassrls=true`, `rolsuper=false`). Read in the same statement: `anon` — `{statement_timeout=3s}`; `postgres` — `{search_path="$user", public, extensions}`, `rolbypassrls=true`. **Confirms the work order §1 Problem 3 assumption exactly**: `service_role` sets nothing of its own, so a connection that authenticates as `authenticator` and then `set role service_role` keeps authenticator's session-level **8s**. 8s is the real import ceiling for C:RF-01 / T-014 / T-048. **New, and not in the work order: `lock_timeout=8s` is also set on `authenticator`.** That changes the shape of the secondary effect C:RF-01 names — two writers serialising on `member_stats` do **not** block indefinitely; the second aborts with `55P03 lock_not_available` after 8s. So the 2,000-row timing has two distinct 8s walls to measure against, statement and lock, and a failure at the lock wall will look nothing like a timeout — carried into **T-048**, which owns that timing. |
| T-006 | `count(*) from member_period_metrics(<this week>)` | B:unknown-4 | Whether T-042 fires today | ☑ 17-Sep-2026 21:12:33 UTC, read-only on prod. `select count(*) from member_period_metrics('2026-09-14','2026-09-20')` — **1,087 rows**. Week is Mon 14-Sep to Sun 20-Sep, matching `period.ts` `weekStart()` and `follow_up_config.week_start_day = 1`. Signature confirmed from `pg_proc` first: `member_period_metrics(p_from date, p_to date, p_member_id uuid DEFAULT NULL, p_offering_id uuid DEFAULT NULL, p_branch_id uuid DEFAULT NULL, p_course_id uuid DEFAULT NULL)`. Context read in the same statement: `members` 1,154 total, 1,152 with `deleted_at is null`, `member_enrollments` active 1,128. **T-016 and T-042 fire today.** 1,087 is 87 rows past PostgREST's documented 1,000-row default, and those 87 members fall to `metric?.expected ?? 0` at `repository.ts:229, :1953, :1977` — zero expected, zero missed, never eligible for follow-up, and silently absent from every total. Honest limit on this read, per work order §8: the live `db-max-rows` is **not** readable from SQL (`current_setting('pgrst.db_max_rows', true)` null, no `pgrst*` entry in `authenticator`'s `rolconfig`, no matching `pg_settings` row), so 1,000 is the documented default rather than a measured platform value. The 1,087 is measured. What would settle it outright is one authenticated PostgREST call to the RPC counting the rows actually returned — that is a client-surface read, written here for B to fold into T-016's proof. |
| T-007 | `csv_imports` status since 2026-09-16 | B:unknown-3 | Whether any import has succeeded since 0071 | ☑ 17-Sep-2026, read-only on prod `lhpzhkzbnquwjljmbylo`. **Yes — one, and only because it carried no new names.** 0071 went live 16-Sep 05:08 UTC (commit `5e361de`). Since then exactly one import completed: `81341cf8` 17-Sep 01:06 UTC, `unmatched_count=0`. Every import since 0071 carrying ≥1 unmatched row is still `previewed` and never completed: `d4dac576` (5), `e16d3bfc` (5), `de133ddc` (5), `5530afac` (1), `310dfaef` (1). Those five are **the only `previewed` rows in the table's entire history** — 42 `completed`, 9 `reverted`, and before 16-Sep 11:09 UTC not one stranded preview. Last `add_as_new` to reach the database: `a03e5c9e`, 16-Sep 03:08 UTC — pre-0071. No `members` or `member_aliases` row created by an import since. Confirms Problem 2 is live and dates its onset to 0071's apply. Caveat: `csv_imports.error` is null on all 56 rows ever and no row has ever held `status='failed'`, so a stranded preview is consistent with a failed commit but is not itself proof of an attempt — see new row T-109. **Resolved 14:05 UTC:** gateway logs show a `commit_csv_import` 400 for each of the four 17-Sep previews (T-109 addendum). Attempts confirmed. |
| T-008 | Re-run 0073 duplicate-group count at apply time | RV-01, FR | Guard will pass | ☑ 17-Sep-2026 11:23:52 UTC, read-only on prod: **0** `(member_id, alias_type, alias_normalized)` groups with >1 row across 796 alias rows — the guard passes. `member_aliases_member_name_unique` absent; `pg_proc` scan finds the stale `on conflict (alias_type, alias_normalized)` in exactly `commit_csv_import` and `merge_member_into`. Also read: `merge_member_into` in production carries 0061's wording (`merged into themselves`), not 0032's — see T-111. |
| T-009 | `get_advisors(type:'performance')` | C:§20-4 | Confirms T-047; surfaces index gaps | ☑ 17-Sep-2026 21:13:01 UTC, read-only via Supabase MCP. **`auth_rls_initplan` hits: 0** — and the zero does **not** clear C:RF-07 / T-043. Only two lints returned anything, both INFO: `unindexed_foreign_keys` ×**29** and `unused_index` ×**3**. Unindexed FKs in full: app_users(created_by), attendance_records(corrected_by), attendance_records(created_by), course_communication(template_id), course_communication(updated_by), course_follow_up_config(updated_by), csv_imports(offering_id), csv_imports(uploaded_by), email_batches(sent_by), email_batches(template_id), email_messages(member_email_id), email_templates(created_by), email_templates(updated_by), follow_up_config(updated_by), holidays(created_by), member_aliases(confirmed_by), member_emails(created_by), member_enrollments(created_by), member_import_runs(default_offering_id), member_import_runs(imported_by), member_schedules(created_by), members(created_by), members(updated_by), mobile_number_changes(approved_by), mobile_number_changes(initiated_by), offering_schedules(created_by), pin_reset_requests(resolved_by), sessions(created_by), super_admin_recovery(question_id). Unused indexes: `auth_rate_limits_window` on `auth_rate_limits`, `audit_remarks_entry` on `audit_remarks`, `holidays_range` on `holidays`. Note the linter checks FK coverage only, so it does not and cannot surface T-074's missing `member_schedules (effective_from, effective_to)` — that is not an FK and stays a hand-written row. **Why the initplan zero is a false green, verified rather than assumed.** `pg_policies` holds **62** policies in `public` across **32** tables. **59 of the 62** call `is_active_app_user()` (38), `is_super_admin()` (21) or `is_subscription_writable()` (28), and **0 of the 59 wrap the call in a scalar subquery**. The only two policies that reference `auth.uid()` at all — `app_users_read` and `app_users_self_update` — are **already** wrapped as `( SELECT auth.uid() AS uid)`, which is 0013's fix, so the lint has nothing left to report. The proof sits inside one predicate: `app_users_self_update` is `((is_super_admin() OR (auth_user_id = ( SELECT auth.uid() AS uid))) AND is_subscription_writable())` — a wrapped `auth.uid()` and a bare `is_subscription_writable()` side by side, and the lint flags neither. On this evidence the lint's scope is `auth.*` and it does not follow user-defined helpers, so it reads clean today and will read clean after T-043 lands. **T-043's Proof column (“T-009 lint clears”) therefore cannot detect its own fix.** Also: C:RF-07's “47 policies” undercounts — the measured figure is 59 of 62. Filed as **T-118**. |
| T-010 | SES console — sandbox?, send rate, 24h quota, config set, bounce/complaint rates | C:§3 `[U]` | Sizes Gate 4 rate limiter | ☐ **Owner read — exact lookups written by C, 18-Sep-2026.** AWS console only; nothing here is reachable from the repo or the database. **Region first:** an SES account is per-region, so confirm which region `send-followups` sends through before reading anything — check `AWS_REGION` / the SES endpoint in the function's Edge secrets, then do every lookup below **in that region**, and record the region with the answers. **(1) Sandbox or production access.** SES → Account dashboard. Record verbatim which banner shows: “Your Amazon SES account is in the sandbox” or production access granted. In sandbox every recipient address must be separately verified, which on its own would account for the 5 `completed_with_failures` batches T-002 found. **(2) 24-hour sending quota.** Same dashboard, “Daily sending quota”. Record the quota and “Sent in last 24 hours”. Caps Gate 4's batch size. **(3) Maximum send rate, messages per second.** Same panel. **This is the number that sizes Gate 4's token-bucket rate limiter** — work order §6 marks it required before that limiter can be written. Record it exactly including decimals (sandbox is typically 1/s, production commonly 14/s). **(4) Configuration set.** SES → Configuration sets. Record whether one exists and its exact name. Without one there is no per-message event stream, and T-052's `unknown` outcome has nothing to reconcile against. **(5) Event destination and SNS topic.** For that configuration set, record which events publish (`Send`, `Delivery`, `Bounce`, `Complaint`, `Reject`, `Open`, `Click`) and the destination SNS topic ARN. Then SNS → that topic → Subscriptions: record the subscription **Status** (“Confirmed” or “Pending confirmation”) and its endpoint URL. A pending subscription means `ses-feedback` has never been reached at all — cross-check T-066. **(6) Reputation metrics.** SES → Reputation metrics. Record `Bounce rate` and `Complaint rate` as percentages and whether any account-level review or sending pause is in force. AWS acts at bounce 5% (review) / 10% (pause) and complaint 0.1% (review) / 0.5% (pause). **(7) Verified identities.** SES → Identities. Record each identity, whether it is a domain or a single address, its verification status and its DKIM status. Normally the sender in use would be read from `email_messages.from_email`, but T-119 leaves one row there, so the live `from` address has to come from the function's secrets instead. Paste each answer into this row with the region and the time of reading. |
| T-120 | `md5(prosrc)` for every function in `public` on production, diffed against the same after a full harness replay; and the twelve unledgered migration files mapped to applied / not-applied by body evidence | T-014 pre-flight, RC-047, T-112, T-033 | Whether the unledgered migrations produced anything the replay does not — and how far a green `db-harness` run can be trusted to predict production | ☑ 18-Sep-2026. Production read 04:38–05:05 UTC; harness side emitted by `54_function_body_inventory.sql` on `db-harness` run #132. Extension-owned functions excluded via `pg_depend deptype='e'`. **60 application functions on production, 62 in the replay: 43 identical, 15 different, 2 in the harness only.** Harness bodies are consistently the longer ones — what a replay carrying migrations production never received looks like. **Harness only, absent from production:** `set_attendance` (0035, 9,160 bytes — see T-126) and `attendance_backdates_membership` (0046, 3,216). **Different (production bytes → harness bytes):** `commit_csv_import` 18,521→21,033 · `update_member` 9,625→11,213 · `reset_day_attendance` 3,554→5,303 · `set_member_status` 2,847→3,772 · `refuse_course_duplicate` 2,103→2,600 · `bulk_import_members` 6,198→6,530 · `save_course` 4,785→4,944 · `set_offering_schedule` 4,564→4,668 · `audit_log_as` 802→1,254 · `effective_course_message` 628→771 · `is_in_course` 632→725 · `branches_guard_removal` 782→908 · `branches_fill_code` 656→734 · `holidays_apply_effects` 943→921 · `attendance_reset_preview` (two overloads; one identical, one 2,578→3,144). **The twelve unledgered files by body evidence — decisively NOT applied:** `0035_set_attendance` (function absent live), `0046_attendance_backdates_membership` (absent), `0045_import_change_counts` (`v_absent_added` and the `'changes'` key absent live — T-125). **Inconclusive from bodies alone**, because a later migration restated the same function: `0016_create_member`, `0017_holiday_delete`, `0018_offering_schedule_rpc`, `0025_trigger_function_grants`, `0031_member_status`, `0034_pin_reset_requests`, `0037_import_override` (its markers ARE live), `0038_staff_write_access`, `0044_override_scoped_by_meeting_instance`. **Consequence:** `npm run test:db` green proves the replay is self-consistent; it does not predict production for 15 of 60 functions. Gate for 0075 onward (D-10), and the reason 0074 is committed but not merged. **Re-measured 22-Sep-2026 by session (Supabase MCP, read-only), before any apply:** identical picture — 60 live, 62 in the full replay, 2 harness-only, 15 different. **Then separated by stripping comments and whitespace:** 13 of the 15 differ in comments only; the two real code differences were `commit_csv_import` (0045, T-125) and `bulk_import_members` (T-132). A replay of every file MINUS 0035/0045/0046 matched production name-for-name with `bulk_import_members` the only code-level difference, so "what production runs" = "the repository minus those three" + one guard. The ledger-only replay (68 ledgered files) fails at 0069 because 0017's `holidays_apply_effects` is missing — proof that the unledgered nine had been applied without rows. After the applies below: 0035 and 0046 are live and ledgered; the nine were ledgered after the fact (T-033). |

Already settled by FR (record, don't re-read): RV-39 — `member_aliases_unique` absent in prod, stale clause in exactly `commit_csv_import` and `merge_member_into`, function bodies match repo. Import is broken in production.

## Gate 1 — Stop the bleeding

| ID | Defect | Sources | WO item | Proof | Verify | Status |
|---|---|---|---|---|---|---|
| T-012 | `db-harness` CI job runs but its verdict is unread: 18 of 56 spec files red since CI first executed (11-Sep), `main` unprotected, step name says 135 assertions (776) | RV-40, A:F-01, B:T-01, FR §6, T-001 | 1.1 / D-7 | `db-harness` green on `main`; step name corrected (776 assertions, not 135); verdict consumed via **T-026** | T-001 | ☐ **First in Gate 1 (reordered 17-Sep-2026): proof surface for T-011 and T-013** — neither can be shown green anywhere else. |
| T-011 | `04_members.sql:37,:40,:58` assert indexes 0071 dropped; suite contradicts `48:209-213` | RV-02, FR §11 | 1.1 / D-2, **D-2a** | `npm run test:db` green in CI (T-012 is the proof surface — no `psql`/`docker`/`supabase` on the dev machine). `:37`/`:40` re-pointed to `member_aliases_member_name_unique` via `RF-000118` (three literals each); `:58` inverted to acceptance + `t.ok` naming `refuse_course_duplicate`, cross-ref 48. **Depends on 0073 being in the replay.** | — | ☑ PR #20, 17-Sep-2026, RC-048. `db-harness` run #111 (`ab6bac6`, 14:15 UTC): `04_members.sql` green — all 16 assertions incl. the four re-pointed/rewritten; suite 790 PASS (was 779), red files 18 → 17. Merged `main` 17-Sep 21:0x UTC. |
| T-013 | `ON CONFLICT (alias_type, alias_normalized)` infers dropped index → `42P10`; whole import aborts; `merge_member_into` same | RV-01, RV-39, A:F-01, B:F-01, FR | 1.2 / D-3 | `51_alias_unique_per_member.sql` 1–9; `aliasConflictTarget.test.ts`; then one real upload | `pg_indexes` shows `member_aliases_member_name_unique` unique; `pg_get_functiondef` scan = 0 stale | ☐ |
| T-014 | `recompute_member_stats()` unscoped in `commit_csv_import` `0045:428`, `update_member` `0027:304`, `create_member` `0026:406` | A:F-06, B:F-07, C:RF-01-A | 1.3 (0074) | Harness spec: untouched member's stats row not rewritten by an import | Timing at 2,000 rows (T-062) | ⏳ A |
| T-015 | `expected_members_for_session()` called once per file row inside the loop | B:F-07, C:RF-01-B | 1.3 (0074) | Same harness spec; `v_expected_ids` computed once | — | ⏳ A (with T-014, one PR) |
| T-016 | `member_period_metrics` RPC unpaged/unguarded at `repository.ts:229, :1953, :1977` → silent zeros past 1,000 | A:F-02, B:F-02 | 1.4 stopgap | Fake-server test: 1,001 rows → throws, nobody zeroed | T-006 | ☑ PR #30, 18-Sep-2026, RC-071. Merged `7ddb3848`. **Was firing in production:** T-006 read 1,087 rows against the 1,000 cap. Stopgap only — the proper fix splits: `period_totals()` for the two summing callers stays with A (needs 0075), and the keyset half (`fetchMembers` reading `member_period_metrics` through `pageAllByKey` with `key: member_id`) is B's, as its own row and PR once 0075 lands. **Code complete, branch `fix/T-016` pushed, PR NOT YET OPEN** (same tooling wall as T-017). RC-071 written. Spec `src/data/periodMetrics.test.ts` 9/9; typecheck clean; icons 75/75; contrast 2852/2852; `test:unit` 14 failures, the same 14 measured on `origin/main`. Both RC-070 and RC-071 insert above RC-048, so whichever PR merges second WILL conflict there — resolve by keeping both (D-9). |
| T-017 | `client_batch_id` never passed (`api.ts:269` optional; `send/index.tsx:225`, `member/[id].tsx:278` omit); server mints UUID; retries duplicate | RV-06, A:F-05, B:F-05, C:RF-05, B:C-6 | 1.5 | Spec: two calls same key → one batch; source-reading spec on call sites | — | ☑ PR #25, 18-Sep-2026, RC-070. Merged `7304ef6a`. Was: **code complete, branch pushed.** RC-070 written. `npm run check`: typecheck clean, icons 75/75, contrast 2852/2852, `test:unit` 14 failures which are the SAME 14 measured on `origin/main` in a second worktree (T-023/T-024/T-025 + five T-013/0073 alias cases); the branch adds 13 passing cases and regresses nothing. **Blocked on tooling, not on the work:** this machine has no `gh`, the GitKraken MCP needs an interactive sign-in, and reading the git credential (the route T-001 used for the API) is refused by the auto-mode classifier as credential exploration. Needs the owner to open the PR from `fix/T-017`, or to allow one of those routes. Same wall will stop every later Session B row at PR time. |
| T-018 | Send screen `failed` gate omits `already.state==='error'`; `defaultSelection` pre-ticks everyone when sent-map unknown | B:F-06, C:RF-08 | 1.5 | Spec: `fetchSentForPeriod` rejects → nothing ticked, screen refuses | — | ☑ PR #24, 18-Sep-2026, RC-072. Merged `e55c203b`, before T-017 so the shared file took the smaller change first. Was: touches `app/send/index.tsx`, which `fix/T-017` also changes; branched from `main` per the loop, so expect a small conflict in that file depending on merge order. **Code complete, branch `fix/T-018` pushed, PR NOT YET OPEN.** RC-072 written. Spec `src/data/sendFailsClosed.test.ts` 6/6 (3 were red first, 3 are controls: a known-empty map must still tick everyone). Typecheck clean, icons 75/75, contrast 2852/2852, `test:unit` back at the 14-failure baseline. The `SentMap \| null` type surfaced three further sites that assumed the map was known — the re-send count, the everyone-already-sent banner, and the per-row Sent mark — all three fixed here. Second path filed as T-300 (renumbered from T-122 under D-9c). |
| T-300 | **The member record's Reach out prompt pre-ticks everyone when the already-sent read fails** — the same defect T-018 fixes on the send draft, on the second path. `app/member/[id].tsx` does `mergeSent(already.data ?? {}, …)` and passes the result down as each recipient's `sentAt`; `FollowUpTriggerPanel` then rebuilds the map with `sentMapOf` (`:618`, read at `:403`), a shape in which an absent `sentAt` means "not sent" and "unknown" cannot be said at all. So a rejected `fetchSentForPeriod` still ticks every flagged member on that screen. T-018 did not widen into it because the fix is a prop change on a shared component | T-018, B:F-06, C:RF-08 | 1.5 | Spec: `already` in error → the prompt ticks nobody and says why; `sentMapOf` can express unknown | — | ☐ Session B's surface, unclaimed |
| T-019 | `fetchSentForPeriod` promises "costs the mark and nothing else" then `checked()` throws at 1,000 messages | A:F-15, C:RF-08 | 1.5 (route) / 3 (page) | Covered by T-018 spec + T-045 | — | ☐ |
| T-020 | `send-followups/index.ts:282` discards insert error; `msgRow!`, `emailRow!` ×2 → TypeError aborts batch mid-loop | RV-10 | 1.5 | Spec: failed insert fails one recipient, not the batch | — | ⏳ C (18-Sep-2026). PR open. **Carries the Deno proof surface**, because the row had none: no `deno.json`, no Deno test in the repo and no Deno step in CI, and `npm run check` is Node scoped to `src/`, so no line under `supabase/functions/` had ever been executed by any job. Built first, per the T-012 precedent. Adds `supabase/functions/deno.json` and a `deno test --no-check` step in the `gate` job — the first execution of Edge Function code by any job here. **`--no-check` is temporary and T-027 widens this same config** to `deno check supabase/functions/**/*.ts` inside `npm run check`. Fix also moves the loop to `send-loop.ts` so a fake admin client can drive it, and replaces `emailRow!` with a discriminated union carrying the address. Deno is not installed on the dev machine, so CI was the spec’s first run anywhere. **PR #23. Spec GREEN in CI, run `35333793846`, 18-Sep-2026: `running 3 tests from ./send-followups/send-loop.test.ts` → `ok \| 3 passed \| 0 failed`.** The first execution of any Edge Function code by any job in this repository. It took a second push to get there: on the first run (`35332936064`) the step never executed at all, because `npm run check` fails ahead of it and a job stops at its first failed step — so the step was moved above `check`. That is filed generally as **T-402**, and the two runs are its evidence. Both jobs remain red at main’s baseline and neither failure is this branch’s: `gate` 1628 tests / 1614 pass / **13 fail**, identical to `main` run `35332129859`, and `db-harness` red on the same pre-existing spec files. RC-100. ☑ **PR #23, merged 18-Sep-2026 11:04 UTC, RC-100.** Final run `35337283950`: `Commit guards are reachable` green, `Edge Function specs` green (`ok, 3 passed, 0 failed`), gate 1656 tests / 1647 pass / 8 fail — identical to `main`’s baseline at `69fb2b78`, so nothing new. Merged under D-12a. |
| T-021 | PWA auto-reload fires on 60s idle or `hidden` while a send/import is in flight | RV-30, C:RF-20 | 1.6 | In-flight flag unit spec now; Playwright in Gate 2 | — | ☑ PR #32, 18-Sep-2026, RC-075. Merged `69fb2b78`. The `api.ts` overlap with T-017 was a real semantic conflict, not a keep-both: HEAD had the required `client_batch_id` and this branch the old optional signature plus the wrapper, so both were kept and then verified. Was: touches `src/data/api.ts` (`sendFollowUps`), which `fix/T-017` also changes; branched from `main`, so expect a conflict there if T-021 merges after T-017. **Code complete, branch `fix/T-021` pushed, PR not yet open.** RC-075 written. Spec `src/data/inFlight.test.ts` 9/9; typecheck clean, icons 75/75, contrast 2852/2852, suite at the 14-failure baseline this branch sits on. Six call sites hold the flag: `sendFollowUps`, `csvCommit`, `csvPreview`, and the three bulk importers (the delete holds it across the whole loop, since a flag released between members leaves a gap after each one). `isWriteInFlight()` is consulted OUTSIDE `safeToReload`, so neither `hidden` nor the idle timer can get past it; `safeToReload` itself is untouched and keeps its four cases. Short single-row writes are deliberately NOT wrapped — the flag would almost always be set and would quietly disable the deployment refresh. The Playwright half waits on **T-036**, as the row always said. |
| T-125 | **`0045_import_change_counts` is not applied to production** — RV-19 realised: two files share the prefix `0045` (`0045_import_change_counts`, `0045_member_inactive_from`) and only the second reached the ledger, under the name `member_inactive_from`. The import result screen's `added` / `updated` / `unchanged` / `absent_added` counts have **never computed live**: `commit_csv_import` returns no `changes` key at all. **Applying 0045 as written is forbidden** — it restates `commit_csv_import` and would revert 0060, 0073 and 0074 (T-112 class, RC-047's mechanism). Its deltas must be re-derived as in-place edits against the live body | T-120, T-033, D-10, RC-047 | 1 | A migration carrying 0045's counters as `pg_get_functiondef` edits; `52`'s three result-count assertions hold against the live body, not only the replay | Result screen shows non-zero counts after a real import | ☐ Re-confirmed 22-Sep-2026: `commit_csv_import` on production still lacks `v_absent_added`; it is the ONLY code-level difference left in that function after comments and whitespace are stripped (the replay minus 0035/0045/0046 matches production body-for-body except `bulk_import_members`, T-132). Not applied, per this row: needs the in-place re-derivation. |
| T-126 | **`set_attendance` does not exist in production.** `0035_set_attendance` is unledgered and the function is absent from `pg_proc` (verified 18-Sep-2026: 0 rows; the only live `set_*` functions are `set_member_active_from`, `set_member_status`, `set_offering_schedule`, `set_updated_at`). `repository.ts:3936` calls `supabase.rpc('set_attendance', ...)`, so marking a member present or absent by hand can only return PostgREST's "function does not exist". No such call appears in `edge_logs` for 17–18 Sep — the app has only been signed into — so the breakage is structural, not yet observed. `attendance_backdates_membership` (0046) is absent by the same route. **0035 also carries the scoped-recompute rationale 0074 cites**, so T-014's own precedent is not live | T-120, T-033, D-10 | 1 | `set_attendance` exists on production and one hand-mark round-trips; `27_set_attendance.sql` green | `select count(*) from pg_proc … proname='set_attendance'` returns 1 | ☐ ☑ **Applied 22-Sep-2026 20:08 UTC** via the Supabase MCP from `0035_set_attendance.sql` verbatim, ledgered `20260922200821 / 0035_set_attendance`. Verified read-only after the apply: `set_attendance(uuid, date, text)` present, `md5(prosrc)` = `076e4572f44a3c54e376807957cd1e32` (9,160 bytes), byte-identical to the full harness replay; `anon` cannot execute, `authenticated` can. Rehearsed first on a production-like replay (every file minus 0035/0045/0046): applied cleanly, body identical whether applied in sequence or last. **Proof column not fully met:** `27_set_attendance.sql` has one pre-existing red assertion in the repo's own replay too ("absent on a day she was not expected is refused" is ACCEPTED, because 0035 itself creates an all-enrolled session for an off-schedule day) — T-133. The hand-mark round-trip on production is not yet observed. |
| T-111 | 0073 re-emits `merge_member_into` from 0032's body and so **reverts 0061's two de-gendered refusals** (`0073:540` "merged into herself", `:568` "an email address of her own, so merging her"); production holds 0061's wording today (T-008 read). 0061 edited in place via `pg_get_functiondef`; 0073 restated. No spec pins 0061's wording, which is why 51 and the harness are green on it. **Blocks T-013 — 0073 must not be applied as written.** | T-008 read 17-Sep-2026, 0061, 0071 "rewritten in place, not restated" | 1.2 | Copy-lock on the two 0061 strings in `merge_member_into` (fails on 0073 as written); 0073 corrected before apply | `pg_proc` scan: no `\y(she\|her\|herself)\y` in a `raise exception` in `merge_member_into` after apply | ☑ PR #19, 17-Sep-2026, RC-047. Proof: `51_alias_unique_per_member.sql` red on run #107 (`4b41a68`) on exactly the new copy-lock, green on #108 (`0f1b1e5`), 22/22. Verify (17-Sep 13:01:35 UTC, post-apply): `merge_member_into` holds 0061's wording — `merged into themselves` true, `merged into herself` false, no `she/her` in any `raise exception`. |
| T-115 | `send-followups` **mints a `client_batch_id` when the caller omits one** (`index.ts:55`, `String(body.client_batch_id ?? crypto.randomUUID())`), so an idempotency key the client forgot to send is silently replaced by one that can never collide — every retry is a fresh batch and a second set of emails. The key must be **required**: refuse with 400 when it is absent, rather than inventing one. Second half: the 409 raised at `index.ts:183` answers with a bare sentence (`This send has already been submitted.`) and no batch, so a client cannot say what the first attempt did without reading `email_batches` back itself. Return the existing batch's id and counts in the 409 body. | T-017, RV-06 | 1.5 | Deno spec: a body with no `client_batch_id` → 400, no batch row written; a duplicate key → 409 whose body carries the first batch's `id`, `requested_count`, `sent_count`, `failed_count`, `status` | — | ☐ **Session C's surface** (`supabase/functions/`) — unclaimed. T-017 ships the client half (key always minted and passed, 409 rendered as a result state) and does not depend on this row; until it lands the server still tolerates a caller that omits the key. |
| T-113 | **Upload sends no network request from the PWA the operator is using** — no `csv-import` invocation, no gateway `edge_logs` entry, no `csv_imports` row (17-Sep 13:07–13:42 UTC, two attempts). Server-side diagnosis 13:45–14:02 UTC: (a) production `rosi-fit.vercel.app` serves `entry-a8a63660….js` built from `cfce708` = `main` HEAD (deployed 12:58:05 UTC, READY, 0 commits behind); the bundle carries `lhpzhkzbnquwjljmbylo.supabase.co` and the anon JWT, so `isConfigured` is true there and an upload on that origin must hit the network. (b) `sw.js` on prod is byte-identical to `public/sw.js`: `VERSION='v1'`, shell-only, no precache list, `/_expo/static/` cache-first by content hash, documents network-first, POST never handled — it cannot swallow a request. (c) Gateway logs: the app's last live session today (06:00–07:00 UTC) was sign-in only (`/auth/v1/token`, `/rest/v1/app_users`); no upload was attempted against production today. (d) **Symptom reproduced on the dev machine:** `npx expo export --platform web` with a correct `.env` (expo logged `env: export EXPO_PUBLIC_SUPABASE_ANON_KEY EXPO_PUBLIC_SUPABASE_URL`) still produced a fixture-mode bundle — no host, no key, `localhost:54321` fallback present — because of a **stale Metro transform cache**; `--clear` produced a live bundle. In fixture mode `app/upload.tsx:674` (`if (!staged.preview)` → `fixtureOutcome()`) reports the import completed instantly with no request. A local `expo start` on this machine shows exactly the reported behaviour. **Two tells for the operator:** (1) the address bar — `rosi-fit.vercel.app` is live; `localhost:8081` or any `rosi-fit-git-…` preview URL is suspect; (2) the sign-in phone field pre-fills `80563 29742` in fixture mode (`app/index.tsx:25`), empty in live mode. **Fix for a local session:** `npx expo start --clear` once (the `--clear` export on this machine produced a live bundle from the same `.env`). Underlying defect filed as T-114. Vercel env-var scoping (Production vs Preview) is not readable through the tools available; a Preview URL built without `EXPO_PUBLIC_*` would behave the same way. Cause is client-side and not diagnosable further without a browser trace. | T-013 verification attempt; `app/upload.tsx:166,:674`; `src/lib/supabase.ts:19` | 1.2 | Owner: the tab's origin (address bar), whether the phone field pre-fills, Network tab on one upload. Then one upload on `rosi-fit.vercel.app` produces `function_edge_logs` + a `csv_imports` row. Rung to file if confirmed: fixture mode must announce itself on screen, never silently report a write as done. **A.1 bundle check run by C, 18-Sep-2026, after the T-401/T-020/T-038 merges:** `GET https://rosi-fit.vercel.app/` → 200; entry bundle `entry-5a4929e40d4eef3799b7e91be86c0054.js` (2,630,563 bytes) → 200; **contains `lhpzhkzbnquwjljmbylo`: true**; **contains `localhost:54321`: false**. Vercel is serving a live-configured bundle, so the fixture-mode fallback is not what production is running. Note the build id has moved since this row was written (`entry-a8a63660…` → `entry-5a4929e4…`), which is the redeploy, not a new defect | — | ☐ |

## Gate 2 — Turn verification back on (freeze lifts at exit)

| ID | Defect | Sources | WO item | Proof | Status |
|---|---|---|---|---|---|
| T-022 | `npm run check` red: 9 failures | RV-03, A:F-09 | 2.1 | `npm run check` green | ☐ B counts **14** on 18-Sep-2026, not 9. Five of the fourteen are `aliasConflictTarget.test.ts`, retired by **T-121** under D-2b — 14 → 9 once that lands, and the nine are the original RV-03 set. |
| T-023 | `courses.tsx:7` dead `Dropdown*` imports; list-screen filter rule violated — **real regression** | A:F-28 | 2.1 | `formDropdownMenu.test.ts:148` passes by fixing code | ☐ |
| T-302 | **A source-reading spec that slices by CHARACTERS reads a different number of lines on Windows than in CI**, because a CRLF checkout spends one extra character per line. Measured 18-Sep-2026 on `memberJoinedOn.test.ts:141`: `src.slice(at, at + 600)` over `app/member/edit.tsx` spans **11 lines CRLF, 12 lines LF**, and the twelfth carries the `testID` the assertion looks for — so the case fails on the dev machine and passes in CI, which is why the register's "nine RV-03 failures" is eight in the `gate` job. **The class is wider than the one case:** every `slice(at, at + N)` in the suite has the same exposure, including three Session B wrote this week (`sendBatchKey.test.ts` 700, `sendFailsClosed.test.ts` 500, `inFlight.test.ts` — already converted to a whole-function search after it bit). `.gitattributes` does not exist and `core.autocrlf=true`, so the two checkouts genuinely differ | T-024, measured against runs 35332977464 / 35333028648 | 2.1 | Windows and CI agree on the failure count; the windows are line-based or delimiter-based, not character-based | — | ☐ Session B's surface (`src/`). Take it WITH T-024, whose fix is the same change to the same line |
| T-024 | `memberJoinedOn.test.ts:141` brittle 600-char window | A:F-40 | 2.1 | Test passes on the passing implementation | ☐ |
| T-025 | `message.test.ts` ×5, `rosterFilter.test.ts` ×2 copy-locks not re-pinned | RV-03 | 2.1 | Each still pins a rule | ☐ **Half cleared, 24-Sep-2026.** The two `rosterFilter.test.ts` locks are re-pinned and green: the academy asked the dropdown for two more options by name, so changing that list was the intent of the work, which is the one case the append-only rule leaves open. Both now pin the whole current list — `All members · Present · Absent · Yet to mark · No email · Bounced · Unsubscribed · Active · Inactive` — including the `Active`/`Inactive` pair that was already in the tree and already failing these locks before this change went near them. Only the string literals moved: no assertion removed, nothing skipped, no matcher loosened. The suite went 8 fail → 6 on this change, and **the remaining 6 are the `message.test.ts` ×5 plus `formDropdownMenu.test.ts:148` (T-023)**, none of them touched here. `message.test.ts` still owes its five. |
| T-026 | `gate`/`db-harness` not required; Vercel deploys on red; no post-deploy smoke; `post-release-monitor` unscheduled | RV-20, A:F-10, A:§10 | 2.2 | Branch protection screenshot in PR; red CI blocks a test deploy | ☐ |
| T-027 | `tsconfig` excludes `supabase/`, `scripts/`, `**/*.test.ts` — Edge Functions and specs untypechecked | RV-21, A:F-11 | 2.3 | `deno check` in `npm run check`; introduce a type error in a scratch branch, watch it fail | ☐ **The config already exists — widen it, do not create it.** T-020 added `supabase/functions/deno.json` and a `deno test --no-check` step in the `gate` job because its spec had nowhere to run; that step is the first execution of Edge Function code by any job in this repo. T-027 owns three things on top: turn `--no-check` off by wiring `deno check supabase/functions/**/*.ts` into `npm run check`, remove `**/*.test.ts` from the client tsconfig exclude, and leave `supabase/` excluded from the client tsconfig (RV-21 is explicit that removing it is the wrong fix). One known cast waits for this row: `admin as unknown as AdminLike` at `send-followups/index.ts` where it calls `runSendLoop`. ⏳ C (18-Sep-2026). **Measured before starting, and half this row’s premise is wrong.** RV-21 and A:F-11 say the specs are untypechecked. They are not: `scripts/tsconfig.json` already includes `../src/**/*.test.ts`, `tsc -p scripts/tsconfig.json --listFiles` resolves **130 spec files**, and that project is the second half of `npm run typecheck` and **passes**. Every spec in `src/` has been typechecked all along. There are 0 specs in `app/` and 0 in `scripts/`. **Removing `**/*.test.ts` from the client tsconfig exclude is the WRONG fix and the repository already says why**: `scripts/tsconfig.json`’s own header records that the app config sets `customConditions: ["react-native"]`, under which `@types/node`’s exports do not resolve, “so these files are checked separately rather than loosening the app’s own config to accommodate them”. Doing it anyway produces **435 errors, 385 of them TS2591** (`Cannot find name ‘node:test’`, `node:assert/strict`, `node:fs`, `node:path`) — exactly the failure that header predicts. Measured on this tree 18-Sep-2026, then reverted. **So the exclude stays, and the genuine gap is the one the row also names: the Edge tree.** `supabase/functions/` holds 1 spec and 11 functions, none of them typechecked by anything — the client tsconfig excludes `supabase/` (correctly, RV-21 is explicit) and T-020’s CI step runs `deno test --no-check`. That is what this row now delivers: `deno check` over the Edge tree wired into `npm run check`, turning `--no-check` off. **PR #33.** First run `35345932542`: `Edge Function specs` executed **without `--no-check`** and passed 3/3, so `send-loop.ts`, its spec and their module graph are the first Edge code this repository has ever type checked. But `check:edge` itself did not run — it sat behind `test:unit` in the `&&` chain, the same trap T-020’s Deno step fell into, so it was moved ahead of it. The other ten functions are reached only by `check:edge`. **The `customConditions` finding, kept here as the standing reason the client exclude must not move:** `tsconfig.json` sets `customConditions: ["react-native"]` through `expo/tsconfig.base`, and under that condition `@types/node`’s `exports` do not resolve. Any future change that pulls `**/*.test.ts` or `supabase/` into the client project reintroduces 435 errors, 385 of them `TS2591`. The separation is deliberate and documented in `scripts/tsconfig.json`’s own header; it is not an oversight to be tidied up. ☑ **PR #33, merged 19-Sep-2026 06:12 UTC, RC-104.** **The Edge tree type checks clean** — final run `35351221777`: `OK [EDGE TYPES] 25 file(s) in supabase/functions type check clean (deno 2.9.7)`. Nothing was wrong with it; it had never been asked. So there is nothing to file from the sweep, which is the honest answer and not a quiet one. Gate 1668 / 1659 / **8 fail**, identical to `main`’s baseline. Three runs were needed and **all three failures were in the check, not the code**: `check:edge` sat behind the red `test:unit` and never executed (`35345932542`); then `nodeModulesDir` was missing so `npm:@supabase/supabase-js` would not resolve at `_shared/db.ts:1:46` (`35346441309`); then that setting was present and never read, because `deno check` ran from the repository root and Deno looks for `deno.json` from its cwd upwards (`35347901748`). Recorded because a check that goes red for reasons unrelated to its subject is how a check gets ignored. |
| T-028 | `requestSize.test.ts` `MUST_CHUNK` is a 2-entry allowlist | RV-27, B:T-02, C:L | 2.4 | Every `.in()` in `src/**` + `supabase/functions/**` chunked or `BOUNDED` with reason | ☐ |
| T-029 | `edgeFunctionPagedReads.test.ts` pins one file | RV-28, B:T-02 | 2.4 | Walks every function dir | ☐ |
| T-030 | `pagedReads.test.ts` never scans `supabase.rpc(` | B:T-03, A:F-02 | 2.4 | RPC set-returning reads paged/bounded/exempted | ☐ |
| T-031 | No rule against dropping an index still named in `ON CONFLICT` | A:§12-P4, FR test | 2.4 | The rule is carried IN-DATABASE by `51_alias_unique_per_member.sql:60-63` and generalised from there. **Repointed 18-Sep-2026:** this Proof named `aliasConflictTarget.test.ts`, which T-121 retired under D-2b, so it pointed at a file that no longer exists | ☐ |
| T-032 | Unchunked `.in()` in `resolveContext` `:1574-1602`, `fetchNotifications` `:2144`, `csv-import:297-303` | A:F-29, C:RF-15 | 2.4 (covered by T-028) | T-028 names each with reason or chunks it | ☐ **Split by surface (C, 18-Sep-2026).** The row names three sites on two surfaces and they cannot move together. `resolveContext :1574-1602` and `fetchNotifications :2144` are `src/data/repository.ts` — **B**. `csv-import:297-303` is **C**: three consecutive unchunked reads, `course_offerings .in(id, offeringIds)` then `courses .in(id, courseIds)` then `branches .in(id, branchIds)`, each also discarding its `error` (that half belongs to T-046, also C). **Ordering is deliberate and C is waiting on purpose.** `requestSize.test.ts` today is a two-entry `MUST_CHUNK` allowlist that does not look at `supabase/functions/**` at all, so chunking `csv-import` now would land a fix under a guard that could not have caught it and would not catch the next one — the exact shape RC-039 to RC-043 to RV-05 records three times over. **B owns widening the guard (T-028); C owns the fix and takes it once the widened guard is red on these lines.** C does not edit `src/data/` to widen it, per D-9a; this note is the request. Same dependency as T-041, which is C’s Gate 3 row against the five `send-followups` reads. |
| T-033 | Duplicate migration prefixes `0038`×2, `0044`×3, `0045`×2, `0057`×2, `0041` gap; test prefixes `30`,`34`,`48` | RV-19, A:F-19, B:unknown-9 | 2.5 / D-3 | Collision check fails-first naming all four | ☐ Ledger scheme (read 17-Sep-2026): every remote `schema_migrations` row is a 14-digit timestamp `version` with `name` = the file name (`20260916050449 / 0071_duplicate_is_per_course`); local files are `00nn_`-prefixed, so the two ledgers share no version strings. New applies follow the remote shape: 0073 is recorded as `20260917hhmmss / 0073_alias_unique_per_member` via `migration repair`. 22-Sep-2026: nine after-the-fact ledger rows inserted for the migrations whose effects were already live but unrecorded — `20260922201501..09` for 0016, 0017, 0018, 0025, 0031, 0034, 0037, 0038, 0044_override_scoped_by_meeting_instance — each with a `statements` note saying no SQL was run. `list_migrations` now names every local file except 0045_import_change_counts (T-125). One ledger row, `0039_meeting_code_groups_commit`, has no local file. |
| T-034 | Gates G1/G2/G3 crash (`design/tokens.json`), G5 empty, G6 blocked, G8 nonexistent script, G10/G13 inert, `conformance.json` all BLOCKED | A:F-10 | 2.6 | Each gate either passes or is deleted with a `DECISION_LOG` line | ☐ |
| T-035 | ESLint configured, not installed, not run; CI comments say no runner exists while `check` runs 1,628 tests | RV-22, A:F-10 | 2.6 | `lint` script + CI step; both CI files' comments corrected together | ⏳ C (18-Sep-2026). Branch `fix/T-035` pushed; PR pending tooling. Installed pinned after checking each name and peer range against the registry: `eslint@10.10.0`, `typescript-eslint@8.70.0` (peers: eslint 8.57 or 9 or 10, typescript >=4.8.4 and <6.1.0, against this repo’s `~6.0.3`), `eslint-plugin-react-hooks@7.1.1`. `"lint": "eslint app src --max-warnings 0"` runs at the FRONT of `npm run check`, so CI gets it through the existing check step rather than invoking ESLint twice. Two config changes the first real run forced: a TypeScript parser, without which every matched file reported a parse error instead of no findings; and `react-hooks` **registered but not enabled**, because three `eslint-disable-next-line react-hooks/exhaustive-deps` comments already exist and ESLint refuses a directive naming a rule it cannot find — enabling `exhaustive-deps` would mean auditing every hook in `app/`, which is B’s surface and a separate decision. The config keeps its ONE rule and no `extends`, so lint cannot bury a real finding under cosmetic ones. **Proven both ways locally:** clean tree exits 0; a `supabase.from()` written outside `src/data` exits 1 with the RC-039 message. Both CI files corrected together and verified still identical apart from their two intentional header lines. **Step name: the count was dropped, not refreshed** — `135` had been wrong through 776 and past 790, and substituting today’s figure only schedules the next stale one; say so if a number is wanted there instead. Also fixed: `test:unit` was listed as “no JS test runner configured” while the step above it ran the whole unit suite. RC-102. **Known merge conflicts, both trivial:** the `check` script line collides with T-038 (keep both) and the `ci.yml` hunk sits near T-020’s Deno step. |
| T-036 | No runtime test of any screen; 19 `.harness/` checks wired to nothing | A:F-12 | 2.6 | ≥3 harness checks in CI | ☐ |
| T-037 | No DB gate — `test:db` is not one of the 13 | A:F-10 | 2.7 | G14 exists and blocks | ☐ |
| T-038 | No `supabase/config.toml`; `verify_jwt` only in comments; already flipped once on redeploy | RV-17, A:F-20 | 2.8 | Spec: every function dir has an entry | ⏳ C (18-Sep-2026). Branch `fix/T-038` pushed; PR pending tooling. **Production read read-only 18-Sep-2026** to record what is deployed rather than what a September document says. Public and matching source: `auth-login` v11, `auth-bootstrap` v12, `auth-lookup` v5, `recovery-check` v11, `ses-feedback` v4, `unsubscribe` v4 — note the last three have been redeployed since `ENVIRONMENTS.md` last read them at v1 and `verify_jwt=false` survived each time. Authenticated and matching: `pin-issue` v13, `pin-reset` v13, `csv-import` v17, `send-followups` v18. **One divergence, filed as T-127: `pin-reset-request` v5 is deployed `verify_jwt=true` while its own source says it must be public.** `config.toml` records the source posture for that one, so deploying it changes production — flagged in the file header and not done here. **Four functions state no posture at all** in their headers (`pin-issue`, `pin-reset`, `csv-import`, `send-followups`); their values came from the deployed state and the silence is recorded beside each entry. Also noticed, not filed: `auth-lookup`’s header says it is “the sixth public function; the previous five are” and then names three — the count is right, the list is short by two. Spec is `scripts/audits/check-function-jwt.mjs`, wired into `npm run check`, proven against four broken trees (undeclared dir, missing `config.toml`, section with no value, misspelt section). RC-101. ☑ **PR #27, merged 18-Sep-2026 11:13 UTC, RC-101.** Final run `35338074359`: gate 1656 / 1647 / 8, identical to `main`’s baseline; `check:functions` green on the rebased tree (11 declared, 7 public, 4 authenticated); both earlier proof steps still green. Merged under D-12a. **Changes nothing on its own** — it takes effect at the next deploy, which D-14 gates on T-400. |
| T-039 | Harness has no volume fixtures; every SQL test is a handful of rows | A:§12-P4, B:T-04, C:§23-P0 | 2.9 | `seed_scale.sql` at 500/2,000/5,000 runs | ⏳ A (seed only, first commit of fix/T-014; timings stay open) |
| T-040 | Harness cannot assert grants; `migrationGrants.test.ts` covers functions only | A:K-04 (KL-008), RV-16 | 2.9 → used in G5 | Table/column grant coverage added | ☐ |
| T-124 | **The migration rehearsal runs on a different Postgres major version than production.** `CLAUDE.md` makes the local harness the whole of the pre-flight check — “replay every migration from scratch against a fresh local Postgres 16” — and the `db-harness` CI job installs psql 16 to match. Production is **17.6.1** (read 18-Sep-2026). A replay that is green on 16 is evidence about 16. The gap is narrow for ordinary DDL but not empty: 17 changed planner behaviour and several catalogue views, and the harness is explicitly the thing that certifies a migration before it reaches a project it does not resemble | T-038 production read 18-Sep-2026, `CLAUDE.md`, `.github/workflows/ci.yml` psql 16 step | 2 (with T-037) | Harness and the `db-harness` job both pin Postgres 17 to match production, and the version is asserted rather than assumed — the suite fails if `server_version_num` is below the production major | ☐ Filed by C 18-Sep-2026 out of T-038. Harness and CI matrix are **A’s surface** for the harness half; C owns the workflow file and will take that half on A’s word about which version to pin. |
| T-112 | Migrations that restate a function body from a historical file revert later in-place edits (0061/0071 `pg_get_functiondef` idiom). 0073 did it to `merge_member_into` (T-111); sweep of every restatement after 0060 found no second instance yet | T-111, 0071 header | 2.4 | Rung: spec fails if a migration `CREATE OR REPLACE`s a function edited in place by any migration numbered between the restated source and itself | ☐ |
| T-121 | `src/data/aliasConflictTarget.test.ts` (FR's source scan) fails ×5 since 0073 became an in-place edit — it resolves function bodies from migration FILES, the diff RC-047 named as wrong: a file is not what the database runs. `51_alias_unique_per_member.sql` already asserts the same class in-database after a full replay. Retire the source scan; **B deletes the file** (`src/`), authorised here as **D-2b** — a spec whose premise RC-047 disproved is not a protected lock. B counts 14 failures in `npm run check`; five are this file | T-111, RC-047, D-2b, B's 14-failure count | 2.1 / 2.4 | File gone, `npm run check` failures 14 → 9, and T-031's rule carried by 51 instead | ☑ PR #26, 18-Sep-2026, RC-073. Merged `52a4dec9`, first of the six because D-12's own note put it on the critical path. Gate on merged main is now **8** in CI. D-2b's premise was verified before deleting: `51_alias_unique_per_member.sql:60-63` asserts that `pg_get_functiondef` matches `on conflict (alias_type, alias_normalized)` in **0** rows of `pg_proc` after a full replay — the same class, asked of what the database runs rather than of what a file says, which is strictly stronger than the scan being retired. **Code complete, branch `fix/T-121` pushed, PR NOT YET OPEN.** RC-073 written. **Measured: `test:unit` 1,619 cases, 14 failures → 9**, and the nine are exactly the RV-03 set (T-023 ×1, T-024 ×1, T-025 ×7). Typecheck clean, icons 75/75, contrast 2852/2852. The register count and the machine now agree for the first time since 0073. T-031 Proof repointed to 51 in the same sweep. |
| T-117 | **The commit guard cannot see a harness spec.** `scripts/hooks/pre-commit-guard.sh:94-101` (G1) accepts a change as tested only when a path matches `(\.spec\.\|\.test\.\|tests/cases/)`, so a commit whose ONLY change is a `supabase/tests/*.sql` spec is blocked and needs `CASES-NA:` — the token then says "no test" about a commit that is nothing but a test. `code_changed()` also matches `supabase/` wholesale, so a spec drags in G2's TEST_SUMMARY obligation as though it were application code. Second, smaller: `.claude/hooks/pre-tool-use-guard.mjs:38-45` reads a `-F <file>` message at hook time, so the file must already exist from an earlier tool call — writing the message and committing in one call always fails. Hit on the T-011 commit (`ab6bac6`) and the 52 commit (`b069661`); both shipped with a `CASES-NA` that is literally untrue | this session, T-011, T-014 | 2.6 | G1 counts `supabase/tests/*.sql` as a test case; a spec-only commit needs no token | ☐ (Session C's surface — unclaimed. Filed as T-117, not T-115: B took that number) **Addendum 18-Sep:** a third failure mode, worse because it blocks work that is correct. The PreToolUse adapter computes its push range in the SESSION'S working directory, not the worktree the `git push` runs in — under D-9b those differ. If the session's checkout has fallen behind `origin/main`, `origin/main..HEAD` holds **no commits** (so the escape text is empty and no token is visible) but a **non-empty diff**, and G1 blocks a push whose own commits are correctly tokened. Workaround: fast-forward the session's checkout before every push. Observed repeatedly 18-Sep on `fix/T-014`. |

## Gate 3 — Bounded reads and RLS cost

| ID | Defect | Sources | WO item | Proof | Status |
|---|---|---|---|---|---|
| T-041 | **`send-followups` has EIGHT `.in()` reads, none chunked or paged, and SIX discard the error** (measured 19-Sep-2026; the original cell said four plus a fifth, and four of five). `:109` members (checked), `:114` member_enrollments, `:120` course_offerings, `:126` courses, `:128` branches, `:139` course_communication (checked), `:153` member_emails, `:156` member_stats. Four scale with the recipient list and `:120` scales with it through the enrolments; `:126`, `:128` and `:139` are bounded by the course and branch counts but unchunked and discarding all the same | RV-05, A:K-02 (KL-009), B:F-03, C:RF-03 | 3 | Fake-client test at 1,001 recipients: no recipient classified from a missing map entry; T-028/T-029 fail-closed | ⏳ C (19-Sep-2026). **Counted before starting: there are EIGHT `.in()` calls, not five, and SIX discard their error.** Current lines: `:109` members, `:114` member_enrollments, `:120` course_offerings, `:126` courses, `:128` branches, `:139` course_communication, `:153` member_emails, `:156` member_stats. Four scale directly with the recipient list (`memberIds`: 109, 114, 153, 156) and `:120` scales with it through the enrollments; `:126`, `:128` and `:139` are bounded by the course and branch counts but are still unchunked and still discard. Only `:109` (`mErr`) and `:139` (`ccErr`) check their error at all — the row says 4 of 5, the tree says 6 of 8. `_shared/pageAll.ts` already exists with `pageAllByKey`, `PAGE_SIZE`, `PAGE_CAP` and `PagedReadError`; what it lacks is `inChunks`, `MAX_IDS_PER_REQUEST` and the `ChunkedSource` branch, which is the port this row names. The contract is pinned to the client copy by `src/data/edgeFunctionPagedReads.test.ts` (B’s surface), so the port must match `src/data/pageAll.ts` clause for clause rather than be rewritten. |
| T-042 | `member_period_metrics` proper fix — keyset for all three callers (owner, 18-Sep-2026, superseding the `period_totals()` half of the WO plan; that returns as **T-301**) | A:F-02, B:F-02, T-006, RC-071 | 3 | Fake-server 999/1,000/1,001 → correct totals, nobody zeroed | ☑ PR #31, 18-Sep-2026, RC-074. Merged `88df27dd`, last of the six and held back until the owner confirmed 0075 applied to production (A, 09:46 UTC, ledger `20260918094609`). **0075's FILE is still not on `main`** — A's #22, blocked on a CI trigger — so the repository and the database disagree until it lands (D-10). Was: **code complete, stacked on `fix/T-016`.** RC-074 written. Spec `src/data/periodMetricsPage.test.ts` 11/11 against a fake RPC holding **1,087** rows: all present, none duplicated, none zeroed, row 1,001 carries real figures, the keyset advances, a failed page takes the whole read down. Typecheck clean, icons 75/75, contrast 2852/2852; suite 14 failures, which is the pre-T-121 baseline this stack sits on. **Written against A's stated signature — `0075` is NOT applied, so nothing here is verified against production.** Merge with T-016, after A confirms the apply. |
| T-301 | **The two SUMMING callers now transfer a row per member to add up two numbers.** T-042 put `fetchWeekRows` and `fetchBucketMetrics` onto the keyset pager along with `fetchMembers`, per the owner's instruction, so each of them pages 1,087 rows per period and reduces them to `expected` and `attended`. Correct, and wasteful in exactly the way `0067` was written to avoid for the week strip (673 kB → 600 B). The work order's Gate 3 plan was `period_totals(p_from, p_to)` server-side for these two, returning one row; that is still the right shape and is what this row restores. `fetchMembers` keeps the keyset — it genuinely needs a row per member | T-042, RC-074, A:F-02, WO §4 Gate 3 | 3 | Spec: 1,087 members → one row returned to each summing caller, and the totals equal the sum the keyset produced | — | ☐ Needs a migration (`period_totals`), so the SQL half is Session A's; the three-line client change is B's |
| T-043 | 47 RLS policies call `is_active_app_user()` bare — re-evaluated per row; 0013 fixed two and never generalised. **Measured 17-Sep-2026: 59 of 62 policies across 32 tables, none wrapped** (38 `is_active_app_user()`, 21 `is_super_admin()`, 28 `is_subscription_writable()`); C:RF-07’s 47 undercounts | C:RF-07, T-009 read, T-118 | 3 | **Catalogue spec, not the lint.** `09_grants.sql` walks `pg_policies` for `schemaname='public'` and asserts that no `qual` or `with_check` contains a bare `public.is_*(` outside a `( select … )` wrapper. Fail-first must name 59 of 62 before the fix and 0 after. **The `auth_rls_initplan` lint is NOT an exit condition** — it follows `auth.*` and not user-defined helpers, and returned 0 findings on 17-Sep with the defect fully present (T-118). Record the lint as a read; never gate on it | ☐ Proof column rewritten 18-Sep-2026 on the owner’s instruction, per T-118. |
| T-044 | `offering_schedules` read whole, unguarded, in `fetchCourses`/`fetchOfferings`/`fetchBranchUsage`; grows with time | C:RF-13 | 3 | Spec: 1,200 schedule versions → correct current weekdays | ☐ |
| T-045 | `TruncatedReadError` bypasses `fail()` translation — developer sentence reaches operator; `checked()` kills `fetchCourseDayRows`, `fetchAttendance`, `fetchSentForPeriod` at 1,000 | A:F-25, A:F-14, C:RF-09 | 3 | Spec: 1,000-row course-day renders; error text is person-readable | ☐ |
| T-046 | 32 reads discard `error` → false zeros incl. inside a sent email; `fetchRules` silently applies global threshold | RV-18, A:F-13 | 3 | Spec: no un-annotated discard remains | ☐ |
| T-047 | `upload.tsx:963` renders `err.message` raw; `ENGINE_WORDING` misses `42P10` wording | FR §9 | 3 | Spec: `42P10` text → translated sentence | ☐ |
| T-048 | `v_present_ids` `array_append` + `= any()` O(n²) in `commit_csv_import` | C:RF-01-C, T-005 read | 3 (if T-062 timing says so) | Timing at 2,000 rows under 8s | ☐ **Two 8s walls, not one (T-005, 17-Sep-2026).** `authenticator` carries `statement_timeout=8s` **and `lock_timeout=8s`**; `service_role` sets no `rolconfig` of its own and so inherits both. The timing run must tell them apart: a statement that runs too long fails `57014 query_canceled`, while a second writer blocked on `member_stats` fails `55P03 lock_not_available` — the second is the lock contention C:RF-01 names as its secondary effect, and it aborts at 8s rather than blocking. A run that only records wall-clock will not distinguish them. |
| T-109 | A failed `commit_csv_import` leaves no server-side trace: the `status='failed'` / `error` write sits inside the transaction that aborts, so it rolls back with it. `csv_imports.error` is null on all 56 rows ever written and no row has ever held `status='failed'`. A stranded `previewed` row is the only evidence a commit was attempted, and it cannot be distinguished from a preview the operator abandoned | T-007 read 17-Sep-2026 | new — placed in Gate 3, move if it belongs elsewhere | Harness: a commit that raises inside the transaction still leaves a durable `failed` row naming the SQLSTATE (written by the caller after the rollback, or from an autonomous transaction) | ☐ Addendum 17-Sep 14:05 UTC: the gateway `edge_logs` DO record the attempts — `POST /rest/v1/rpc/commit_csv_import` → **400** at 01:02:01, 01:03:12, 01:54:52, 01:55:31 UTC, each ~1.5 s after its preview's `POST /rest/v1/csv_imports` 201; the one 200 (01:06:33) is the zero-unmatched file. The database holds no trace; the gateway holds it for the log retention window only. |
| T-114 | **A production-configured build can silently fall back to fixture mode** (`localhost:54321`, fake outcomes, no request) when the Metro transform cache is stale; nothing in the build or at runtime refuses to ship or run an unconfigured bundle. Reproduced 17-Sep-2026 on the dev machine: `expo export` with a correct `.env` → bundle with no host and no key; `--clear` → live bundle. `isConfigured=false` is only a `console.warn` in `__DEV__` (`src/lib/supabase.ts:21`); `upload.tsx:674` then reports an import completed without a request | T-113, `app/upload.tsx:674`, `app/index.tsx:25`, `src/lib/supabase.ts:19-28` | 3 | (1) a spec that fails the export when the bundle lacks the Supabase host; (2) a runtime refusal in the app when `isConfigured` is false on a non-localhost origin | ☐ |
| T-118 | **T-043's proof cannot detect T-043's fix.** T-043 exits on “`09_grants.sql` asserts no bare helper; T-009 lint clears” — but the `auth_rls_initplan` lint is **already** clear, today, with the defect fully present. Measured 17-Sep-2026 21:13 UTC: `get_advisors(type:'performance')` returns 0 `auth_rls_initplan` findings, while `pg_policies` shows 59 of 62 policies across 32 tables calling `is_active_app_user()` (38), `is_super_admin()` (21) or `is_subscription_writable()` (28) with **none** wrapped in a scalar subquery. The two policies that do reference `auth.uid()` were already wrapped by 0013, so the lint has nothing to report; `app_users_self_update` holds a wrapped `auth.uid()` and a bare `is_subscription_writable()` in one predicate and the lint flags neither. The lint follows `auth.*`, not user-defined helpers. Half of T-043's exit condition is therefore a green that means nothing, and the `09_grants.sql` assertion is carrying the whole gate alone. Second correction from the same read: C:RF-07's “47 policies” undercounts — the measured figure is **59 of 62** | T-009 read 17-Sep-2026, C:RF-07, T-043 | 3 (with T-043) | T-043's exit drops the lint clause and rests on `09_grants.sql` asserting zero unwrapped helper calls across all 62 policies, fail-first at 59. Lint stays a recorded read, never an exit | ☐ Filed by C 18-Sep-2026. Fix is SQL plus a `supabase/tests` assertion — **A's surface**. |

## Gate 4 — Durable, idempotent bulk send (introduces `pg_cron`)

| ID | Defect | Sources | WO item | Proof | Status |
|---|---|---|---|---|---|
| T-049 | Send is one synchronous loop on an open browser request; terminal status written only at end; no resume, reconcile, or listing | RV-11, RV-07, A:F-04, B:F-04, B:F-32, C:RF-04, C:RF-06 | 4 claim-and-drain | Kill drain mid-slice → every row defensible; status derived from rows; same key retry sends nothing twice | ☐ |
| T-050 | `member_period_metrics` once per recipient in the loop; one batch, two versions of the truth if an import lands | RV-07, C:RF-04-F, C:§18-12 | 4 | Metrics read once per batch | ☐ |
| T-051 | SES non-2xx = permanent `failed`, `attempt_count` literal 1; no backoff, rate limit, quota awareness, or `AbortSignal` | RV-08, B:F-26, C:RF-11 | 4 | Fake provider 429,429,200 → one delivery, `attempt_count`=3 | ☐ |
| T-052 | Timeout after SES accepted recorded as `failed`; retry double-sends | RV-09 | 4 | `unknown` state; reconciled by `provider_message_id` | ☐ |
| T-053 | `sending` is terminal in practice; nothing reads `sending`/`queued`/`processing` back | B:F-14, C:RF-06 | 4 sweeper | `pg_cron` job marks stale rows `unknown`; surfaced on result screen | ☐ |
| T-054 | Writes have no third state; "Nothing was written / sent" asserted on transport failure (`upload.tsx:672,:716`, `send/index.tsx:238`) | RV-12, A:F-24, B:F-10 | 4 | Spec: dropped response → "did not hear back" naming batch/import id | ☐ |
| T-055 | Already-sent guard is advisory, client-side; no server refusal | RV-31, B:F-06 | 4 / D-6 | Server refuses without `resend:true` | ☐ |
| T-056 | `fetchNotifications` `.limit()` with no `.order()` — tray freezes on oldest failures | C:RF-16 | 4 | Spec: 100 historical failures → newest shown | ☐ |
| T-057 | No cancellation of a running send or import; `callFn` passes no `AbortSignal` | B:F-31 | 4 (drain makes cancel = stop claiming) | Cancel action leaves queued rows unsent | ☐ |

## Gate 5 — Atomic security controls and integrity guards

| ID | Defect | Sources | WO item | Proof | Status |
|---|---|---|---|---|---|
| T-058 | `auth-login:64-70` read-then-write `failed_attempts`; parallel guesses never trip lock | RV-13 | 5 | Concurrency spec: N simultaneous wrong PINs → lock at 5 | ☐ |
| T-059 | Same in `recovery-check:105` + upsert, `pin-reset-request:69-88`; limit-row read error discarded; default conflict target | RV-14 | 5 | Same spec per function | ☐ |
| T-060 | `failed_attempts` never resets on `locked_until` expiry; + `auth-lookup` oracle + no rate limit on `auth-login` = permanent lockout of every staff account | A:F-08, A:F-42 | 5 | Spec: lock expires → counter 0; `auth_rate_limits` applied to `auth-login` | ☐ |
| T-061 | `recovery-check` never checks `is_active` | RV-15 | 5 | Spec: disabled super-admin refused | ☐ |
| T-062 | `member_emails_unique_live` dropped, no replacement; `add_email` branch unguarded | RV-04, A:F-03, B:F-09 | 5 trigger | Harness: duplicate address in same course refused with Edit form's sentence; and `04_members.sql:58` re-inverted to `t.rejects` against the new trigger (D-2a) | ☐ |
| T-063 | `commit_csv_import` `add_as_new` creates members outside the course advisory lock | A:F-03, B:C-5 | 5 | Harness: concurrent import + Add Member → one record | ☐ |
| T-064 | `authenticated` holds direct `insert,update` on `members`, `member_emails`, `member_aliases`; `src/lib/supabase.ts` header says otherwise; no column guard | RV-16, A:F-27 | 5 | T-040 grant spec; direct PATCH refused or guarded | ☐ |
| T-065 | `audit_attendance` update-only; `audit_members` no delete | RV-32 | 5 | Trigger spec, or documented out-of-scope | ☐ |
| T-066 | `ses-feedback` secret in `?s=` (logged); no SNS signature verification | B:F-27, B:S-1 | 5 | Forged notification with valid secret, bad signature → refused | ☐ |
| T-067 | Any active staff can mail whole academy via `requireCaller`; no volume cap | B:S-2 | 5 — **owner decision** | Cap spec if decided | ☐ |
| T-127 | **`pin-reset-request` is deployed `verify_jwt=true` while its own source says it must be public, so the PIN-reset path refuses exactly the people it exists for.** Measured on production 18-Sep-2026, read-only: slug `pin-reset-request`, v5, `verify_jwt: true`, ACTIVE. Its header states the posture in the same terms `recovery-check` uses — the caller is there BECAUSE they cannot sign in, so there is no session to verify — and `recovery-check` v11 is correctly public. A locked-out staff member calling this gets 401. Compounds T-060: that row already shows `failed_attempts` never resets on `locked_until` expiry, so an account can be locked permanently; this row is why the documented way out does not work either. First recorded in `ENVIRONMENTS.md` under “Noticed in passing, NOT changed” during the 09-Sep SES pass and never changed since | T-038 production read 18-Sep-2026, `ENVIRONMENTS.md`, RV-17 | 5 (with T-060) | Deploy `pin-reset-request` with `verify_jwt=false` from `supabase/config.toml`, then re-read `list_edge_functions` and assert `verify_jwt` is false; an unauthenticated POST must reach the function rather than 401 | ☐ Filed by C 18-Sep-2026 out of T-038. **Not fixed there:** `config.toml` already records the correct posture, but applying it is a production **write** on an auth endpoint — A’s surface under D-9a, and needs the owner’s explicit go-ahead. Nothing was deployed. **Ruling 18-Sep-2026: the owner flips `verify_jwt` off for this function in the Supabase dashboard — NOT by redeploying.** A redeploy would push repository code over whatever is actually running, and no deployed function has ever been diffed against this repository (T-400). The dashboard toggle changes the gateway setting and leaves the running code alone, which is the smaller and reversible action. **Verification, after the owner confirms the toggle:** one unauthenticated `POST` to the function URL with no `Authorization` header. Expect **the function’s own 4xx validation** — its complaint about a missing or malformed body — and **not** a gateway `401`. A 401 means the toggle did not take. Paste the status line and body verbatim into this row with the time. C runs the read once the owner says the toggle is done. |
| T-400 | **No deployed Edge Function has ever been diffed against `supabase/functions/`.** Every one of the eleven was deployed by hand, at different times, from different working copies — the entrypoint paths read back from the platform still show it: `auth-login` and `recovery-check` resolve bare under `source/<name>/`, `csv-import` under `source/functions/<name>/`, and the other eight under `source/supabase/functions/<name>/`. Three different upload roots. Versions run from v4 to v18 and several predate migrations that changed the tables they read. Nothing establishes that the code running in production is the code in this repository, and T-120 has already shown exactly this class one layer down: 15 database functions in production differ from what a full harness replay produces. The Edge tree has never been asked. Until it is, every step of the form “the function does X, because the source says so” is unfounded — including where T-020, T-038 and T-041 rely on it | T-038 production read 18-Sep-2026, T-120, T-127 | 2 (before any redeploy) | Read each deployed function’s source — `supabase functions download <name>`, or the dashboard viewer — and diff against `supabase/functions/<name>/`. Record per function: identical, or the diff. **Any difference is a T-120-class finding and gets its own row.** Do this BEFORE any function is redeployed, because a deploy destroys the evidence | ⏳ C — claimed 19-Sep-2026, read started. **Read in progress, 19-Sep-2026 (C).** Connector up; 11 deployed functions, versions 4–18, three upload roots as described. Method: fetch each deployed file through the Supabase MCP, write it to disk verbatim, `diff --strip-trailing-cr` against `supabase/functions/<name>/`, and where it differs, bisect the repo history for the revision it IS. **Timestamps turned out to be useless as a predictor and are recorded only as context** — this project deploys BEFORE it commits (auth-bootstrap is deployed 06-Sep 03:03 UTC and its commit `78a4444` is dated 05:34 UTC the same day, yet the deployed bytes contain that commit's text), so nothing but the bytes settles it. **5 of 11 done. 3 identical, 2 stale, and one of the two is a live defect (T-130).** ☑ `unsubscribe` v4 — identical, entrypoint and all three `_shared` files it carries (`db.ts`, `from-address.ts`, `unsubscribe-token.ts`). ☑ `ses-feedback` v4 — entrypoint identical. ☑ `auth-bootstrap` v12 — entrypoint identical. ☒ **`send-followups` v18 — STALE.** Deployed entrypoint is byte-identical to this repo at `b814ccf~1`, the commit immediately before T-020's spec, and the deployed bundle carries **no `send-loop.ts` at all** (5 hunks, 41 lines out / 58 in against HEAD). **T-020's fix is not in production**: the live send still discards the `email_messages` insert error and asserts `msgRow!.id`, so the defect RV-10 describes is exactly what is running today, and every claim resting on “the function does X because the source says so” for this function is unfounded until it is redeployed. ☒ **`pin-issue` v13 — STALE**, identical to `78a4444`; missing the whole `delete_only` branch, which makes Remove-a-staff-member a PIN rotation. Filed as **T-130**. Note also, from the same reads: every `_shared` file in the 06-Sep and earlier bundles arrives CRLF while the 09-Sep bundles are LF — different working copies, which is the row's own claim confirmed from the bytes. And `pin-reset-request` still reports `verify_jwt: true` (T-127 not yet toggled). **Still to read: `auth-login`, `auth-lookup`, `csv-import`, `pin-reset`, `pin-reset-request`, `recovery-check`.** **ENTRYPOINT SWEEP COMPLETE, 19-Sep-2026 (C): 11 of 11 diffed byte-for-byte. 9 identical, 2 stale.** Identical: `auth-login` v11, `auth-bootstrap` v12, `auth-lookup` v5, `recovery-check` v11, `pin-reset` v13, `pin-reset-request` v5, `ses-feedback` v4, `unsubscribe` v4 (plus all three `_shared` files it carries), `csv-import` v17 — the last of those is the reassuring one, since it is the newest deploy and carries RC-043's paging exactly as the repo has it. Stale: **`send-followups` v18 = `b814ccf~1`** (no `send-loop.ts` in the bundle; T-020's fix is not live) → **T-131**, and **`pin-issue` v13 = `78a4444`** (no `delete_only` branch; Remove rotates a PIN instead of removing anybody) → **T-130**. Both stale copies are exact repo commits, not mystery code, which narrows the class: what this project has is UNDEPLOYED WORK, not untracked deployments — so far. **What is still unread, and it is not a formality:** the `_shared` modules inside the other ten bundles. They were fetched and eyeballed but only `unsubscribe`'s were diffed, and `_shared` is where a drift would be hardest to notice and widest in effect. The bytes already prove the bundles come from different working copies — every `_shared` file in the 06-Sep and earlier uploads arrives CRLF, the 09-Sep and 12-Sep ones LF — so “they are all the same file” is exactly the assumption this row exists to refuse. Also recorded from these reads: `pin-reset-request` is still deployed `verify_jwt: true`, so **T-127 has not been toggled**. Method note for whoever finishes it: deploy timestamps are useless as a predictor on this project, which deploys before it commits — `auth-bootstrap` is deployed 06-Sep 03:03 UTC and carries commit `78a4444`, dated 05:34 UTC the same day. Only the bytes settle it. Scope honesty: `_shared` modules have been diffed byte-for-byte only for `unsubscribe`; for the rest only the entrypoint is diffed so far, and the remaining `_shared` copies are named here as unread rather than assumed clean. Filed by C 18-Sep-2026, first row in C’s D-9c block. It is the reason T-127 is fixed by a dashboard toggle rather than a redeploy. |
| T-131 | **T-020's fix is merged but not deployed: the `send-followups` running in production still discards the `email_messages` insert error.** T-400's diff (19-Sep-2026) shows the deployed entrypoint — v18, uploaded 09-Sep-2026 18:57 UTC — is byte-identical to this repo at `b814ccf~1`, the commit immediately before T-020's spec, and the deployed bundle carries **no `send-loop.ts` at all**. The live code is therefore still `const { data: msgRow } = await admin.from('email_messages').insert(...)` followed by `msgRow!.id`: a refused insert sends the email anyway, then throws TypeError on the assertion, and the throw escapes the loop — so every recipient after the first failure goes unattempted and the batch is left `processing`, which is exactly RV-10. The repo is fixed and production is not, and nothing in this project measured that gap until now; T-020's merged PR is not evidence about production. It also means T-020's root-cause entry has a Proof but cannot have a Verify, and that every statement about this function's live behaviour made from the source since 18-Sep is unfounded | T-400 diff 19-Sep-2026, T-020, RV-10, T-120 | 5 (with T-400) | Deployed `send-followups` diffs clean against `supabase/functions/send-followups/`, and `send-loop.ts` is present in the deployed bundle. Then T-020's Verify can be taken for the first time | ☐ Filed by C 19-Sep-2026. Deploy is C's surface, but **T-400 forbids redeploying anything until the whole tree has been read** — a deploy destroys the evidence — and a redeploy of this function also lands every other 18-Sep change to it, so it is a change in its own right and goes through the loop rather than riding along with a read. ☑ **Deployed 22-Sep-2026 20:17 UTC** as `send-followups` v19 via the Supabase MCP, `verify_jwt` kept `true`. Bundle re-read from the platform and diffed by script against `supabase/functions/`: all nine files byte-identical (`send-followups/index.ts`, `send-loop.ts`, `email.ts`, `_shared/authz.ts`, `cors.ts`, `db.ts`, `from-address.ts`, `response.ts`, `unsubscribe-token.ts`). `send-loop.ts` is now in the deployed bundle. No new secret: v18 already read `UNSUBSCRIBE_SECRET` (it went up in the same minute as 0066). T-020's Verify can now be taken. |
| T-132 | **The repository is BEHIND production by one guard in `bulk_import_members`.** Measured 22-Sep-2026: production's body carries `if not public.is_active_app_user() then raise exception 'only a signed-in, active user can import members' …` immediately after the `v_actor is null` check; the full replay's body does not. Provenance: on production 0038 was never applied, so 0050's in-place edit found `is_super_admin()` in the body and rewrote it to `is_active_app_user()`, keeping the block; in the replay 0038 (present in the repo) had already restated the body without any such block, so 0050 skipped it ("already open"). Behaviour is the same — `current_app_user_id()` already returns null for an inactive user — but `53_harness_body_matches_production` can never go green for this function while the two disagree. Also a one-line formatting difference in the "is not a real date" `reason` | T-120 re-measure 22-Sep-2026, 0038, 0050 | 5 | An in-place edit migration adding the guard block so the replayed body equals production's `md5(prosrc)`; or a recorded decision that production's extra guard is dropped. `53` extended to `bulk_import_members` | `md5(prosrc)` equal on both sides | ☐ Filed 22-Sep-2026 while syncing production. A's surface. |
| T-133 | **Two DB specs are red on their own SQL, not on the migrations they test.** `27_set_attendance.sql:161` asserts that `set_attendance(…, 'absent')` on an off-schedule day is refused; 0035 itself creates an `all_enrolled` session for such a day, so the member IS expected and the mark is accepted — spec and migration have disagreed since they were written, in every replay. `35_attendance_backdates_membership.sql:126` errors `column reference "status" is ambiguous`: it selects unqualified `status` from `attendance_records ar join members m`, and a later migration added `members.status`. Both reproduce in the full harness replay and on a fresh production-like replay + 0035 + 0046, so neither is an apply-order artefact. Test files are append-only; the fix is a re-pointed assertion (27) and a qualified column (35), each the copy-lock-style minimal diff | harness run 22-Sep-2026, T-126 | 5 | `27` and `35` green in `db-harness`; `27`'s assertion re-stated to 0035's actual rule or 0035 changed, decided out loud | `npm run test:db` shows both files green | ☐ Filed 22-Sep-2026. A's surface. |
| T-137 | **Three DB specs still pinned behaviour the product deliberately changed.** `09_grants.sql`'s intended-privilege list was extended for 0017 and 0043 but never for `course_communication` (0021), `member_import_runs` (0028) or `pin_reset_requests` (0034), whose grants are deliberate; `12_offering_schedule.sql` asserted that staff cannot set a schedule, which the owner's 08-Sep-2026 instruction and 0038/0050 reversed; `34_reimport_feedback.sql` asserted the 4th import updates nothing, but 0046 back-dates a member's start to an 'extra' day, so the next re-import finds that member expected — and the owner decided (26-Sep-2026) that is right. | harness run 26-Sep-2026 (triage) | 5 | each file green in `db-harness` (`09` also needs T-138) | `npm run test:db` shows `12`, `34_reimport_feedback` green; `09`'s grants case green | ☑ 26-Sep-2026, branch `fix/T-137`, RC-117. `09`: the three tables added with the privileges their migrations grant — its grants case passes, and the file then stops at the next case, which is T-138 (a real defect, not this row). `12`: staff CAN set a schedule, checked in a rolled-back transaction so the versioning after it is unchanged. `34_reimport`: the 4th import's one update is the back-dated member (now present); the "nothing to update over a hand-corrected register" cases move to a 5th import of the same file, where they hold. Harness: `12` 24, `34_reimport` 43 PASS; `09` 8 PASS then T-138. |
| T-138 | **`pin_reset_requests` enables row-level security but does not FORCE it** — the only table in `public` that does not, against the rule 0015 established and `09_grants.sql` asserts ("every table in public forces RLS"). Without FORCE the table's owner is not bound by its policies. Found while re-pinning T-137. | harness run 26-Sep-2026 | 5 | `09_grants` green end to end | Harness: with the table forced by hand `09` runs 10/10 PASS; a migration `alter table public.pin_reset_requests force row level security` (additive, idempotent) — production state to be read first | ☐ Filed 26-Sep-2026. Needs a migration; SQL to the owner before any production apply. |
| T-130 | **In production, “Remove” on the staff list removes nobody — it silently rotates that person's PIN to a value nobody has ever seen.** The deployed `pin-issue` (v13, uploaded 06-Sep-2026 08:15 UTC) is byte-identical to this repo at commit `78a4444`, which predates the `delete_only` branch entirely: **production has no staff-deletion path**. `src/data/api.ts:123` still posts `{ app_user_id, delete_only: true }`; the deployed function does not know that flag, so the call falls through to the REGENERATE branch and one press of Remove sets `must_change_pin=true` and `pin_set_at=now()`, clears `failed_attempts`/`locked_until`, calls `rotatePin` — which invalidates the PIN that person is actually holding — closes every open `pin_reset_requests` row for them, audits `auth.pin_issued` with `mode: 'regenerate'` instead of `auth.staff_deleted`, and returns the freshly generated PIN in a body the client throws away (`staffDelete` types the response `{ app_user_id, deleted: true, name }`, none of which the deployed function returns). `app/staff/index.tsx:123-125` then flashes “<Name> removed · the PIN no longer works” and refetches — the row is still on the list, the account is still active and able to sign in, and the only true half of that sentence is that the old PIN stopped working. Same stale copy also still serves `'Enter her name.'`, which `547c95c` de-gendered on 09-Sep | T-400 diff 19-Sep-2026, T-120, T-123, `src/data/api.ts:123`, `app/staff/index.tsx:123` | 5 (with T-400) | Deployed `pin-issue` diffs clean against `supabase/functions/pin-issue/index.ts`. **Before any redeploy**, a production read that is not recoverable afterwards: every `audit_logs` row with action `auth.pin_issued` and `metadata->>'mode' = 'regenerate'`, paired against the staff the academy believes it removed — each match is a person who still has an account and a rotated PIN. A redeploy fixes the forward behaviour and settles nothing about what has already happened | ☐ Filed by C 19-Sep-2026 out of T-400's second diff. The fix is a redeploy and that is C's surface, but **T-400 is explicit that the whole tree is read before anything is redeployed** — a deploy destroys the evidence. Order: finish T-400's eleven diffs, take the audit read above, then redeploy. The production read is A's surface (D-9a) and the exact query is written out here for A to run. ☑ **Deployed 22-Sep-2026 20:20 UTC** as `pin-issue` v14 via the Supabase MCP, `verify_jwt` kept `true`, from the repo's `pin-issue/index.ts` plus `_shared/{authz,cors,db,response,phone,pin,identity}.ts`. Re-read from the platform: the deployed entrypoint carries the `delete_only` branch (`auth.staff_deleted`), `reactivate_only` and `create_only`; the four shared files common with `send-followups` are the same bytes verified there. Production now has a staff-deletion path. The pre-deploy audit read of `auth.pin_issued` rows with `mode: regenerate` from Remove presses was NOT taken first — anyone hit by the old behaviour still needs their PIN re-issued by hand. |
| T-404 | **`check:contrast`, `check:icons` and `check:functions` have never run in CI either — `npm run check` is an `&&` chain and `test:unit` is red.** T-402 found this at the JOB level, where every step after `npm run check` is skipped. This is the same fault one level in, inside the npm script: `lint && typecheck && test:unit && check:contrast && check:icons && check:functions` stops at `test:unit`, which fails on the 8 known RV-03 assertions, so nothing after it executes. Verified on run `35345932542`: the log contains no `OK [CONTRAST]`, no `OK [ICONS]` and no `OK [FUNCTION JWT]` line at all. **The gate step is named “Lint, types, contrast (2,800 pairs) and icons (71 glyphs)” and the contrast and icons halves of that name do not run.** Guardrail 2 of `CLAUDE.md` says colour ships measured, never trusted, and names `scripts/check-contrast.ts` as where that is honoured — it is honoured on a developer machine only, and has been unenforced in CI for as long as `npm run check` has been red. T-038’s `check:functions` was born stranded for the same reason | verified on run `35345932542` 18-Sep-2026, T-402, RV-03 | 2 | Checks that do not depend on the unit suite do not sit behind it. Either order the independent ones first, or split `check` so one red suite cannot mask five other verdicts. Fail-first: with `test:unit` red, break a contrast pair and `npm run check` must still name the contrast failure | ☐ Filed by C 18-Sep-2026 while confirming T-027. C’s surface (`package.json`). **T-027 moved only its own `check:edge` ahead of `test:unit`**, because a check that cannot execute is not a check and that much belongs to the row that adds it; the other three are this row, not an inline fix. ☑ **PR #34, merged 19-Sep-2026 06:37 UTC, RC-105.** `npm run check` is now `scripts/check-all.mjs` — every step runs, failures are collected, non-zero at the end, in `db/harness/test.sh`’s shape. Final run `35426840830`: **6 of 7 PASS, `test:unit` the only failure**, gate 1668 / 1659 / 8 identical to `main`. Contrast and icons executed in CI for the first time in the pipeline’s life; **icons reports 75 glyphs, not the 71 the step name claimed.** Step renamed with no counts in it, in both CI files. Spec `scripts/check-all.test.mjs` 5/5, wired into `guard:test` so it cannot itself be hidden behind a red step. **It earned itself on its first real run:** the aggregated summary showed a SECOND failure an `&&` chain would have hidden — `check:functions` blocking on `node_modules`, because T-027’s `nodeModulesDir: "auto"` materialises that directory inside `supabase/functions/` and T-038’s gate treated every directory there as a function. Invisible locally (no Deno, so no `node_modules`) and invisible in CI until this row made `check:functions` reachable: three rows had to land before the defect could be seen once. Fixed in this PR as a regression between my own rows, not filed. |
| T-402 | **Every step after `npm run check` in the `gate` job has never executed.** GitHub Actions stops a job at the first failed step, `npm run check` is the first real step, and it has been red on every run since CI first executed on 11-Sep. So `All ratchets, both app/ and src/` (`npm run audit:all`) and `Commit guards are reachable` (`npm run guard:test`) have **never run in CI, once**. Verified 18-Sep-2026 on two runs a day apart — `35319876920` on `main` and `35332936064` on `fix/T-020` — both showing `X Types, contrast…` followed by `-` for every later step. The gate job is therefore one check wearing the name of four, and the two that never run are precisely the ones that exist to catch a class rather than an instance: the ratchets, and the test that proves the commit guards can still fire. It also makes T-020’s `Edge Function specs` step unreachable on arrival — the Deno tree gets a runner and the runner never starts — and it will do the same to anything any session adds below `check` until `npm run check` goes green, which T-022 and T-121 gate | measured 18-Sep-2026 from runs 35319876920 and 35332936064, RV-03, A:F-09, T-001 | 2 | Steps that do not depend on `npm run check` do not sit behind it: give each an `if: always()` (or `if: !cancelled()`), or order the independent ones first, in **both** CI files per the template header. Fail-first: with `check` red, a deliberately broken ratchet must still turn the job red **for its own reason**, named in the log | ☐ Filed by C 18-Sep-2026 from the first CI run of T-020. C’s surface (`.github/`, `ci/`). **This is why T-020 moves its own Deno step above `check`** — a proof that cannot execute is not a proof, and that much belongs to T-020. The general fix, for the ratchets and the guard test, is this row. |
| T-403 | **After a rebase, the pre-push guard judges everyone’s work, not the pushing session’s.** `pushRange()` returns `@{u}..HEAD`, and for a feature branch `@{u}` is its own stale remote tip. Rebase onto a newer `main` and that range stops meaning “what this push adds”: it becomes “everything since the remote branch was last written”, which includes every commit `main` gained in between. Measured 18-Sep-2026 pushing `fix/T-020` after a rebase: `@{u}..HEAD` spanned **32 commits** touching `app/`, `src/`, `package.json` and three registers, while `origin/main..HEAD` — the range that actually describes the push — held 4 commits and 8 files. G3 duly blocked on four new spec files (`inFlight`, `periodMetrics`, `sendBatchKey`, `sendFailsClosed`) that are **B’s, already merged to main with their own fail-first evidence**. The session pushing is made to answer for work it did not write and cannot vouch for, and the only way through is an escape token — so the guard teaches the habit of escaping it. Same root as T-401 and the same shape: a guard handed the wrong range does not fail closed, it reports on a change nobody in the room made | measured 18-Sep-2026 pushing `fix/T-020`, `.claude/hooks/pre-tool-use-guard.mjs` `pushRange()`, T-401 | 2 (with T-402) | `pushRange()` uses the merge base against the target branch — `git merge-base origin/main HEAD`..HEAD — falling back to `@{u}..HEAD` only when no such base exists. Fail-first: rebase a branch onto a `main` that carries someone else’s new spec file, and the push must be judged on the branch’s own commits alone | ☐ Filed by C 18-Sep-2026 while pushing T-020, whose commit carries the `FAILFIRST-NA` this row exists to make unnecessary. C’s surface, C’s block. |
| T-401 | **The pre-push guard judges the wrong branch under D-9b, so one session’s unjustified change blocks every other session’s push.** `.claude/hooks/pre-tool-use-guard.mjs` resolves `repoRoot` with `git rev-parse --show-toplevel` **in the hook’s own cwd**, which is the Claude Code session’s primary working directory — not the worktree the command targets. `pushRange()` then computes `@{u}..HEAD` with `cwd: repoRoot`, so since D-9b gave each session its own worktree the hook reads whichever branch the primary tree happens to hold and applies that verdict to a push from elsewhere. Measured 18-Sep-2026: a C push of a docs-only commit carrying `CASES-NA` was refused `BLOCKED [G1]`, while `PRE_PUSH_RANGE=origin/main..HEAD bash scripts/hooks/pre-commit-guard.sh` run by hand in C’s worktree over the identical range printed `[G1] escaped via CASES-NA`. The guard is right; the range it is handed is not. **Commits are unaffected** — for a commit the adapter recovers the message from the command string, so the token is found however cwd resolves; only `push` reads a log range. Worst of all it is intermittent on someone else’s activity, which trains people to read the guard as noise | measured 18-Sep-2026 during C’s T-400 push, `.claude/hooks/pre-tool-use-guard.mjs:60-120`, D-9b | 2 | `repoRoot` derives from the command’s target worktree, not the hook’s cwd — parse `git -C <path>` or a leading `cd`, or honour `GIT_DIR`. Fail-first: with the primary worktree on a branch whose range holds an unjustified code change, a docs-only `CASES-NA` push from another worktree must succeed | ☐ ⏳ C (18-Sep-2026), branch `fix/T-401` pushed, PR pending tooling. Assigned to C by ruling. **Fixed in two parts.** (1) **D-13** records the interim rule: the primary worktree stays on `main` and is fast-forwarded before any push. (2) The adapter now derives where the command will actually run — `git -C <path>` wins, else the last `cd` before the git call, falling back to `process.cwd()` when the path does not exist so an unparseable command cannot wedge the hook. **Second defect found while fixing the first:** `isCommit`/`isPush` allowed global options but not the values they consume, so `git -C <path> push` matched neither and **passed the hook untouched** — a two-token bypass. Detection now walks tokens per shell segment, skipping options with their values. **Spec: `scripts/hooks/adapter-worktree.test.mjs`**, which builds a bare origin, a primary clone and a second worktree and drives the adapter as Claude Code does. Fails 3/3 on the parent commit, passes 3/3 with the fix; `guard-reachability.test.sh` unchanged at 28/28; `npm run guard:test` runs both and CI already invokes it. The load-bearing case is the second: an unjustified change in the TARGET worktree must still be refused when the primary is clean, or a fix that always allows would go green. RC-103, severity S1 — for five days after D-9b every guard was inert for any session whose worktree differed from the primary, which is the arrangement D-9b mandates. It blocks C’s register pushes now. **Unblocked 18-Sep-2026, and the workaround is worth knowing.** The exact trigger was the primary worktree sitting on `regs-a` **five commits BEHIND `origin/main`** with 0 commits of its own. `origin/main..HEAD` then has no commits to carry an escape token but a non-empty reversed diff, so every guard sees a change and none can find a justification — the block is unescapable by anything the pushing session writes. Fix in the moment: fast-forward the primary worktree’s branch (`git merge --ff-only origin/main`) once it is clean and strictly an ancestor, which loses nothing and is reversible. That cleared it immediately. **This makes the row worse, not better:** a stale checkout in an unrelated worktree silently disables the entire guard suite for every session, and a passing push proves nothing about the guards while it persists. ☑ **PR #29, merged 18-Sep-2026 10:50 UTC, RC-103.** Final run `35335937465`: `Commit guards are reachable` executed for the first time ever and passed — `guard-reachability` 28/28 and the new adapter spec 3/3 — with the gate at 8 failures matching `main`. The step had to be moved above `npm run check` to run at all (T-402). Merged under D-12a. **Sibling still open: T-403**, the same wrong-range fault on a rebased branch’s `@{u}..HEAD`. |
| T-119 | **The per-message send ledger is unprotected and unaudited, and 22 of its 23 rows are gone.** `email_messages` carries **no `created_at`** column and **zero user triggers** (`pg_trigger` where `not tgisinternal` = 0), so a delete leaves no record anywhere: `audit_logs` has never held an `email_message.*` action (the email-ish actions are `communication.batch_sent`, `communication.unsubscribed`, `email_template.update`, `member_email.hard_deleted`, `member_email.insert`, `member_email.update` — all about the address book or the batch, never the message). Measured 17-Sep-2026 21:10 UTC: `n_tup_ins` 23, `n_tup_del` 32, `n_live_tup` 1, stats unreset since 25-Aug, against 17 batches whose counters claim 13 sent + 5 failed + 5 excluded = 23. Every row was written and all but one removed. Consequence: `email_batches.sent_count` / `failed_count` — loop counters, per RV-11 — are now the **only** surviving evidence that this academy ever sent mail, which is exactly the single-source-of-truth failure T-049 is meant to end, and it makes T-002 and T-003 unable to answer their own questions from data. Not a `send-followups` defect: the inserts at `index.ts:270,:282` succeeded. Deletion route not established — no audit trail exists to establish it, and dashboard table-editor deletes bypass triggers in any case | T-002 and T-003 reads 17-Sep-2026, RV-11, B:F-14, T-065 | 5 (with T-065) | Trigger spec: a delete on `email_messages` writes an `audit_logs` row naming actor, row id and batch; and a `created_at default now()` so an unsent row can be aged. Then re-read `n_tup_del` against audited deletes | ☐ Filed by C 18-Sep-2026 from the T-002/T-003 Gate 0 reads. Fix is SQL — **A's surface**; C owns neither the migration nor the trigger. |

## Gate 6 — Remaining P2, scale programme, debt

| ID | Defect | Sources | WO item | Proof | Status |
|---|---|---|---|---|---|
| T-068 | Matcher O(rows×members): linear alias/canonical scans + Dice per member, allocation per call | RV-26, A:F-07, B:F-12, C:RF-02 | 6 (measure first — T-090) | Benchmark 2,000×2,000 under 2s CPU | ☐ |
| T-069 | `pickCsvFiles` no `maxBytes`; sync main-thread parse; UTF-8 assumed | C:RF-21 | 6 | 10,000-row fixture in time budget | ☐ |
| T-070 | Per-row `exception` subtransactions in `bulk_import_members`/`bulk_set_member_dates` (64-subxid overflow) | C:RF-17 | 6 (set-based rewrite) | Concurrent-read latency during a 500-row import | ☐ |
| T-071 | Status import: client 5,000 vs server 500 cap | C:RF-12 | 6 (**after** T-070) | Harness spec at 501 rows | ☐ |
| T-072 | `generate_sessions` has no caller; "Awaiting upload" dead; `.limit(20)` | B:F-08, C:RF-24, B:F-21 | 6 | Tuesday offering, no import → Tuesday awaiting | ☐ |
| T-073 | Two definitions of "enrolled"; `expected_members_for_session` ignores `status`, `deleted_at` | A:F-18, A:V-8 | 6 | Harness: ended/soft-deleted member not expected | ☐ |
| T-074 | Missing index `member_schedules (effective_from, effective_to)` on import hot path | C:§6 | 6 | `explain analyze` before/after | ☐ |
| T-075 | DB `current_date` is UTC (20 migrations); `app_settings.timezone`, `week_start_day` stored and read by nothing; client `schedule.ts:62 today()` UTC at 3 sites | A:F-16, A:F-17, RV-29 | 6 | Clock pinned 02:00 IST: attendance markable, `joined_on` today, schedules today | ☐ |
| T-076 | Member sharing a staff name set aside every week, never marked present | A:F-21, B:F-20 | 6 | Fixture: member + staff same name → member present | ☐ |
| T-077 | `dedupeRows` vs `normalizeName` disagree on "same name" | A:F-22, B:F-18 | 6 | Both pinned to one fixture table via shared module | ☐ |
| T-078 | Row numbers index filtered attendees, not file lines | RV-23, B:F-17 | 6 | Fixture with preamble + dropped rows → real line named | ☐ |
| T-079 | Blank rows dropped client-side uncounted; `dropped_count` merges blanks+dupes; `duplicates_in_file` counts N blanks as N−1 | RV-24, B:F-19, B:F-16 | 6 | Fixture: 2 blank rows → named by line | ☐ |
| T-080 | Quoted field with embedded newline mis-parses | RV-25, A:F-35 | 6 | Parse across lines or refuse unterminated quote | ☐ |
| T-081 | `meeting_started_at` raw Meet string cast to `timestamptz`; long form fails; stored −5h30 | A:F-23, B:F-23 | 6 (after T-091) | Both shapes parse; stored value correct | ☐ |
| T-082 | `autoDecisions` files every non-exact row as `add_as_new` silently; display-name drift splits one person across two members | B:F-11 | 6 | Same person, two spellings, two files → one member | ☐ |
| T-083 | Multi-file batch progress held only in component state | B:F-13 | 6 | Abort after file 4 of 9 → remaining 5 discoverable | ☐ |
| T-084 | 12s `LOAD_TIMEOUT_MS` vs paged member fan-out; no `AbortController` → retries multiply in-flight work; paged read restarts from page 1 | A:F-33, C:RF-14, C:§7 | 6 | Spec: timeout aborts work | ☐ |
| T-085 | No list virtualized; `readBounded` unused | C:RF-10 | 6 | Render-count assertion at 5,000 rows | ☐ |
| T-086 | Two operators, same file → raw `23505` string reaches screen | B:C-1 | 6 | Two concurrent commits → `already_imported` sentence | ☐ |
| T-087 | Two files, one day → last writer wins, first operator's result screen false | B:C-2 | 6 | `supersedes` re-checked in transaction, returned | ☐ |
| T-088 | No optimistic concurrency on `update_member` / `set_member_status` / `set_member_active_from` | B:C-4, C:§11 | 6 | `updated_at` precondition refuses stale save | ☐ |
| T-089 | `bulkDeleteMembers` N sequential RPCs, no resume | C:RF-25 | 6 | `delete_members(uuid[])` returns per-member verdicts | ☐ |
| T-090 | `audit_logs` unbounded, on every member write path; no retention/partitioning | C:RF-18 | 6 — **owner decision on retention** | Partition plan before 10M rows | ☐ |
| T-091 | Register drift: `KL-005` describes replaced pager; no `RC-046` for 0071; `useScreenState` dead | RV-33, A:F-31 | 6 | Docs updated | ☐ |
| T-092 | FR fallout: `addMemberAlias` `:3263` no longer refuses another member's name; `aliasClaimedMessage` misreads; `supabase/apply_all.sql` stale | FR §11 | 6 | Wording spec; snapshot retired | ☐ |
| T-116 | **Gendered copy against the standing gender-neutral rule, on both send paths.** One RENDERED string: `app/send/index.tsx:583` — "No email address — she stays counted in every figure" — which is member-facing copy on screen. The rest are the comments the rule covers equally: `app/send/index.tsx:47,49,51,67,71,78,81,88,166,167,231,257`; `app/member/[id].tsx:37,41,48,49,55,57,58,97,105,111,113,137,190,203,205,228,229,251,252,502,546,552,553,555,556,600,657`; `src/data/sent.ts:37,44,89`. The academy is a women's academy; the software is not, and a licensee's copy must read correctly. Restructure rather than swapping the pronoun (`CLAUDE.md` standing rules) | `CLAUDE.md` standing rules; found while reading the T-017 call sites | 6 | Copy-lock on the rendered string; a spec that finds no `y(she\|her\|hers\|herself)y` in the two send screens and `src/data/sent.ts` | ☐ Not fixed inline by T-017 — a pronoun sweep inside an idempotency-key PR is exactly the widening the loop forbids |
| T-123 | Gendered copy in Edge Function header comments, against the standing gender-neutral rule. `pin-reset-request/index.ts:3` — “Public (verify_jwt=false), and it has to be: she is here BECAUSE she cannot…”; `recovery-check/index.ts:4` — “(verify_jwt=false): she is not signed in yet, that is why she is here.” Both are comments, not rendered strings, and CLAUDE.md is explicit that the rule covers the comments around member-facing copy equally. Both describe a STAFF member rather than an academy member, which is if anything a clearer case: the software is licensed to academies whose staff are not all women. Restructure rather than swap the pronoun — “the caller is here because they cannot sign in”. Sibling of T-116, which covers the `app/` send paths; this is the `supabase/functions/` surface. **Third site found 18-Sep-2026: `_shared/match.ts:37`** — “one enrolment per member (0006) -- set_attendance says so out loud, ‘her…’”. Same rule, and it carries a second problem: it quotes `set_attendance` as the authority for the wording, and T-126 establishes that `set_attendance` **does not exist in production at all**. Requote 0006 directly, or drop the citation — do not repeat 0035 or `set_attendance` as precedent anywhere until T-126 lands **Fourth site, and a measured sweep, 19-Sep-2026: `send-followups/index.ts:257`** — “{{follow_up_trigger}} -- the rule that listed her, from the SNAPSHOT taken above” (the comment block runs :257–:259). Found while writing T-049's spec against that file. The same read counted the whole tree rather than adding one more site to a list: `grep -rnE "\b(she\|her\|hers\|herself)\b" --include=*.ts supabase/functions/` returns **57 lines across 10 of the 13 function directories** — `csv-import` 19, `pin-issue` 10, `pin-reset-request` 9, `_shared/match.ts` **6** (not the single line recorded above), `pin-reset` 4, `recovery-check` 4, `auth-lookup` 2, `auth-bootstrap` 1, `auth-login` 1, `send-followups` 1. **56 of the 57 are comments. The 57th is not a comment at all** — `csv-import/index.ts:399` renders “Same name, but she is enrolled in …” to the operator — which is a different and worse defect than this row covers and is filed separately as **T-129**. So this row's Proof (“fail-first at 2”) undercounts by more than an order of magnitude: the grep rung has to be fail-first at **56** over comment lines, and the three sites named above are a sample of the class, not the class. | T-038 header review 18-Sep-2026, CLAUDE.md standing rules, T-116 | 6 (with T-116) | Copy-lock or a grep rung over `supabase/functions/**` for `\y(she\|her\|herself)\y` in a comment, fail-first at 2 | ☐ Filed by C 18-Sep-2026 out of T-038. C’s surface; held, not fixed inline. |
| T-129 | **Gendered copy in a RENDERED STRING, not a comment.** `csv-import/index.ts:399` builds the hint shown beside a candidate in the import match step as `` `Same name, but she is enrolled in ${course} — not this one` ``, so the operator reads the pronoun on screen — where T-123's sites are all comments. The standing rule covers member-facing copy first and the comments around it second, and every refusal string of this shape in the database layer was already de-gendered by 0060 and 0061 (“refusals stop saying she”, “the last gendered refusals”); the Edge Function tier was never swept, so this is the same defect one layer out. Restructure rather than swap the pronoun: “Same name, but enrolled in `<course>` — not this one” drops it entirely, and reads correctly for any academy that licenses this software. The surrounding comment (`:394-:396`, “this is probably her”, “so she says so”) is a T-123 site and goes with it | T-123 sweep 19-Sep-2026, CLAUDE.md standing rules, migrations 0060/0061, T-116 | 6 (with T-116 and T-123) | Copy-lock on the rendered hint, re-pinned to the pronoun-free string — this is exactly the copy-lock exemption CLAUDE.md names, so the diff must show the string literal changing and nothing else — plus T-123's grep rung widened from comments to **all** lines, so a rendered pronoun cannot pass where a comment would fail | ☐ Filed by C 19-Sep-2026 out of T-123's sweep. C's surface (`supabase/functions/`); held, not fixed inline — one row per PR, and T-123 is the row that owns the rung. |
| T-093 | Scale programme not run (import 500/1,000/2,000/5,000; send 50/200/500; chaos 14–19) | A:§12, B:T-04/T-05, C:§23 | 6 | Phases 1–3 green at 2,000; chaos items defined+recoverable | ☐ |

## Gate 7 — P3 / polish (through a working pipeline, whenever there is slack)

| ID | Defect | Sources | Status |
|---|---|---|---|
| T-094 | `fetchAudit` orders by non-unique `occurred_at`, no `hasMore`; `readBounded` unused here | A:F-30 | ☐ |
| T-095 | `is_active` re-checked only on `restoreSession`; open tab keeps direct PostgREST reads | A:F-32 (state, don't fix) | ☐ |
| T-096 | `parseMinutes("1:30")` → 1 | A:F-34 | ☐ |
| T-097 | `generatePin` modulo bias (`buf[0] % 10000`) | A:F-36 | ☐ |
| T-098 | Non-constant-time HMAC compare `pin.ts:93` | A:F-37 | ☐ |
| T-099 | `manifest.webmanifest` cached with no invalidation | A:F-38 | ☐ |
| T-100 | Three theme/background colours across `app.json` and manifest | A:F-39 | ☐ |
| T-101 | `course_week_day_status` `marks` CTE counts cancelled/holiday sessions | A:F-41 | ☐ |
| T-102 | `fetchSenders` hardcoded; per-course sender never reaches SES | A:K-03 (TD-016) | ☐ |
| T-103 | `readMeta` `pick()` splits every line three times | B:F-22 | ☐ |
| T-104 | One address, two members, two opt-outs — documented per-course behaviour | B:F-25 — **owner decision** | ☐ |
| T-105 | Import-added addresses never validated; `unknown` status sent to and bounces | B:F-29 | ☐ |
| T-106 | Declined/failed previews accumulate as inert `csv_imports` rows | B:F-30 | ☐ |
| T-107 | Member deleted mid-send is still mailed; `member_stats` updated for deleted row | B:C-7 | ☐ |
| T-108 | CORS `*` on every function while `auth-lookup` is a public oracle | B:S-3 (documented) | ☐ |

## Verify before acting (Tier C — a read or benchmark promotes each to a row above)

| ID | Claim | Sources | What settles it | Status |
|---|---|---|---|---|
| V-01 | Matcher exceeds 2s Edge CPU at 2,000×2,000 | C:RF-02, RV-26 | Node benchmark of `_shared/match.ts` | ☐ |
| V-02 | 456-recipient send exceeds wall clock today | C:RF-11 model | Time at 50/200/500 with dev provider | ☑ **Measured 19-Sep-2026, Deno 2.9.7, dev provider, against the real `runSendLoop` plus index.ts’s per-recipient metrics RPC.** **The send path makes exactly 4.0 network round trips per recipient**, at every size, plus one batch finalise: `member_period_metrics` rpc ×n, `email_messages` insert ×n, `email_messages` update ×n, `member_stats` update ×n, `email_batches` update ×1. 50 → 201 calls, 200 → 801, 500 → 2,001. Perfectly linear, and a property of the code rather than of any machine. **Loop CPU floor, I/O at zero: 0.003 ms per recipient** (50 → 0.2 ms, 200 → 0.6 ms, 500 → 1.5 ms) — the JavaScript costs nothing; the wall clock is round trips × latency and nothing else. **Measured with 20 ms injected per call, the loop really waiting: 0.125 s per recipient** — 50 → 6.3 s, 200 → 25.0 s, 500 → 62.4 s. **So the answer to this row is a threshold, not a yes or no.** With 4n+1 calls, the 150 s free wall clock is crossed at **82 ms per call for 456 recipients** and **75 ms for 500**; the 400 s paid ceiling at 219 ms and 200 ms. Projection at three latencies (500 recipients): 40.0 s @20 ms, 100.1 s @50 ms, **200.1 s @100 ms — over the free limit**. **What is NOT measured, and why:** real per-call latency. The Supabase connector is down, and a 500-recipient run against production would write 500 rows. The nearest real figures are whole-request, from `email_batches` read 18-Sep-2026: the only 2-recipient batch took 1.29 s end to end and 1-recipient batches 0.29–0.72 s. Those bound the whole request, not one call, so the per-call number remains open — **re-run section C against the local harness, or read one real batch’s gateway timings, and this row closes with a single number.** Harness needs `psql` (T-012). **The fix is not tuning:** at 4 round trips per recipient the ceiling is structural, which is what T-049’s claim-and-drain and T-050’s hoisted metrics read exist to remove. |
| V-03 | Subxid overflow measurably degrades concurrent reads | C:RF-17 | `pg_stat_slru` during 500-row import | ☐ |
| V-04 | Alias name density forces row-by-row at 5,000 | C:RF-19 | Likely moot after 0073 (per-member uniqueness) — re-evaluate | ☐ |
| V-05 | `csv_imports.summary` reaches MBs | C:RF-22 | `pg_column_size` on a real preview | ☐ |
| V-06 | `meeting_started_at` long form fails / stored wrong | A:F-23, B:F-23 | `select distinct meeting_started_at … limit 20` vs source files | ☐ |
| V-07 | SNS fan-out saturates pool at 10,000 | C:RF-26 | Pool peak during large send | ☐ |
| V-08 | Edge body limit vs 10,000-row preview | C:§3 `[U]` | Probe at 1 MB / 5 MB | ☐ |
| V-09 | Reset-vs-upload race (`reset_day_attendance` vs concurrent commit) | B:C-3 | Targeted harness test | ☐ |
| V-10 | `expected_members_for_session` returns duplicate ids for any live session | A:V-8 | Run over recent sessions | ☐ |

## Verified sound — protect from regression, do not touch

`pageAllByKey` (RV-34, C:§19), `inChunks` / `MAX_IDS_PER_REQUEST=150` (RV-35), `useAsync` cancellation (RV-36), re-import of same file (RV-37), `refuse_course_duplicate` advisory lock — **the pattern to generalise** (RV-38), `set_attendance` scoped recompute (0035), `course_week_day_status` server-side aggregation (0067) — **the model for every heavy read**, `ses-feedback` idempotency and suppression logic, secrets posture, service worker, `app_users` column guard, attendance override semantics (0037/0042/0044/0045), change-notification wiring, Vercel static hosting.

---

## Accounting

**116 tracked rows** (T-001–T-117, with T-110 reserved and not yet filed — it is the triage of the 17 spec files still red in `db-harness`, which T-012 owns and which waits on T-013) **+ 10 verification items**, after de-duplication across RV (41), A (46), B (48), C (25) and FR, plus nine found while working the rows (T-109, T-111–T-117). Closed so far: 5 ticked (T-001, T-007, T-008, T-011, T-111), 1 not a defect (T-004). The raw sources overlap heavily — the same four P0s appear in all three audits under different IDs. The consolidated register's 75 (or 77) will map onto a subset of these rows; when it surfaces, add its ID to the *Sources* column of the matching row and open a row only for anything genuinely not here.
