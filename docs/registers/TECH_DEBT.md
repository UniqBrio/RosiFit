# Technical Debt

> Debt nobody wrote down is not debt. It is a surprise with a delay fuse.
>
> Writing it down also makes the cost **arguable**, which is the only way it ever gets
> prioritised over the next feature.

| ID | What | Why accepted | What it costs | Paid down when | Added |
|---|---|---|---|---|---|
| TD-001 | **G1 Theme artifacts in sync** cannot run — no `design/tokens.json`. Reports FAIL. | Colour stays in `src/theme/tokens.ts` (ADR 001). Migrating shipped theme code to turn a gate green weakens what the gate proves. | No mechanical proof that generated theme output was not hand-edited. Low here: there is no generated theme output to edit. | RosiFit adopts `design/tokens.json`, or the framework supports a TypeScript token source. | 02-Sep-2026 |
| TD-002 | **G2 framework contrast gate** cannot run — no `design/tokens.json`. Reports FAIL. | **Substitute rung: `scripts/check-contrast.ts`** — 2,800 pairs, all 360 custom hues, both themes, fails the build. Strictly stronger than the step it replaces. | None in substance. The cost is that `npm run gate` shows a red step that is actually covered elsewhere, which trains readers to discount red. | The framework's contrast gate accepts a non-JSON token source. | 02-Sep-2026 |
| TD-003 | **G3 Theme assets per theme** cannot run — no declared asset set in `design/tokens.json`. Reports FAIL. | RosiFit declares no per-theme brand assets, so there is nothing for the step to check. | Nothing today. If a per-theme logo is ever added, its absence in one theme would ship unnoticed. | The first per-theme brand asset is introduced — at which point this becomes a real gap, not a formality. | 02-Sep-2026 |
| TD-004 | **G6 Lint** is BLOCKED — this project has no ESLint. | Never configured; the app relies on `tsc --noEmit` (G5, passing) for correctness rules. | The correctness rules a linter decides — unused bindings, exhaustive-deps, unreachable code — are unchecked. This is the largest genuine gap of the eight. | ESLint is added with a flat config and a `lint` script, then ratcheted rather than switched on clean. | 02-Sep-2026 |
| TD-005 | **PARTLY PAID, 02-Sep-2026.** *Was:* no `test:unit` script, so **G7 Unit + pure specs** reported FAIL with an empty log. | The runner is now `node:test` under `tsx` — already a devDependency, so nothing was added to the tree to get a suite. `npm run test:unit` runs it and `npm run check` includes it; **G7 moves FAIL → PASS**. | Still open: only `mock.ts`'s attendance fixture and the range helpers in `period.ts` are covered (`src/data/attendance.test.ts`, 10 cases). `followup.ts`, `csv.ts` and `iconAlias.ts` remain pure, trivially testable and unproven in JS. | Those three get specs. The runner question is now settled, which was the part that blocked everything. | 02-Sep-2026 |
| TD-006 | **G8 Functional / integration** — no `test:functional` script exists. Reports FAIL with an **empty log**. | No browser runner is configured. `.harness/*.mjs` route checks exist but are hand-run and not wired to the gate. | Route, render and both-theme checks are hand-run, so they are run when someone remembers. | `.harness/` is wired to a `test:functional` script, or Playwright is adopted. | 02-Sep-2026 |
| TD-007 | **G10 Backward compatibility reports PASS while inert.** It prints `this gate is INERT` and exits 0; the runner reads 0 as PASS. | Framework defect F-3 (ADR 003). Not fixable here without editing Half A. | **Worse than a red step: a false green.** Anyone reading the gate report sees `G10 - PASS` and concludes compatibility was checked. Nothing was compared. | The framework separates "skipped" from "passed" — `RATCHET_SKIP` must not be `0` under the runner. `/promote` candidate. | 02-Sep-2026 |
| TD-008 | **`npm run gate` covers `src/` only.** `gate-runner.mjs` passes no `--dir`, so G4/G9/G11 never see `app/` — where all 30 screens live. | `gate-runner.mjs` is Half A and is not edited locally (ADR 004). | A gate PASS reads as whole-repo when it covers roughly a third of the code. Mitigated by `npm run audit:all`, which covers both trees and is what CI runs. | `gate-runner.mjs` accepts a repeatable `--dir` or configurable roots. `/promote` candidate. | 02-Sep-2026 |
| TD-009 | **RC-003 claims `src/components/Dialog.tsx`, which does not exist here.** One baselined dead rung. | Q3: at n=1 the carrying cost is trivial, where CP-2…CP-21's twenty was not (ADR 002). The row is inherited framework root-cause history, and the registers are append-only. | One permanent entry in `.baselines/rule-coverage-baseline.txt` that can never be paid down, because the file it names will never exist here. | RC-003 is superseded by a RosiFit root-cause entry that names a real rung, or the row is retired with a declared reason. | 02-Sep-2026 |
| TD-010 | **PAID DOWN, 02-Sep-2026.** *Was:* the only automated target could not be run — neither `psql` nor Docker was on PATH on the adopting machine, so `bash db/harness/test.sh` aborted in `reset.sh`. | The diagnosis was right about that machine and wrong about the repository. Debian and Ubuntu link only the PostgreSQL **client** tools into `/usr/bin`; `initdb`, `postgres` and `pg_ctl` live under `/usr/lib/postgresql/<major>/bin`, so a complete Postgres 16 reads as "not installed" to `command -v postgres`. | Nothing now. While it stood, the 135 SQL assertions could not be executed at all, and every `ENVIRONMENTS.md` rule of the form "applied to the harness first and confirmed" was unenforceable — a governance gap, not just a tooling one. | **Paid:** `db/harness/start.sh` finds the server binaries by searching the platform bin directories rather than trusting `PATH`; `reset.sh`/`test.sh` take `PGHOST`/`PGPORT`/`PGUSER`; and the `db-harness` job in `.github/workflows/ci.yml` runs all 135 against `services: postgres:16`. CI is the durable half — it depends on no one machine. ADR 013. | 02-Sep-2026 |
| TD-011 | **`gate.md`'s contract and `gate-runner.mjs`'s behaviour disagree on the exit code for an unverifiable class.** The documented contract defines exit `3` / BLOCKED as *"a class could not be verified — an owner decision, never a pass"*. Here five such classes (G1–G3, G7, G8) exit `2` / FAIL, and the runner can only reach `3` if a step itself exits `3`. | Framework defect **F-5**. `gate-runner.mjs` is Half A and is not edited locally (ADR 004). A RosiFit addendum in `.claude/commands/gate.md` records the real behaviour, so the two no longer disagree *silently*. | The three-valued verdict is the framework's central honesty mechanism, and its most important distinction is unreachable in practice: **an owner is never actually asked to accept a BLOCKED class**, because the gate calls it a failure instead. The addendum documents this but cannot restore the decision point. | The runner classifies a missing input as BLOCKED (F-1), a missing script as BLOCKED (F-2), and an inert step as BLOCKED rather than PASS (F-3) — at which point exit `3` becomes reachable and the contract is true again. `/promote` candidate. | 02-Sep-2026 |
| TD-012 | **PARTLY PAID, 04-Sep-2026 (second time).** *Was:* five forms flashing a confirmation for a write that never happened, then three. **`app/member/edit.tsx`'s EDIT half is now paid** — 0016 shipped the CREATE path only and the form said so in its own footer; `0027` adds `update_member` and the form saves, refuses and revalidates like the add path. **PARTLY PAID, 02-Sep-2026.** *Was:* five forms flashing a confirmation for a write that never happened. `app/holiday.tsx` (this session) and `app/member/edit.tsx` (a parallel session, migration 0016) now write, await, and render refusals. | Each remaining one still needs its own write path, RLS-refusal message and list revalidation, and none has been asked for yet. | **Still open on three:** `app/course/rules.tsx`, `app/staff/add.tsx`, `app/templates.tsx`. Each is a user believing a record exists that does not, and each will be found the way RC-008 was — from a user report, one at a time. | Each gets a repository write, an awaited save, a rendered refusal and an `onChanged` revalidation, following `app/course/edit.tsx` and `app/holiday.tsx`. | 02-Sep-2026 |
| TD-013 | **PAID DOWN, 04-Sep-2026.** *Was:* migrations 0016 and 0017 were written, committed and applied nowhere, so Add Member and Delete Holiday were refused by PostgREST. | The live project was never an automated target without explicit instruction, and the harness could not run (TD-010) — so neither could be applied or proven. Both conditions have since gone: TD-010 was paid down, and the owner gave explicit instruction. | Nothing now. While it stood, two shipped forms failed against production with a "function not found in the schema cache" error, which reads like an application bug rather than a missing migration. | **Paid:** 0016, 0017 and 0018 were found ALREADY APPLIED on the live project (their objects verified individually) though absent from the migration ledger; 0019–0024 were applied in order on 04-Sep-2026, each verified before the next. All 24 replay clean on the harness. | 02-Sep-2026 |
| TD-014 | **PARTLY PAID, 04-Sep-2026.** *Was:* `app/(tabs)/members.tsx` and `app/(tabs)/attendance.tsx` reachable only by typing their URL. **Paid:** the chevron on a course card now opens `members` scoped to that course. **Still open, and one worse:** `attendance` still has no caller, and `weekly` lost its only one when the course detail's Attendance action rows were removed on request. | The rows were two-thirds duplicates — Send Communication repeated this screen's own header button and Upload Attendance repeated the day strip — so removing them was right; Weekly Review rode along. Re-homing it is a placement decision, not a cleanup. | Weekly Review is unreachable in the app: the Saturday check can only be opened by URL. `attendance` is the same as before. Both still cost every sweep — contrast, icons, test ids, typecheck. | Weekly Review gets a home (the day strip, or the Attendance tab), and `attendance` either gets a route or is deleted with its entry in the route inventory. | 03-Sep-2026 |
| TD-015 | **`app/offering/edit.tsx` cannot be opened from anywhere in the app.** Nothing references the route but `_layout.tsx`'s own `Stack.Screen` declaration — not a push, not an href, not a link. Same class as TD-014 and found the same way, by walking every navigation target; it was missed there because TD-014 was written about the two hidden tabs. | Not accepted so much as **not noticed**: `2d2877a` converted it to the shared `FormDialog` shell along with three screens that *are* reachable, so it reads as live code in every diff and every audit. | A whole form — the per-branch offering's times and schedule — is unreachable except by typing the URL. It is also carried by every sweep (typecheck, contrast, icons, test ids) at full cost, and it is the screen `set_offering_schedule` exists to serve. | It gets a caller (the course detail screen owns the offering, and is the obvious home), or it is deleted with its `Stack.Screen` line and its row in the route inventory. **A placement decision, deliberately not taken here:** `520ebfd` set the More tab to "the three groups the canvas lists, and nothing else", so a new entry there would contradict the canvas. | 04-Sep-2026 |
| TD-016 | **PARTLY PAID, 07-Sep-2026, twice in one day.** *First:* the two addresses stopped being fictitious -- `SENDERS` now reads `support@getfit.rosifit.com` / `support@getfit.ravisfit.com`, both under domains VERIFIED as SES identities, so the bounce-every-message half closed. *Second, and the one that was actually costing something:* **the From Email ID picked in the course form was stored by `save_course` and read by NOTHING.** `send-followups` called SES with `FromEmailAddress: SES_FROM_ADDRESS` unconditionally, so a course deliberately set to the academy's second address sent as the first one -- and reported SENT, truthfully, because delivery succeeded. The sender was the only part that was wrong, and nothing anywhere recorded it. `chooseFromAddress` (`supabase/functions/_shared/from-address.ts`, spec `src/data/courseFromAddress.test.ts`) now decides per message: the course's own address where it has one, `SES_FROM_ADDRESS` where it has not, and a REFUSAL where the stored value is not an address -- that recipient is excluded naming the course, never quietly sent from the fallback, because succeeding from the wrong address is the defect. `0036` adds `email_messages.from_email` so which address a message used is written down rather than inferred from a secret's current value. **AND THE DATA WAS ALREADY WRONG, measured on PRODUCTION 07-Sep-2026 before any deploy.** Nine courses carry their own from-address and EIGHT held one the academy cannot send from: `support@rosifit.com` (7 courses) and `support@ravisfit.com` (1), the fixture addresses the picker offered for as long as it existed. Harmless for exactly as long as the column was ignored -- those courses silently sent from `SES_FROM_ADDRESS` and arrived. The moment the new `send-followups` deploys they stop being ignored, each is well-formed so `chooseFromAddress` accepts it, and SES refuses it: 8 of 9 configured courses would go from silently-working to every-message-failed, looking like the new code rather than eight-day-old data. `0038` repoints each onto its own brand's verified subdomain (rosifit -> getfit.rosifit, ravisfit -> getfit.ravisfit, nobody consolidated onto one address) and then REFUSES TO APPLY if any row is still outside them. It must LEAD the deploy, not follow it. **APPLIED AND DEPLOYED 07-Sep-2026, in that order, and verified live:** `0036` applied (column present, text, nullable), then `0038` applied (its guard passed, so it did not raise), then `send-followups` deployed as version 14. Production now reads 9 courses configured, 0 on an unverified domain, and 1 still on `support@getfit.ravisfit.com` -- the second address survived the repoint rather than being consolidated away, which was the point. **Still open, one thing:** the list is HARDCODED in `src/data/mock.ts`, so adding or changing an address is a code change rather than a setting, and the send screen still does not show which address a batch will go out as. *Was:* **`fetchSenders()` read `app_settings.reply_to_email`, a column no migration has ever created.** PostgREST answered 400 on every Add/Edit Course dialog open; the error was discarded by a `const { data } =` destructure, so the fallback ran silently and the from-address list was always the two FIXTURE addresses in `mock.ts`. | Invisible by construction — the only symptom was a list that looked plausible. Found by diffing every `.select()` column in the tree against `information_schema.columns` on the live project. | **The worst finding of the sweep.** `save_course` stores whatever is picked, and `support@rosifit.com` / `support@ravisfit.com` are not addresses the academy owns — which is exactly the bounce-every-message failure the function's own comment warned about. Nothing has been sent yet (0 rows in `email_messages`), so no mail has actually bounced. | A migration adds a from-address to `app_settings` and a settings field sets it, then `fetchSenders` leads the list with it. **Deliberately not repointed at another column:** `app_settings` has `sender_name` and no address at all, so there was nothing truthful to read; the dead query was removed rather than aimed somewhere else. | 04-Sep-2026 |
| TD-021 | **PAID DOWN, 06-Sep-2026** — `requests/2026-09-06-calendar-style-all-date-fields.md`, whose "form visibility" half is exactly this row. `FormDialog` now takes its cap from `dialogMaxHeight()` (`src/components/dialogSize.ts`, spec `dialogSize.test.ts`): a viewport of 0 is an UNKNOWN viewport, not a short one, so no cap is applied until there is a real height to take 90% of. That is the fallback this row proposed, and it closes the cold load and the blank-behind-a-picker together. **Verified on the built export at 1280x900**, both themes: `/holiday` with the start-date picker open measures card 811px -> 811px where this row measured 811 -> 2; cold direct loads of `/member/edit` and `/holiday` render the form (985px, 1334px) where they measured 560x2 blank. **Residual, stated rather than hidden:** on a cold load the viewport is never re-read, so the card takes its natural height and can exceed the window -- `/holiday`'s Apply sits at 1022px on a 900px viewport. The page scrolls and it comes into view (measured), so nothing is unreachable; it is simply taller than it would be had the viewport been known. *Was:* **RENUMBERED FROM TD-017, 05-Sep-2026** — `eb5315b` (11:33) had already taken that id for the `auth-lookup` oracle an hour before `c5620a0` (12:34) reused it; two parallel sessions, one worktree. The later row moves, and it is also the one nothing references. **A form dialog opened COLD collapses to a 2px sliver and shows nothing.** Load `/member/edit` (or any dialog route) directly by URL — a refresh, a bookmark, a deep link — in the exported web build and the card measures `560×2` with `overflow: hidden`, so the entire form is clipped away and the screen is a flat field of scrim. Reached by clicking, the same dialog renders correctly. The cause is `maxHeight: height * DIALOG_MAX_H` in `FormDialog` reading `useWindowDimensions().height` as `0` on the prerendered page and never re-reading it. **AMENDED 06-Sep-2026 — the cold load is not the only trigger.** A dialog that IS rendering correctly collapses to the same 2px sliver the moment any `Modal` mounts over it, which is every picker in every form. Measured on the built app at 1280×900, `/holiday` after a resize has made the card render: card 811px with the form on screen → open the start-date picker → card 2px and the form gone, while `#root` stays `display: flex, visible`. So the blank background behind an open picker is this row, not the picker; the bottom sheet the date fields used until 06-Sep-2026 was a `Modal` too and did the same. It also means the fallback this row proposes buys back more than a cold load: it is what makes a form visible behind its own picker (ADR 024). | **Not accepted — found on 05-Sep-2026 while verifying the backdrop change, and deliberately not fixed there.** Measured on the build BEFORE that change and the build after: identical `560,2` card both times, so it is pre-existing and unrelated. Fixing it means changing how the card is sized, which is not what that request asked for. | A user who refreshes the page with a form open, or opens a bookmarked form URL, gets a blank coloured screen with no error and no way to tell what went wrong. It is worse now only in that the blank screen is a lighter colour than it used to be. | A request of its own. The likely shape is a fallback when `useWindowDimensions()` reports `0` — the card does not need a max height until there is a viewport to be 90% of. Verify by loading each dialog route cold in the exported build at desktop width. | 05-Sep-2026 |
| TD-018 | **`FormDialog` does not trap focus; the other two dialog shells do.** With "Welcome a new member" open, ten Tab presses walk the SCREEN BEHIND — the profile button, the notification bell, Overview, Attendance, Add, the member search — and never enter the dialog. `Sheet` and `ConfirmDialog` both set `accessibilityViewIsModal`; `FormDialog` sets it on nothing. | **Not accepted — found 05-Sep-2026 by the keyboard walk in the same change.** Pre-existing: the DOM behind the dialog was always present and always tabbable; it was merely painted over, so nobody could see the focus ring leave. Making the backdrop visible did not create this, it exposed it. Fixing it is a behaviour change nobody requested. | Keyboard and screen-reader users cannot reach a form's own fields by tabbing, and land in controls that are visually behind a scrim. Every form in the app. | A request of its own. `accessibilityViewIsModal` on the card, matching what `Sheet.tsx` already does in two places — then walk each dialog route keyboard-only. **NARROWED 07-Sep-2026** by the caret change (CP-018): a dialog's first field now holds the caret on arrival, so the walk STARTS inside the dialog instead of behind it. The trap itself is untouched — tabbing past the last control still leaves for the screen behind, and Shift+Tab from the first still walks backwards out of it. | 05-Sep-2026 |
| TD-019 | **PAID DOWN, 06-Sep-2026** — RC-020, `requests/2026-09-06-member-day-chips-not-selected.md`. `Member` now carries `weekdays: number[] | null`, `fetchMembers` reads `member_schedules` through the same effective-dating resolver the offering schedules use, and both member forms open on the days in force — hers where she has them, the course's where she does not (`openingDays`, `src/data/memberDays.ts`). A failed read of that table now fails loudly rather than reading as "nobody has days of her own". Found as predicted: from a user report, this one about the colour of the chips. *Was:* **the EDIT member form cannot see her saved day override, and saving ENDS it.** `Member` (`src/data/mock.ts`) carries no weekdays, so `app/member/edit.tsx` opens Edit with the day chips blank however many days she is actually on; the save then sends `weekdays: null`, and `update_member` (0027) reads that as "back to following the course" and closes her `member_schedules` row. Correcting her email address silently discards her schedule. | **Not accepted — found 05-Sep-2026 by the B2 sweep for the add-form day seeding.** Pre-existing and untouched by that change, which deliberately seeds ADD only: seeding EDIT from the COURSE would show days that are not hers, which is worse than the blank row. | A member with an individual schedule loses it the next time anyone edits her record, and the audit entry says `own_days` changed to null — so it is recorded, just not by anyone who meant it. | `Member` carries her current override, the roster query reads `member_schedules`, and the Edit form seeds from that. A request of its own — it is a BUG, not this debt row's cleanup. | 05-Sep-2026 |
| TD-017 | **`auth-lookup` is an unrate-limited public staff-enumeration oracle.** Anyone can POST numbers to it until one answers `registered: true`, which reveals that the number belongs to the academy's super admin or one of its staff. | Accepted deliberately by the repo owner on 05-Sep-2026, shown the trade-off in full (ADR 016 / `docs/decisions/008`). The sign-in screen is specified to send an unrecognised number to registration rather than to a PIN screen it can never pass, and that decision cannot be made without answering the question out loud. Rate-limiting was offered as a middle option and not taken. | The academy's staff and admin mobile numbers are discoverable by anyone who can reach the function. It gives nothing else — no name, no role, no active flag, nothing that narrows a PIN guess, and `auth-login`'s five-attempt lockout is untouched — so the exposure is *which numbers are staff numbers*, not access. | The lookup is rate-limited per IP and answers only the routing decision. This is the option that was already costed and declined once, so paying it down is a decision to revisit, not new design. | 05-Sep-2026 |
| TD-022 | **`buildErrorReport` and `ReportLine` (`src/data/memberXlsx.ts`) have no caller.** The member import's *Download error report* button was removed on 06-Sep-2026 — the requester's *“download error report is not needed”* — and the workbook builder behind it stayed. | **Deliberate, and the smaller of two rule-breaks.** Deleting the function means deleting the spec that proves it (`the error report carries Row, Status, Reason, then every column as it was`), and CLAUDE.md's standing rule is that test files are append-only. The ask was to take the button off the screen, not to remove the capability from the codebase, so the surgical change stops at the screen. | ~30 lines of exceljs report-building carried by every typecheck and every sweep, proven by a spec, reachable by nothing. It reads as live code in any diff. Not user-visible and not a correctness risk. The dead-weight audit will not catch it: `check-dead-weight.mjs` scans `scripts/` only, and declines application source on purpose. | Either the report comes back (one button, one handler — this is why it was kept), or a request removes the capability and its spec together, deliberately and on the record. | 06-Sep-2026 |

| TD-020 | **"Expected at nothing this week" no longer has a word anywhere on the roster.** The course roster's pill used to read `expected === 0` and draw "Inactive"; on 05-Sep-2026 it was repointed at `members.status`, which is a different fact and the right one. Nothing took over the fact it stopped stating: a member on a course with no weekdays set, or enrolled at an offering that does not run this week, is now drawn "Active" with `Missed …: 0 · consecutive 0` and no line saying why the zeros are zeros. | **Accepted deliberately, 05-Sep-2026.** The request was to make the pill settable, and it could not be both a stored status and a derived schedule reading. Adding a second line to the card to carry the derived fact was not asked for, and the course card on the Attendance tab already states the more serious version of it — `courseSummary().noDays` renders "No weekdays set, so nobody is expected" per course, in words. | A member whose offering has no schedule reads as a normal member with a perfect week. The academy sees zero missed sessions and no reason to look — the course-level warning is one screen away, on the card she was tapped from. | A line under her name when `expected === 0` and she is active, saying she is expected at nothing this week and why. It is a copy question more than a code one, so it wants a request of its own. | 05-Sep-2026 |

---

## What counts

- A shortcut taken deliberately, with a known cost.
- A baselined ratchet entry — the accepted violations in `.baselines/` **are** recorded debt.
- A module everyone avoids editing.
- A test class that cannot currently be written, and why.
- A dependency that is unmaintained or pinned to an old version.

## What does not count

A bug. A missing feature. Something you dislike. Debt is a **decision** that traded future cost
for present speed — if there was no decision, it is just a defect, and it belongs in the
root-cause register or the backlog.
| TD-022 | **"Could not reach the academy database" is also what a MISSING Edge Function looks like.** `src/data/repository.ts` decides between a network fault and a real refusal by testing the error text against `/fetch|network|timeout|Failed to send/i`. supabase-js reports a **404 from `functions.invoke` as "Failed to send a request to the Edge Function"**, so an endpoint that is not deployed renders as a connection failure. On 05-Sep-2026 this sent the owner looking at their network while `auth-lookup` was simply not deployed. | **Accepted for now.** The regex is doing the job it was written for — keeping supabase-js's transport wording off the screen — and it has no way to tell "the network dropped" from "the endpoint is not there", because the string it is given is the same. Distinguishing them means reading the HTTP status, which `functions.invoke` does not hand back on the error path. | Every not-yet-deployed function reads to the user as a connection problem, and the app tells them to check something that is not wrong. It cost a real diagnosis cycle on the sign-in screen, where it is worst: the sentence is the only thing on the page. | `callFunction` catches `FunctionsHttpError` and reads `context.status`, so a 404 says the function is not deployed and a 401 says it needs a session — both actionable, neither blaming the network. A request of its own. | 05-Sep-2026 |

| TD-023 | **The whole holiday feature is now unreachable in the app.** The Holidays row in `app/(tabs)/more.tsx` was the ONLY navigation to `/holiday`; it was removed on 05-Sep-2026 at the owner's request. The screen, its repository writes, migration 0017 (`holidays_apply_effects`, its three triggers, the delete policy and the DELETE grant) and its specs all remain — only the way in is gone. | **Accepted deliberately, and it reverses an earlier decision of the owner's** recorded in that file's own comment: holidays had been KEPT there specifically because the row had become the feature's last route. Removing the row was asked for; deleting a migrated, tested feature was not, and doing that unasked would destroy schema and specs on an inference. | Same class as TD-014 and TD-015, and the largest instance of it: a complete feature with database triggers behind it, reachable only by typing the URL, carried at full cost by every sweep — typecheck, contrast, icons, test ids. A holiday applied in the past still suppresses sessions, so the ENGINE half is live while the UI half is not. | Either it gets a home (the Attendance tab's day strip is where a closure belongs), or the screen, its route, its `Stack.Screen` line and its row in the route inventory are deleted together and 0017 is left as applied history. **A decision, not a cleanup** — and the owner has now moved it once in each direction, so the next move wants to be the settled one. | 05-Sep-2026 |
| TD-024 | **`AttendanceBars` is a second copy of the bar rows in `app/(tabs)/reports.tsx`.** The Overview's member section draws stacked attended-vs-missed rows from `src/components/AttendanceBars.tsx`; Reports draws the same rows inline, from the same `reportBars()`/`reportMeta()` arithmetic. | The request that added the Overview sections names the Reports tab under MUST NOT CHANGE, and extracting the markup out of Reports would have edited a screen the request froze — with no render test there to catch a slip. Duplicating the VIEW while sharing the ARITHMETIC was the smaller risk. | Two places to fix a bar. The figures cannot drift (one `report.ts`), but the look can: a padding or radius changed on one is a visible inconsistency between two screens showing the same chart. | A dedicated pass points `app/(tabs)/reports.tsx` at `AttendanceBars` and deletes its inline copy — a pure extraction, checked by screenshotting Reports before and after. | 06-Sep-2026 |
| TD-025 | **`fetchWeekRows()` and `useWeekRows()` have no callers.** `src/data/repository.ts` and `src/data/hooks.ts` still carry the week-by-week query the 03-Sep dashboard cut; `src/data/buckets.ts` + `fetchBucketMetrics()` now do the same job, per member and filter-aware. | Already dead before this change — the last caller went with the week table. Deleting them was not traceable to the request that added the buckets, and `npm run audit:deadweight` covers `scripts/` only, so nothing flagged them. | Two exported functions and a `WEEK_ROWS` fixture that read as live code in every diff, plus a reader's question about which of the two period queries is the real one. | A cleanup pass deletes `fetchWeekRows`, `useWeekRows`, `WEEK_ROWS` and the `WeekRow` type, or `check-dead-weight` learns to audit `src/` exports. | 06-Sep-2026 |
| TD-026 | **The dropdown list rows announce no checked state.** `DropdownItem` renders `role="radio"` and `DropdownCheckItem` `role="checkbox"`, but React Native Web 0.21 drops `accessibilityState`, so a screen reader hears every row as unchecked. Verified in the built page, not inferred. | `DropdownCheckItem` is FIXED — it passes `aria-checked` directly. `DropdownItem` is not: it is the radio row used by the Attendance and Reports filters and the period presets, all of which this request froze under MUST NOT CHANGE. | Every radio dropdown in the app announces its selection to a screen reader as unselected. The visible row says "Selected" in words (guardrail 3), so this is an assistive-tech gap, not a sighted one. Also: `Space` does not activate either row type on web — only `Enter` does — because RN Web binds keys by role and these are `div`s. | A dedicated accessibility pass gives `DropdownItem` `aria-checked` and both row types a `Space` handler, then walks every dropdown in the app keyboard-only. **AMENDED 06-Sep-2026 (member-status-in-edit-form):** both halves now have a worked precedent to copy — `aria-checked` written directly, and a typed `onKeyDown` helper (`spaceSelects`, `app/member/edit.tsx`) that presses on Space. The platform facts behind this row are KL-002 and KL-003. The uncovered controls are unchanged: `DropdownItem`, and the day chips and primary-address radios on the member edit form, which that request froze. | 06-Sep-2026 |
| TD-027 | **Every route in the web export raises React #418 on load — the hydration mismatch CP-015 exists to prevent.** Measured 06-Sep-2026 with Chromium against a static `expo export`: 3/3 loads on `/member/edit`, `/member/edit?id=…`, `/course/edit?id=…` and `/(tabs)` — and 3/3 on the **pre-fix** build in `dist/` (07:28) on the identical URLs, so it is app-wide and predates this change. React discards the server tree and re-renders on the client; the app still works, which is why nobody has seen it. **Re-measured 06-Sep-2026 (member-status-in-edit-form) against a later export: NOT every route.** `/member/edit` with no `id`, `/member/1`, `/(tabs)/members` and `/offering/edit?id=…` loaded clean; `/member/edit?id=1` and `/course/edit?id=…` still raise it. The shape that raises it is a **dialog route read with a param**: the prerendered HTML is that route’s NO-param form — `dist/member/edit.html` is literally the Add form, “Welcome a new member” — so the client rendering the Edit form mismatches on the title before reaching any field. That narrows the hunt; it does not close it. See `.evidence/member-status-edit-form.txt`. | **Not accepted — found 06-Sep-2026 while walking the RC-021 fix in the running app.** Not caused by that change, and folding an app-wide hydration repair into a member-form bug fix would hide both. Nothing in the gate looks at page errors, so it went unmeasured rather than accepted. | The static export is decorative: every screen pays a full client re-render, and the class of defect CP-015 was written for is live and unmeasured. Any future first-paint work starts from a tree React is already throwing away. | A request of its own: capture the un-minified mismatch (a dev build names the text node), fix at the source, then wire a page-error assertion into `.harness/` so a #418 fails a run instead of scrolling past. | 06-Sep-2026 |
| TD-028 | **Three more forms gate a save on a length rule looser than the constraint behind it.** The sibling sweep for RC-023: `app/branches.tsx` (`name.length >= 2`) and `app/holiday.tsx` (`name.trim().length >= 2`) carry no upper bound against `between 2 and 80`; `app/member/edit.tsx` and `app/staff/add.tsx` gate on `name.trim().length > 0` against `between 2 and 120` and `between 2 and 80`. Found by listing every text-length CHECK in `supabase/migrations/` against every `const valid` in `app/`. | Outside the reported defect, which was the course form. RC-008 set this precedent in this repo: fix the reported site, record the siblings rather than let them be rediscovered one user report at a time. The member and staff RPCs raise their own worded refusals, so those two fail **readably** today — an over-long branch or holiday name is the one that would still surface as a constraint, and `personReadable()` now catches its wording even so. | A name at the wrong length is offered a Save the database refuses. Nobody has hit it: the bounds are generous and the failing window is narrow. | The next change that touches any of these four forms takes its own bound with it — the constants and the `problem()` idiom already exist in `src/data/message.ts`. | 07-Sep-2026 |
| TD-029 | **Clearing the wording boxes saves NULL and silently returns the course to its template.** `saveCourse` sends `subject.trim() \|\| null`, and NULL means "use the template's" (0021) — which is correct and is what Reset writes. But the preview above the boxes shows the *cleared* text, so a person who empties the subject sees blank and gets the template's subject on the next open. | Found while fixing RC-023 and outside it: the reported defect was a save that failed, this is a save that succeeds and does something else. Folding a second behaviour change into a bug fix would hide both. | A course's wording can change to something the person did not choose, with no message saying so. Bounded — it only reverts to that course's own template, never to another course's. | Either the form says "empty means this course uses the *<template>* wording" beside the boxes, or clearing is routed through Reset and the boxes refuse to go empty. A `/request` of its own. | 07-Sep-2026 |
| TD-030 | **The five NON-member write translators still open a refusal lowercase.** Every `raise exception` in `supabase/migrations/` opens lowercase, as Postgres messages do, and `personReadable()` passes the hand-written ones through to the screen verbatim (CP-003, RC-023). Both translators that feed the member dialog's banner — `memberWriteError()` and `setMemberStatus`'s own throw — now run them through `sentenceOpening()` (`src/data/refusalCase.ts`); `courseSaveError`, `branchSaveError`, `offeringSaveError`, the schedule throw and `holidaySaveError` do not. Two related gaps found with them: `setMemberStatus` has **no `personReadable()` guard at all**, so an engine string on that path reaches the screen and is now capitalised on the way (RC-023 closed this at every other translator and missed this one); and the bulk import stores raw `sqlerrm` as a row `reason` (0029), which `app/member/import.tsx` renders as given, so a report can show a capitalised whole-file refusal above uncapitalised per-row ones. | **Scope, deliberately.** The request named the add/edit member dialog and one sentence in it, and Track B's freeze rule holds every other shipped string. The two translators behind THAT banner were taken, because leaving one banner opening two ways is the defect the request reported. The five behind other screens were not: rewording them is a product change nobody approved. The helper is a shared, tested module rather than an inline expression precisely so the sweep is a call site each, not a second idiom. | A course, branch, offering, schedule or holiday refusal still opens lowercase mid-dialog, so the same class of sentence reads two ways depending on which screen refused. Cosmetic, and bounded: the wording itself is identical either way. The `personReadable` gap is not cosmetic — it is RC-023's own defect class, live on one path. **Also not freeze-clean:** `memberWriteError` serves `bulkImportMembers` too, so the whole-file refusal on `/member/import` changed case as well. That screen was not named in the request; the alternative was splitting one translator in two, which is worse. | One pass wraps the other five translators in `sentenceOpening()` and extends `src/data/refusalCase.test.ts`. The `personReadable` gap on `setMemberStatus` is a `/bug` of its own against RC-023's recurrence sweep, not a cleanup. | 07-Sep-2026 |
| TD-031 | **Two members who share a name AND have no email address between them are still two identical rows in the merge picker.** `app/course/[id].tsx` fills the row's second line from `primaryEmail(m) || 'No email on file'`, so a no-email pair renders the same phrase twice under the same name, with the same `— · —` meta. The only remaining discriminator on the `Member` type is `code` (`RF-000105`), which FEATURE_TRUTH records as deliberately never displayed — *"it is an internal identifier: it tells nobody which member this is, and it was the entire second line for anyone with no email address"*. | Showing the code here would reverse a recorded product decision, which needs a decision record and not a correction run. And it is **not reachable in the register today**: the live academy has exactly one same-named pair (Kavitha Ramesh × 2) and both members hold an address — checked, not assumed. | If such a pair is ever created, the picker offers two indistinguishable rows over an irreversible merge — the exact defect this change was filed to fix, for a different pair. | Either the code is blessed as a last-resort discriminator on this row only (a decision record), or the row falls back to her joining month and branch, which are already on `Member`. A `/request` of its own. | 07-Sep-2026 |
| TD-032 | **A row matched on a non-primary address prints a different address from the one typed.** `app/course/[id].tsx` puts EVERY address a member holds into the picker's `search` field but prints only `primaryEmail(m)` on the row. Type an old address from a spreadsheet, and the row that answers shows an address you did not type, with nothing saying why it matched. | Deliberate: searching only the printed address would have failed the ask ("an old address on a spreadsheet still finds her"), and printing every address would make the row grow with the member. The wider haystack is the half that matters. | A small "why is this row here" moment in a sheet that commits an irreversible merge. Bounded — the row IS the right member; only the evidence for it is off-screen. | The row prints the address that MATCHED when it is not the primary one — the matched substring is already known to `pickerMatches`, so it is a return-value change, not a new read. A `/request` of its own. | 07-Sep-2026 |
| TD-033 | **Seven migration files are not in production's migration ledger.** Read from the live project on 07-Sep-2026 while running Track A's parity check: the ledger holds 27 entries and `supabase/migrations/` holds 34. Two different situations sit under one number. **0016, 0017, 0018 and 0025 were applied outside the ledger** — `create_member` exists in production with 0016's exact signature and no ledger row names it, so the objects are real and the record of how they arrived is missing. **0031 and 0032 were never applied at all** — `set_member_status` and `merge_member_into` do not exist in production, which is why marking a member inactive and merging a member both fail there today with a function-not-found message. 0034 is unprobed. | The parity check is a Gate 4 obligation and it found this; reconciling a ledger is not the change that found it. `0035` was cleared to proceed on a narrower question — every object it depends on WAS verified present — rather than by ignoring the drift. | The ledger is supposed to be the system of record and currently is not, so the next migration that assumes an object exists because a file defines it will be wrong. Two shipped features are silently inert in production. | Two passes, in this order: replay 0031/0032/0034 against production after the owner has read the raw SQL (they are additive and each has a spec), then backfill idempotent ledger entries for 0016/0017/0018/0025 so the file list and the ledger agree. Neither is safe to automate — both are `/request` runs of their own, and the harness cannot rehearse either on this machine (no PostgreSQL 16). | 07-Sep-2026 |
| TD-034 | **Every control that reaches the 44pt touch minimum with `hitSlop` is short of it, because `hitSlop` does nothing on this platform (KL-004).** Found while building the attendance chips and measured on the built page. The drawn size IS the hit area, so: the Active/Inactive pill and the Edit button on a course roster card are 30pt tall, and `StripArrow` — whose own comment says *"hitSlop takes this to the 44pt minimum"* — is a 30pt square. `grep -rn 'hitSlop' app/ src/` finds the rest. | Scope. The request was three chips on a member card; those now carry a real 44pt target (padding, drawn box inside). Resizing every other control in the app is a change to controls nobody asked about, and several sit in rows whose height they would change. | Small targets on a phone, on controls that include a destructive-ish one (the status pill, which opens a confirm) and the week arrows. SCREEN_CHECKLIST item 11 has been read as satisfied by `hitSlop` on those screens, so the checklist has been passing on a prop that does nothing. | One accessibility pass applies KL-004's workaround to each control, screen by screen, re-measuring on the built page rather than trusting the prop — and corrects the `StripArrow` comment, which currently states a guarantee the platform does not keep. A `/request` of its own. | 07-Sep-2026 |
| TD-035 | **`0037_import_override.sql` is written, unrehearsed and unapplied**, and `csv-import` is still not deployed. So the upload's new dialog promises an override the live database performs only partly: names in the new file are corrected, and a member the previous file marked present and this one does not name keeps that present. | There is no `psql` and no PostgreSQL 16 on the adopting machine, so `bash db/harness/test.sh` cannot run — the constraint that has stood since `0032` (TD-010's second act). CLAUDE.md also forbids applying to the live project without showing the raw SQL and being given the go-ahead, which has not been asked for. | The gap is INVISIBLE: the confirmation says the register will be replaced and the result reports nothing rather than zeroes, so nobody reading the screen can tell which behaviour they got. `supabase/tests/29_import_override.sql` (28 assertions) is written and unrun -- and eleven of them exist because a review found two correctness holes that only a run could otherwise have caught, which is what an unrunnable spec costs. | The migration is rehearsed against a fresh Postgres 16 with the full suite, its raw SQL is shown to the requester, and it is applied — then `csv-import` is deployed. | 07-Sep-2026 |
| TD-036 | **A status ink on its OWN 13% tint is not among the 2,840 pairs the contrast gate measures.** `scripts/check-contrast.ts` checks each status ink against the five opaque surfaces; every status PANEL in the app draws it on `statusSurface(ink).bg`, which is that same ink at alpha 0.13 — a tint of the ink itself, which lowers the ratio. Five pairs fall below 4.5:1 when composited: light `present` on surface2 (4.25) and on bg (4.18), light `extra` on bg (4.45), dark `cancelled` on surface (4.27) and surface2 (4.44). | Found while measuring the upload's new confirmation on the live DOM (`.evidence/upload-override-confirm-browser.txt`) — the panel this change adds uses `warn`, which clears at 4.98 light / 9.16 dark, so nothing here ships a failing pair. Fixing the five means moving shipped status inks or the tint alpha, which is a change to every status panel in the app and is nobody's request. | **Guardrail 2 says colour ships measured, and this class was never measured** — the gate is green and the rendered pairs are not. The visible cost is the result panel's `Imported · <day>` heading in light theme at 4.18:1. | `check-contrast.ts` composites `statusSurface()` over each surface and adds those pairs to the sweep; the inks or the alpha are then adjusted until all pass, in a request of its own. | 07-Sep-2026 |
| TD-037 | **Two small residues of the upload change.** `meetMatchesSession` (`src/data/meetCsv.ts`) now has no caller in the app — the day the file covers is derived once by `meetCreatedDate` and compared as an ISO day by `importAsk` — so it is exercised only by its own spec. And `src/lib/supabase.ts` reads `url ?? 'http://localhost:54321'`: an EMPTY-STRING env var is not null, so it reaches `createClient` and throws `supabaseUrl is required` at import time, taking the whole app down rather than falling back to fixtures. | Both are out of this request's scope: deleting an exported, spec'd helper and changing the client's fallback are each a change of their own, and the second is a one-character fix in a file nothing in this change touches. | The helper is dead weight carried by every sweep (the dead-weight audit covers `scripts/` only, so nothing flags it). The `??` costs an unexplained white screen for anyone who blanks an env var rather than unsetting it — it cost this session an export before `EXPO_NO_DOTENV=1` was used. | `meetMatchesSession` is deleted with its spec, or given back a caller; `??` becomes `||` with a spec pinning the empty string. | 07-Sep-2026 |
| TD-033 | **The weekly screen’s “Reach out to N members” button opens on “The draft could not be loaded”.** `/send` with no `id` leaves `useCourseMessage(null)` resolving to `null`, and `app/send/index.tsx` treats `!message.data` as its error branch — so the all-courses draft can never render and can never send. Confirmed in a browser against a fixtures build, not inferred. | Found while adding the per-member Reach out, which hit the same null and resolves it **for one member only**, from her own course (guardrail 5: the wording belongs to a course). The weekly button is named in that request’s MUST NOT CHANGE, and fixing it needs a product answer this correction had no mandate for: which course’s stored wording does a send spanning several courses use? Guessing one inside a UI fix is how a member gets another course’s message. | The academy’s one bulk follow-up path is dead from the weekly screen. Bounded: the course screen’s **Send Communication** (`/send?id=<course>`) passes a course and works, and per-member Reach out now works, so no send is unreachable — only the batch is. | Decide the all-courses rule first (most likely: one draft per course, or the send splits by course and uses each one’s wording), then implement. A `/request` of its own. | 07-Sep-2026 |
| TD-038 | **`delete_course` (0020) can violate `member_enrollments`' own check on a future-dated enrolment.** It ends an enrolment with `effective_to = least(coalesce(effective_to, current_date), current_date)`, and the table checks `effective_to >= effective_from`. A member whose enrolment starts next month — a joining date read out of an imported file is not always in the past — makes that expression produce a date BEFORE `effective_from`, and the whole deletion raises. | Found while writing `delete_member`, which needed the same expression. Fixing `delete_course` is a behaviour change to a shipped, tested function nobody has reported a failure in, and it was outside the ask (`requests/2026-09-07-staff-write-access.md`). `delete_member` (0038) carries the corrected form — `greatest(effective_from, least(...))` — with a spec for exactly that case, so the fix is written down and proven next door. | A course cannot be deleted at all while anyone in it is enrolled from a future date, and the person deleting sees a raw check-constraint failure rather than a sentence. Reachable through Add Member and through bulk import. Widened by 0038: staff can now reach it too. | A migration replaces `delete_course` with the `greatest(effective_from, ...)` form, and `supabase/tests/14_delete_course.sql` gains the future-enrolment case, mirroring `30_delete_member.sql`. | 07-Sep-2026 |
| TD-039 | **`DECISION_LOG.md` has a duplicate index number and an unindexed record.** Two rows are numbered `027` (ADR files `019-` and `020-`), and `docs/decisions/021-attendance-gets-a-client-write-path-and-it-is-one-rpc.md` has no row at all. The entry added today was therefore numbered `029` rather than `028`. | Noticed while indexing ADR 029. The register is append-only and its own note says superseded records are never deleted; silently renumbering two existing rows to close a gap would rewrite somebody else's history to tidy a column, which is what that rule forbids. | The index no longer identifies a record uniquely, so "see ADR 027" is ambiguous, and one accepted decision is invisible to anyone reading the log rather than the directory. | Someone with the authority to correct the log decides deliberately: either the second `027` becomes `028` and file `021-` is indexed, or the numbers are declared non-unique and the FILE name becomes the identifier. Either way it is a decision, not a cleanup. | 07-Sep-2026 |
| TD-040 | **`set_attendance` (0035) and `setAttendance()` are defined, tested and called by nothing.** ADR-030 withdrew the only caller — the three chips on the course roster card, which the requester asked to be a status rather than a control — one day after ADR-021 argued the write path into existence. What is left: the migration `supabase/migrations/0035_set_attendance.sql`, its 24 assertions in `supabase/tests/27_set_attendance.sql`, the `authenticated`/`anon` grants recorded in `RBAC_MATRIX.md`, and `setAttendance()` in `src/data/repository.ts`. | Deleting them is a decision, not a tidy-up, and it is not the change that was asked for. 0035 is additive, rehearsed nowhere (no PostgreSQL 16 on this machine) and applied nowhere, so leaving it costs no environment anything — and ADR-021 is the expensive half of the work, worth keeping next to the object it describes. `check-dead-weight` audits `scripts/` only, so nothing flags this automatically. | A reader meets a fully-specified per-member attendance write and cannot tell from the code that the product decided against it. The next person to want attendance correction will either rebuild it or apply an unrehearsed migration for a feature nobody asked for. | Two honest endings, and the owner picks: **keep** — leave 0035 unapplied and let ADR-021 + ADR-030 be the record, which is where this stands today; or **remove** — delete `setAttendance()` from the repository, and decide separately whether an unapplied migration file is deleted or left as history. Reviving it is its own `/request`. | 07-Sep-2026 |

| TD-041 | **`save_course` and `bulk_import_members` have no guard against picking a MEETING GROUP.** 0039 makes a Meet code into a `course_offerings` row, so a course may now have several offerings at one branch. Both functions resolve "the offering for this course at this branch" with no filter — `save_course` (0038:128) by ids, `bulk_import_members` (0038:449) by course and branch NAME — and `select ... into` takes whichever row comes first. `save_course` could write the course schedule onto a group, dropping every member of the real course to zero expected sessions; a bulk upload could enrol its whole file into one meeting. | **0039 could not carry the fix without shipping a permission change.** The only versions available to copy are `0038_staff_write_access`s, which also grant STAFF the right to add, edit and delete courses and to bulk import — a different request (`requests/2026-09-07-staff-write-access.md`), still uncommitted, and not applied to production. Folding it into an attendance migration would have shipped a permission change as a side effect, against this request own DESIGN SURFACE (PERMISSIONS: no). | Narrow but real: it needs a group to EXIST (so, after the first upload carrying a meeting code) **and** somebody to save that same course or bulk-import into it. Silent when it happens — neither function reports which offering it chose. | `save_course` is already covered: `0040_save_course_schedule_only_when_days_change` carries `meet_code is null` and names 0039 as its baseline. `bulk_import_members` is covered by `0041_group_guard_on_bulk_import`, written on the 0038 baseline and applying when 0038 does. Neither is applied to production yet, and neither can be rehearsed on this machine (no PostgreSQL 16 — TD-010, TD-033). | 07-Sep-2026 |
| TD-042 | **The `expo.web` block in `app.json` is inert, and reads exactly like working configuration.** `name`, `shortName`, `themeColor`, `backgroundColor`, `display`, `startUrl` and `description` are options of the retired `@expo/webpack-config` PWA pipeline. This app sets `web.bundler: "metro"`, and the Metro static export never reads them — they emitted no manifest, no head tags and no icons for the whole life of the app. Separately, `backgroundColor: "#130D18"` matches NO value in `src/theme/tokens.ts`: the dark background is `#08040A`. | Deleting keys from `app.json` was outside the ask (`requests/2026-09-07-installable-pwa.md`), and `favicon`, `bundler` and `output` in the same block ARE read — so it is an edit that needs care, not a sweep. The real PWA config now lives in `public/manifest.webmanifest` and `app/+html.tsx` (CP-019), which is where a reader will find the truth. | The next person to change the app icon, the tint or the app name will edit these keys, export, and see no change — with nothing anywhere to tell them why. The stray `#130D18` is a colour literal no gate can see. | The inert keys are removed from `app.json`, leaving `bundler`, `output` and `favicon`; or a comment cannot be added (it is JSON), so removal is the only honest ending. | 07-Sep-2026 |
| TD-043 | **React error #418 — a hydration mismatch — fires on 7 of the 8 primary routes, in BOTH themes.** Measured in Chromium against the built `dist/`: `/`, `/members`, `/courses`, `/reports`, `/attendance`, `/weekly` and `/more` all raise it; only `/register` is clean. `/` mismatches on `text`, the rest on `HTML`. React discards the server tree and re-renders on the client when this happens. | **Pre-existing and unrelated to the change that found it.** Proven, not assumed: the same 14 route/theme combinations fail identically on a build made with `app/+html.tsx` removed. Fixing it is a behaviour change to seven shipped screens with no request behind it. | CP-015 states hydration parity as a canonical pattern and names #418 as "the failure this prevents" — and the shipped app violates it nearly everywhere, so the register currently overstates what is true. The user-visible cost is a discarded first render on almost every screen. | A `/bug` of its own: find which node differs between the Node prerender and the first client render on `/members` (the `HTML` case) and on `/` (the `text` case). The scratchpad harness that measured it drives every route in both themes and fails on #418, so the fix has a ready-made proof. | 07-Sep-2026 |
| TD-044 | **Five of the six brand assets are unedited Expo scaffold placeholders** — a blue "A" with construction guide lines still drawn on it. `assets/icon.png`, `assets/favicon.png`, `assets/splash-icon.png`, `assets/android-icon-foreground.png` and `assets/android-icon-background.png`. `assets/rosifit-logo.png` (268×300) is the ONLY real RosiFit artwork in the repo. | Found while choosing a source for the PWA icons; the PWA icons were pointed at the real mark instead, so this change ships no placeholder. Replacing the favicon and the native icon set is a separate piece of work with its own artwork requirement. | **The browser tab shows the Expo logo today** — `app.json` sets `web.favicon` to `assets/favicon.png`, and `dist/favicon.ico` is built from it. Any future native build would ship Expo-branded launcher icons. | Real artwork at 1024² is supplied and `favicon.png`, `icon.png`, `splash-icon.png` and the two Android layers are regenerated from it. The PWA icons already have a generator to copy: `scripts/generate-pwa-icons.mjs`. | 07-Sep-2026 |
| TD-042 | **The GoTrue refresh token lives in `localStorage`, where JavaScript can read it — an XSS on this origin can steal a session.** ADR-031 made that session survive reloads and browser restarts, which lengthens the window the exposure applies to: it is now indefinite by policy rather than ending when the tab closed. The request that prompted it asked specifically for an **HttpOnly** cookie, and that is the one part of it that was genuinely worth having. | **It cannot be bought at this origin.** The app is a static `expo export` on Vercel with no server of its own (`vercel.json` — no `api/`, no middleware), and the API is on `*.supabase.co`, a different registrable domain. A cookie set there is third-party: `SameSite=Lax` would never be sent, and `SameSite=None; Secure` is blocked by Safari ITP — which would reproduce the very bug being fixed, on iOS, intermittently. And RLS reads `auth.uid()` from the JWT, so the cookie could not authorize anything without re-keying every policy in 0003–0040. | Theoretical today rather than idle: this app builds no HTML from user input and loads no third-party scripts, so there is no known injection point. The cost is that if one ever appears — a rich-text field, an analytics tag, a CDN widget — the blast radius is a stolen session that outlives the theft, and nothing in the codebase would flag that the stakes had changed. | The app and its API sit behind **one origin** (a reverse proxy, or a Vercel function layer in front of Supabase), at which point the cookie is first-party and the mechanism ADR-031 declined becomes buildable for the first time. That is a platform change, not a login change, and it is its own `/request`. Until then: keep the XSS surface at zero, and treat any proposal to render user input as HTML or add a third-party script as touching this row. | 07-Sep-2026 |

---

## The baselined backlog

`.baselines/` is recorded debt by the definition above. As frozen on 02-Sep-2026:

| Ratchet | `src/` | `app/` | Note |
|---|---|---|---|
| Hard-coded colours | 7 files | 13 files | `src/theme/tokens.ts` holds 96 of them and is the legitimate token home |
| testID coverage | 6 files / 22 elements | 29 files / 93 elements | **every** interactive element in the repo; there are zero testIDs |
| Column control | 0 | 0 | clean gate |
| Rule coverage | 1 (RC-003) | — | TD-009 |
| Dead weight | 0 | — | clean gate |

Paying these down is deliberately a **later, gated queue**. The ratchet is two-sided, so the
lists can only shrink: a new violation blocks, and a fixed-but-still-listed one blocks too.
