FAIL-FIRST: src/data/dataLayerBoundary.test.ts
11-Sep-2026

Run before the guard, the npm wiring and eslint.config.mjs existed
(recorded live during the build, in this order):

  before the guard script existed .......... 1-3 red (guard not found)
  after the guard, before the npm wiring ... 2 red:
      not ok 4 - the guard is wired into a command somebody actually runs
      not ok 6 - the same rule is written for ESLint, for whenever it is installed here
  after both ............................... all green (below)

$ npx tsx --test src/data/dataLayerBoundary.test.ts
ok 1 - THE BOUNDARY HOLDS: no Supabase query is written outside src/data/
ok 2 - the guard insists on the RECEIVER, because `.from(` is not a query
ok 3 - it catches BOTH halves: a table read and an RPC
ok 4 - the guard is wired into a command somebody actually runs
ok 5 - it is NOT a ratchet, because the correct number of violations is nought
ok 6 - the same rule is written for ESLint, for whenever it is installed here
# tests 6
# pass 6
# fail 0

And the rule removed from the guard, to show the spec is testing the rule
rather than the file's existence:

  MUTATION: the pattern drops the receiver (matches Array.from too)
    not ok 1 - THE BOUNDARY HOLDS: no Supabase query is written outside src/data/
    not ok 2 - the guard insists on the RECEIVER, because `.from(` is not a query

  restored; clean run:
    # tests 6
    # pass 6
    # fail 0
