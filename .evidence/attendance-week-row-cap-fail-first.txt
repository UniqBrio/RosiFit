FAIL-FIRST — RC-039 / RC-040, 10-Sep-2026
Recorded by reconstructing each defect on the fixed tree and running the spec.

================================================================
1. src/data/pagedReads.test.ts — the rung over the call sites
================================================================

-- 1a. attendance_records read reverted to its unpaged form ------
ok 1 - the spec is looking at a real tree
not ok 2 - every read of a growing table is paged, or listed as bounded with its reason
    src/data/repository.ts:2184 reads attendance_records without pageAll(). That table grows with the academy, and PostgREST answers a too-large read with 1000 rows, 200 and no error — so this returns SILENTLY SHORT once the academy passes the cap (RC-039). Page it, or add it to BOUNDED here with the bound that makes it safe.
ok 3 - every paged read carries its own .range( and .order(
ok 4 - the pager states which ceiling it exists to clear

-- 1b. the same read paged, but with its .order() removed -------
ok 1 - the spec is looking at a real tree
ok 2 - every read of a growing table is paged, or listed as bounded with its reason
not ok 3 - every paged read carries its own .range( and .order(
  error: 'src/data/repository.ts:2184: a paged read must .order( by something unique. A range() into an unordered result is an OFFSET into nothing: two pages can return the same row and never return another.'
ok 4 - the pager states which ceiling it exists to clear
--- restored, and green again ---
# pass 4
# fail 0

================================================================
2. src/data/pageAll.test.ts — the pager itself
================================================================

The defect reconstructed: pageAll reduced to ONE request, which is
exactly what fetchAttendance did before the fix.

ok 1 - the page size is the ceiling the API actually enforces
ok 2 - a table smaller than one page is ONE request
ok 3 - an EMPTY table is one request and an empty answer, not an error
not ok 4 - a table LARGER than one page comes back whole
not ok 5 - the ranges are inclusive, contiguous and never overlap
not ok 6 - a table that is an EXACT multiple of the page still asks once more
not ok 7 - a page that FAILS is returned as a failure, never as a short answer
not ok 8 - a query with no unique order pages forever, and is stopped and NAMED
ok 9 - the runaway cap is far above any real read
# pass 4
# fail 5
--- restored, and green again ---
# pass 9
# fail 0

================================================================
3. src/components/shellTabSelected.test.ts — the tab row
================================================================

Run against the PRE-FIX tree, exported from HEAD~1, via the spec's
own SHELL_TAB_SPEC_ROOT override. No injection: this is the code
that shipped.

ok 1 - the spec is looking at a real tree
not ok 2 - the row asks the shared rule and holds no copy of its own
not ok 3 - the pushed screens of the workspace belong to the Attendance tab
ok 4 - the selected tab is never colour alone
not ok 5 - a screen reader is told which tab is current
not ok 6 - a long label truncates inside its own half of the row
ok 7 - the underline does not change height with the state
# pass 3
# fail 4
--- against the fixed tree ---
# pass 7
# fail 0

-- 3b. re-run after tightening: the fail-first above showed claim 4
   passing on the pre-fix tree, because a whole-file search found the
   string on NavPill. Scoped to the row, the pre-fix tree now reads: --

ok 1 - the spec is looking at a real tree
not ok 2 - the row asks the shared rule and holds no copy of its own
not ok 3 - the pushed screens of the workspace belong to the Attendance tab
not ok 4 - the selected tab is never colour alone
not ok 5 - a screen reader is told which tab is current
not ok 6 - a long label truncates inside its own half of the row
ok 7 - the underline does not change height with the state
# pass 2
# fail 5
--- against the fixed tree ---
# pass 7
# fail 0

================================================================
4. THE FIX, AGAINST PRODUCTION'S OWN ROWS (11-Sep-2026)
================================================================

Read-only, on project lhpzhkzbnquwjljmbylo. The three pages the fixed
fetchAttendance now asks for, replayed as SQL over the nine session ids
of 7-13 Sep 2026 -- `order by id limit 1000 offset 0 / 1000 / 2000`:

  rows returned by the three pages ....... 2220
  distinct rows among them ............... 2220   (no page repeats a row)
  rows that actually exist ............... 2220   (no page skips a row)
  size of page 3 .........................  220   (short -> the loop ends
                                                   here; there is no 4th
                                                   request)
  General's rows recovered ...............  881   (was 0 through the cap)

881 is 61 + 259 + 276 + 285 -- Mon, Tue, Wed and Thu, the four days that
read "Awaiting upload". Before the fix the single capped request returned
1000 rows of which General's share was whatever the unordered scan
happened to reach.

WHAT THIS DOES NOT PROVE. It is the partition, not the round trip: the
client loop and the rendered strip have not been exercised in a browser.
The Vercel preview for PR #13 built and is Ready, but the app is behind
mobile + PIN sign-in, and docs/registers/TEST_ACCOUNTS.md records that the
only account that exists is a real person's real credential on the only
live project there is, and is never to be used as a test account. So the
browser check is left for a human with that credential, named in the PR.
