# Framework process cases

> Cases for the **process's own behaviour** — a runbook step runs or is skipped, a field binds
> or is overridden. Executed manually by running the named runbook against the given input;
> Automation = Manual is honest here because the system under test is an agent following prose,
> which no spec file can execute. Lifecycle per `starter/tests/cases/README.md`:
> add / update-in-place / retire-only-on-removal, IDs never reused.

**Fail-first evidence (docs/15 §6):** cases FW-INTAKE-001..006 and FW-ENH-001..003 were run in
spirit against the pre-change tree by the failure that motivated them: rough one-line requests
fed to `/enhance` and `/feature` produced silent assumption-filling, no defined design pass for
visual corrections, and repeated correction-on-correction rounds (owner report, 04-Sep-2026).
Each case below encodes one observed shape of that failure.

| ID | Title | Input / steps | Expected | Negative (must NOT happen) | Date |
|---|---|---|---|---|---|
| FW-INTAKE-001 | Rough "does not exist yet" ask classifies NEW and flows to Track A | `/request` with a description of a capability the codebase lacks | REQUEST_NEW filled; file written to `requests/<date>-<slug>.md`; the SAME run continues into workflows/feature.md, whose Gate 1 opens by restating the FIELDS "from your request - correct anything wrong" | Any field invented beyond the requester's words; the run ending by asking the requester to run a second command; Gate 1 proceeding without restating the FIELDS | 2026-09-04 |
| FW-INTAKE-002 | "Works but should differ" vs "broken" boundary | Two `/request` runs: (a) "export should also offer CSV"; (b) "export gives wrong totals since last week" | (a) → REQUEST_CHANGE / Track B; (b) → REQUEST_BUG / Track C with error selectivity captured verbatim | (b) classified as CHANGE (skips root cause); (a) classified as BUG | 2026-09-04 |
| FW-INTAKE-003 | Uncovered field = `unknown`, never invented | `/request` with a description that names no users, no repro, no priority | Every uncovered field literally `unknown` (or the template's stated default); MUST NOT CHANGE defaults to "everything not named in DESIRED BEHAVIOUR" | Plausible-sounding invented users/steps/priorities in any field | 2026-09-04 |
| FW-INTAKE-004 | List / open situation / process failure continue directly, with NO file | Three `/request` runs: a 5-item list; a "what should happen when…" situation; "the gate never caught it" | No request file; the SAME run continues into workflows/triage.md, brainstorm.md, framework-update.md respectively, each stopping at its own gate | A file generated for any of the three; the run stopping to ask the requester to retype into a second command | 2026-09-04 |
| FW-INTAKE-005 | Mixed input keeps both halves | `/request` with "the price bug shipped AND the pipeline never caught it" | REQUEST_BUG written for the app half AND the same run handles both: framework-update first (diff-approval gate), then Track C with the file (FIELDS restated at its first stop) | Either half silently dropped; either half left as advice for the requester to run later | 2026-09-04 |
| FW-INTAKE-006 | Re-correction records the round | `/request` with "the export STILL shows old columns after last week's fix" | CORRECTION ROUND ≥ 2 recorded with a pointer to the previous attempt (or `unknown`) | Round field absent or reset to 1 | 2026-09-04 |
| FW-ENH-001 | Stated fields bind; `unknown` fields become questions | `/enhance requests/<file>` where the file states the screen and marks WHY `unknown` | B3 asks about WHY (and only genuine unknowns); the stated screen is never re-asked or overridden; arriving from `/request` in the same run, the first stop opens by restating the FIELDS for correction | A stated field re-asked, or silently overridden by an assumption; a same-run arrival proceeding without the FIELDS restated | 2026-09-04 |
| FW-ENH-002 | Visual correction carries the defined design pass | `/enhance` with a change touching a rendered surface | B4 plan contains all seven rows: states · both themes in semantic tokens · string table · permission answer · keyboard operability · subtraction · lists (each or a per-row reasoned N/A) | Plan reaching approval with the pass absent and no "not visual" claim | 2026-09-05 |
| FW-ENH-003 | "Not visual" is checked against the diff | Plan claims "not visual"; the diff then touches a rendered file | B6 voids the claim; the B4 correction design pass runs before the change proceeds | Merge proceeding on the voided claim | 2026-09-04 |
| FW-INTAKE-007 | A whole-new-app ask routes through initialization | `/request` with "build me a contact-management app for freelancers" and no scaffolded codebase present | Classified NEW-APP; REQUEST_NEW scoped to the first shippable slice (the rest in EXPLICITLY OUT as later runs); the run continues into docs/02 (scaffold via new:app); the request file lands in the new app's requests/; Track A runs inside the new app | Classified NEW and run as Track A with no app underneath (e.g. inside the framework repo); one request file binding the whole product | 2026-09-05 |
| FW-LEAK-001 | Placeholder data in shipped source is blocked | Add `const MOCK_ROWS = [{},{}]` (or a fixture-path import, or a two-row hardcoded dataset) to app source | `npm run audit:fixtures` reports it as a NEW violation and blocks | The literal shipping; a constant table (statuses, states) flagged as a leak | 2026-09-06 |
| FW-EDIT-001 | An edit surface is proved populated | Any change adding or touching an edit form | Cases assert: fields populated from the stored record, multi-selects selected, mode is edit, addressed by database id | An edit form covered only by a create-flow test | 2026-09-06 |
| FW-EDIT-002 | An unchanged save round-trips every field | Open an existing record, save without editing | The write contains every stored field unchanged — including fields the form never rendered | A field the form did not load returned as empty, destroying stored data | 2026-09-06 |
| FW-COMP-001 | Route B decides where the capability lives, every run | Any `/framework-update` Route B run | The change log records one of: reuse · refine · contribute · park/app-only, with its reason | The decision unasked; a reusable capability left app-local by silence | 2026-09-06 |
| FW-COMP-002 | An existing capability is reused, never re-implemented | A correction touching search/filter/sort, a dialog, a confirmation, an overflow menu or support contacts | Step 0's registry read finds it; the existing implementation is wired up | A second implementation of a registered concern shipped beside the first | 2026-09-06 |
| FW-COMP-003 | A shortfall in a shared component is REFINED, not worked around | A correction that needs something the shared component lacks | The shared implementation is improved and its registry row notes it; every app gains the fix | A local workaround that leaves the gap in place for every other app and forks this one | 2026-09-06 |
| FW-FANOUT-001 | A 3+ task plan is validated before any agent is spawned | Build stage of a plan with 3 independent tasks | `npm run fanout:check -- fanout.json` runs FIRST and passes; then one implementation-builder per task, spawned in ONE message | Agents spawned without validating the plan; lanes spawned one after another | 2026-09-06 |
| FW-FANOUT-002 | A colliding plan is BLOCKED, not built | fanout.json where two tasks declare the same file (or one reads another's write) | Exit 2 with the colliding path named; no agent spawned; the plan is fixed first | The fan-out proceeding and one lane's work silently lost | 2026-09-06 |
| FW-FANOUT-003 | Lanes stay inside their declared files | A completed fan-out | Every written file belongs to some task's `files`; a builder that needed more reported it instead of taking it | A builder writing a shared helper, barrel export or config outside its lane | 2026-09-06 |
| FW-FANOUT-004 | Review still follows the build, never overlaps it | Any fan-out run | Reviewers spawn after all lanes return and the tree is integrated | A reviewer spawned alongside the builders, reviewing half-written files | 2026-09-06 |
| FW-MICRO-001 | A qualifying correction runs the micro lane | `/enhance` for a label change in one file, round 1 | B0 declares micro; B2 impact table, B3, B4 plan and the QA verdict table are skipped; diff review, both themes, keyboard pass and the test gate still run; commit carries `SCALE: micro` | A full design pass run over a one-file label change; any mechanical gate skipped | 2026-09-06 |
| FW-MICRO-002 | An over-reaching micro claim is BLOCKED at commit | Commit declaring `SCALE: micro` whose diff touches 3 source files (or a migration, a new component, a dependency) | Guard G8 blocks with the instruction to promote to scoped | The commit landing; the process shrunk to fit the label | 2026-09-06 |
| FW-MICRO-003 | Round >= 2 is refused the micro lane | `/enhance` with CORRECTION ROUND: 2, one file | Micro refused; the full B1 analysis of what the previous fix missed runs first | A repeat correction taking the cheapest lane - the exact shape that produces five-round loops | 2026-09-06 |
| FW-MICRO-004 | Micro promotes mid-run when a disqualifier appears | Micro run that turns out to need a third file or a schema change | The run states the promotion out loud and discharges the scoped obligations it had skipped | The extra file added silently under the micro label | 2026-09-06 |
| FW-DASH-001 | A reporting screen uses the analytics module | `/feature` designing a screen that reports numbers | Design names each section's question, cost-like metrics marked higherIsBetter:false, breakdown ladders, role visibility; build uses DashboardShell/MetricCard, not a bespoke dashboard | A hand-rolled dashboard beside CP-24; a section with no stated question; a cost metric rendering a rise as good | 2026-09-06 |
| FW-DASH-002 | A new domain is a config, not a component edit | Adding a dashboard for a business not in examples.ts | A new DashboardConfig only; zero edits to analytics components | A component edited to fit one domain (the model is missing a field - that is the defect to fix) | 2026-09-06 |
| FW-LIST-001 | A new list view carries the standard controls | `/feature` designing a screen with a list/table | Design spec names search fields, filter groups, the date field (presets on) and sortable columns; build uses ListControls/useListControls, not a per-module search or sort | A list shipped with no search, or a hand-rolled search/sort beside the shared one; date data with no presets | 2026-09-05 |
| FW-LIST-002 | Corrections to a list inherit the standard | `/enhance` touching an existing list that lacks the controls | B4 Lists row states which controls are added (or a reason why not); the shared component is used | The gap left in place silently; a bespoke filter added instead of CP-23 | 2026-09-05 |
| FW-SPEED-004 | Scoped runs spawn by the matrix, in parallel | Auto-mode scoped feature touching one visible string, no schema, no roles | Spawned: code-reviewer + copy-gate-reviewer, in ONE message; blast radius, plan, gate run and close-out done inline; preview-smoke-verifier after merge | blast-radius-explorer / implementation-planner / test-gate-runner / close-out-auditor / fresh-context-reviewer spawned for a scoped run; reviewers spawned one after another | 2026-09-05 |
| FW-SPEED-005 | Close-out checklists emit compact tables | Any run reaching DoD and the screen checklist | DoD as one table (item · done or N/A + short reason; gate-proven items cite the gate); screen checklist one row per screen of 20 symbols | Per-item paragraphs; hand re-verification of audit-proven items | 2026-09-05 |
| FW-SPEED-001 | The reading budget holds | Any auto-mode scoped run | Reads: the runbook + CLAUDE.md + touched-module registers, once each; reference docs opened only at the sections stages name | The full docs/ library read front-to-back before work starts; a register re-read per stage | 2026-09-05 |
| FW-SPEED-002 | Evidence cites audits instead of duplicating them | Design QA area overlapping a mechanical audit (contrast/tokens/testids/columns) | The audit's result cited as the evidence line ("theme:contrast PASS"); no hand re-verification | Manual re-checking of audit-covered facts; per-bullet-item evidence paragraphs | 2026-09-05 |
| FW-SPEED-003 | The run report names where the time went | Any auto-mode run completing | Stage timings (ground/plan/build/verify/gate, minutes each) in the run report. **The gate stage is copied from the runner's measured `Time:` line, never estimated**; the other four remain narrator-reported and are declared debt in RC-008, not coverage. `rung: scripts/gate-timing.test.sh` (gate stage only) | A slow run with no timing data, diagnosed by feeling; a gate duration written from memory when the runner printed one | 2026-09-08 |
| FW-SPEED-006 | The gate measures its own cost and names the slowest step | `npm run gate` (or `--only <id>`) on any tree | Report block carries `Time: <total> total - slowest <id> <name> (<duration>)` and a per-step duration on every executed step; prepended to the append-only `TEST_SUMMARY.md` so the trend accrues unattended. `rung: scripts/gate-timing.test.sh` | A gate report with a verdict and no cost; a total with no slowest step named, sending the reader back to guessing which part | 2026-09-08 |
| FW-SPEED-007 | A step that never ran reports no duration, never zero | A gate run under `--only` or `--skip`, so some steps never spawn | Never-spawned steps render `(-)`; only steps that actually spawned carry a number, and only those can be named slowest. `rung: scripts/gate-timing.test.sh` | `0ms` on a step that never ran — which makes the cheapest possible run look like the fastest one, and rewards skipping | 2026-09-08 |
| FW-SPEED-008 | Verification is proportional to the declared scale | A `SCALE: micro` run (≤2 files, no schema) reaching `workflows/test-gate.md` | T1.2 states only the dimensions the diff can reach and gives one line each for the rest; T1.3 is N/A by entry test; T1.1 is one line; T6's ledger covers the touched module. Every skipped row discharged in `TEST_SUMMARY.md` with its reason | A two-file fix enumerating the full constraint and configuration space; a row skipped **silently** rather than discharged; a `micro` claim on a diff G8 would block | 2026-09-08 |
| FW-AUDIT-001 | RBAC changes are recorded with all seven facts | Super admin creates an account, assigns a role, or changes feature access | One entry each carrying **what changed - screen/module - previous value - new value - modified by - modified at - remarks**; account creation records the dash as its previous value, never a blank. `rung: starter/tests/unit/audit.unit.spec.ts` | A role granted with no trace; a blank Previous cell, which reads as "we did not capture it" rather than "nothing existed" | 2026-09-08 |
| FW-AUDIT-002 | An entry with no real actor is never dressed up as System | An action whose identity did not resolve; and a genuine background job | `Unknown (<id>)` for the first - visibly wrong, so it gets fixed; `System (<process>)` for the second, naming the process. The table **counts** unattributed entries. `rung: starter/tests/unit/audit.unit.spec.ts` | "Modified by: System" against something a person did - RC-007 shipped exactly this, and a plausible wrong author is never discovered | 2026-09-08 |
| FW-AUDIT-003 | An audit row never carries the secret it audits | A password, token, OTP or key changes | The event is recorded; both value cells read the redaction marker, which is **not blank** - blank claims nothing was captured. `rung: starter/tests/unit/audit.unit.spec.ts` | A faithful before/after log of credentials, i.e. a credential store with a search box, usually the most widely readable table in the product | 2026-09-08 |
| FW-AUDIT-004 | A change that did not happen does not appear | Roles re-serialised in a different order; null becoming an empty string | No entry. Arrays compare as SETS; the three absent shapes are one absence. `rung: starter/tests/unit/audit.unit.spec.ts` | A log filling with role changes nobody made, until nobody reads it - which costs more than having none, because everyone believes it is being watched | 2026-09-08 |
| FW-AUDIT-005 | The log view reuses the shared controls, and is read-only | The audit table on any screen | Search, module filter, date presets and sorting come from **CP-23**; the seven columns from **CP-21**; there is no edit or delete affordance anywhere in the component. Newest first by default. `rung: starter/tests/unit/audit.unit.spec.ts` plus the column-control audit | A second search/filter implementation behaving differently from every other list; an audit trail with an edit button, which answers "what happened" with "whatever the last editor preferred" | 2026-09-08 |
| FW-DEL-001 | Delete means one declared thing, and both layers agree | Any entity with a delete control (CP-26) | The model is declared at design time (A3.3b-delete): ARCHIVE — reads filter it, the unique index is partial so the name frees up, the control says *Archive*, a restore path exists; or REMOVE — the row is gone, dependants handled by a chosen `on delete` rule, the confirmation says it is irreversible. `rung: starter/tests/functional/reference.functional.spec.ts` | A backend that archives while the frontend expects removal — each layer self-consistent, so the row returning on refresh, the re-add hitting a unique constraint and the stale id 404-ing read as three unrelated bugs | 2026-09-08 |
| FW-DEL-002 | The delete assertion is a round trip, not the response | A delete journey in any suite | Act, then re-read through the SAME path the list uses, and require the outcome to match the declared model — including after a reload. `rung: starter/tests/functional/reference.functional.spec.ts` | Asserting the delete call returned 200, which proves only that the request was accepted; a row removed optimistically from the DOM and never re-read | 2026-09-08 |
| FW-VERIFY-001 | Verification is per COMMIT, not per correction | Several corrections landing in one commit | One verification pass covers them; `npm run gate` says **"this run was avoidable"** when the tree is byte-identical to the previous run. Corrections in SEPARATE commits each need their own, so every commit stays bisectable. `rung: scripts/gate-timing.test.sh` (the runner) | Re-running the full gate after each correction in one tree — only the last run describes what ships, and the earlier ones verified trees that no longer exist | 2026-09-08 |
| FW-CLASS-001 | **RETIRED 2026-09-08 (v1.27.0)** — The obvious requests are sorted before anything reads them | The pre-sorter was withdrawn: it classified one sentence, and a chat carries several requests at once. The feature is gone, so the case is retired rather than left describing a script that no longer exists. **The ID is not reused.** | — | — | 2026-09-08 |
| FW-CLASS-002 | **RETIRED 2026-09-08 (v1.27.0)** — An ambiguous request is REFUSED, never guessed | The pre-sorter was withdrawn: it classified one sentence, and a chat carries several requests at once. The feature is gone, so the case is retired rather than left describing a script that no longer exists. **The ID is not reused.** | — | — | 2026-09-08 |
| FW-STAGE-001 | A run says which stage ate the time | Any run marking `stage ground|plan|build|verify|gate` | The row's Stages cell lists each marked stage with its measured duration, in order; an UNMARKED stage is absent, never `0`. `rung: scripts/run-log.test.sh` | A total with no breakdown, leaving "which part" to memory; a `0` implying a stage ran instantly rather than that nobody measured it | 2026-09-08 |
| FW-STAGE-002 | Stage marks are refused where they mean nothing | `stage` with no open run; an unregistered stage name; the same stage marked twice | BLOCKED (3); refused (2); the duplicate accepted but not recorded, so one stage is never split into two rows summing to itself. `rung: scripts/run-log.test.sh` | Time attributed to a run that does not exist; an ad-hoc sixth stage name making two runs incomparable | 2026-09-08 |
| FW-PAR-001 | Independent checks run concurrently, and every failure is reported | `npm run audit:all` and `npm run guard:test` | All tasks run to completion; output ordered by declaration, not completion; the report names the total, the serial cost and the slowest task. Measured 87s to 41s. `rung: the suites themselves` | An `&&` chain stopping at the first failure, so each re-run reveals one more; the GATE parallelised, discarding its prerequisite order | 2026-09-08 |
| FW-PLAN-001 | Reviewer selection is computed from the diff, not judged | `node scripts/review-plan.mjs` on any staged change | Scale derived and justified; passes named with reasons; the same diff twice gives a byte-identical plan; an empty diff is BLOCKED, never an empty plan. `rung: scripts/review-plan.test.sh` | The matrix applied by eye, so one change selects different reviewers on two runs — and a missing pass looks exactly like a pass that found nothing | 2026-09-08 |
| FW-PLAN-002 | The selector states what it cannot see | A diff whose risk depends on history or schema intent | Hotspot is DECLARED (`--hotspot`), never inferred; any migration forbids micro and pulls parity; visible-string detection errs toward including the copy pass. `rung: scripts/review-plan.test.sh` | A confident plan resting on a guess about file history; a copy pass firing on every React component, which makes it noise | 2026-09-08 |
| FW-CO-001 | The release story is written once and rendered four times | `node scripts/close-out.mjs <record.json> --apply` | One record produces the upgrade section, the changelog paragraph and the commit message; a fact stated once appears identically in all of them or not at all; `--apply` prepends and refuses a duplicate version. `rung: scripts/close-out.test.sh` | The same story retyped four times, drifting between them; a generator rewriting a prior entry in an append-only file | 2026-09-08 |
| FW-CO-002 | The generator owns structure, never judgement | Any record | Headings, ordering, bump grammar and line width are mechanical; the sentences come from the record. Every required field refused BY NAME when absent — `appAction` especially, since silence there reads as "unknown", not "nothing to do". `rung: scripts/close-out.test.sh` | Templated release notes that are generic where they should be specific; an app left to infer that no action is required | 2026-09-08 |
| FW-LOG-001 | Every run is logged with a MEASURED duration | Any run entered through `/request` | `run-log.mjs start` at R1 before classifying, `end` at the close-out; the row carries action (requester's words), type, scale, start, end, computed total, the gate's own cost and the verdict. `rung: scripts/run-log.test.sh` | A start time typed in at the end of a run; a total anyone arrived at by subtracting two remembered clock readings | 2026-09-08 |
| FW-LOG-002 | `end` without `start` refuses rather than inventing | `run-log.mjs end` with no open run | Exit 3 (BLOCKED), no row written, message naming the two honest options — start properly, or `--started <ISO>` to back-fill. `rung: scripts/run-log.test.sh` | A plausible duration invented from nothing; a log where measured and guessed rows are indistinguishable, which is worse than no log | 2026-09-08 |
| FW-LOG-003 | A back-filled start is marked on its own row | `run-log.mjs end --started <ISO> --type <T> --action "<...>"` | The row is written and its Notes cell reads `back-filled start` | An estimate sitting unmarked among measurements | 2026-09-08 |
| FW-LOG-004 | The log's vocabulary stays closed and its rows stay parseable | `--type IMPROVEMENT`; an empty `--action`; `--verdict PROBABLY`; an action containing `|`; two `start`s in a row | Each of the first three refused (exit 2); the pipe escaped so the row still has ten cells; a second concurrent start refused. Newest first, IDs ascend, never reused. `rung: scripts/run-log.test.sh` | A fourth verdict value; a synonym type splitting the log's own counts; a stray pipe shifting every column after it | 2026-09-08 |
| FW-SPEED-009 | The never-scales floor holds in every lane | Any run in any lane, including micro | T1.5 fail-first evidence, T1.6 the registry delta and T2 `npm run gate` run in full regardless of scale; the mechanical stack costs ~87s measured, so there is nothing to save by cutting it | A lane that skips the gate, the fail-first record or the registry delta; "we ran the light lane" offered as the reason a class is unverified | 2026-09-08 |
| FW-REQ-001 | The usage profile drives the rendered hierarchy | `/feature requests/<file>` whose USAGE PROFILE marks info X essential, Y optional, action Z occasional | X on the primary screen by default; Y behind expand/click/context on the same screen; Z in secondary access — no separate screen created merely because Y exists | Y rendered by default; Z a peer of frequent actions; a new screen whose only justification is "the information exists" | 2026-09-05 |
| FW-REQ-002 | The subtraction pass leaves evidence | Track A design (A3.1b) or Track B visual correction (B4) | Per screen: the three answers recorded — what was removed, fields eliminated via automation, or "nothing removable" | A design QA passed with no subtraction evidence; a derivable field still asking for user input with no override reason | 2026-09-05 |
| FW-REQ-003 | Unknown profile lines are asked, never invented | Request file with FREQUENCY and AUTOMATE `unknown` | Gate 1's usage-profile section asks both, each with a recommendation and why | Frequency assumed silently; an invented automation decision binding the design | 2026-09-05 |
| FW-LIB-001 | The library is checked before building | A3.1 needing a concern the registry lists READY for this stack | The registered implementation is used; no second implementation written | A registered component rebuilt from scratch; the registry never read | 2026-09-05 |
| FW-LIB-002 | A baseline-concern build contributes back in the same change | A run building a GAP-row concern (e.g. login screens) from scratch | Component generalized (lexicon grep clean), placed at the stack's implementation location, registry row flipped GAP to READY, tests carried with it — in the same change | The component kept app-local; the row flipped without working code behind it; standards relaxed because "it's just a library copy" | 2026-09-05 |
| FW-LIB-003 | Non-baseline components still face the rule of three | A run producing a reusable-looking component NOT in the baseline | Routed through /promote: domain-free statement, parked n=1 in CANDIDATES, promoted only at n=2 from a different app | The component registered directly at n=1; the promotion gate bypassed | 2026-09-05 |
| FW-ADVISOR-001 | New module triggers the advisor pass | `/feature requests/<file>` for a new module (or NEW-APP) | Gate 1 opens with the feature triage: researched comparators (sources, or "model knowledge" declared and dated), context-lens filtering, Must-Have / Recommended / Good-to-Have each reasoned, plus the ignored list with why | Every researched feature recommended (no ignored list); a scoped in-area feature running the advisor pass; research claimed without sources or the model-knowledge declaration | 2026-09-05 |
| FW-ADVISOR-002 | The triage is a hard stop in auto mode, presented once | Auto-mode NEW-APP run reaching Gate 1 | ONE consolidated package (triage + questions, each with recommendation + reasoning + options ending in "Other"); the run waits for the requester's decisions | The triage auto-resolved from recommendations; the decisions drip-fed as separate stops | 2026-09-05 |
| FW-ADVISOR-003 | The requester's choice binds, including "Other" | Requester answers one question with a custom option not in the list | The custom answer binds the run exactly as a stated FIELD; no re-litigation downstream | The custom option silently replaced by the recommendation | 2026-09-05 |
| FW-MODE-001 | Auto mode runs gates 1-4 without waiting | `/feature requests/<file>` with RUN MODE absent (default auto) on a scoped feature | Gates 1-4 produce their artifacts, decisions logged to the ASSUMPTIONS ledger, build starts immediately after Gate 4; run report delivers FIELDS + ledger + artifacts + verdicts + preview URL | The run pausing at any of gates 1-4 for approval; an assumption overriding a stated FIELD; the mechanical test gate skipped | 2026-09-05 |
| FW-MODE-002 | Hard stops survive auto mode | Auto-mode run hitting a destructive migration (or capability removal / outbound send / production step) | The run STOPS and asks, in auto mode, exactly as in confirm mode | The hard stop logged as an assumption and proceeded past | 2026-09-05 |
| FW-MODE-003 | Scoped scale collapses ceremony, not obligations | Auto-mode scoped feature (<=5 files, additive-only schema) | ONE RUN_<feature>.md carrying assumptions, requirements deltas, design essentials, plan, and touched-area QA verdicts; A2 skipped with the one-line reason; canvas skipped | Four separate gate artifacts for a scoped run; any obligation (states, themes, keyboard, verdicts) dropped rather than compacted | 2026-09-05 |
| FW-MODE-004 | Confirm mode still stops | `/feature requests/<file>` with RUN MODE: confirm | Every gate 1-4 stops and waits for the requester, exactly as before 1.9.0 | A confirm-mode gate proceeding on a logged assumption | 2026-09-05 |
| FW-DESIGN-001 | Gate 3 design spec carries the interaction pass | `/feature` run reaching A3 for a feature with screens | The design spec names the primary action of every screen, counts the frequent scenarios against the three-interaction budget (or states why a longer path is deliberate), and records a keyboard-only dry-run pass | A design approved at Gate 3 with no named primary action, uncounted scenarios, or no keyboard pass | 2026-09-04 |
| FW-DESIGN-002 | Validation loop runs before Gate 3, with evidence | `/feature` run producing a design | All 18 checklist areas carry a verdict AND one line of evidence; findings refined and affected areas re-run; grade computed; Gate 3 sees Production-ready+ or the blocking findings with a question | A bare-tick checklist; a Basic/Acceptable design presented at Gate 3 as final | 2026-09-04 |
| FW-DESIGN-003 | Gate 3 delivers the design canvas where available | `/feature` run reaching Gate 3 in a Claude Design-capable environment | A canvas of the key screens (both themes, key states) published alongside the spec; the spec named as the binding artifact; unavailability stated when the capability is absent | Gate 3 presented spec-only with no statement about the canvas; the canvas treated as the binding artifact | 2026-09-04 |
| FW-DESIGN-004 | Material design decisions go to the requester | A design run hitting conflicting requirements or two defensible directions | A question with a reasoned recommendation reaches the requester before the design is finalized | A silent assumption baked into the approved design; a low-value question the codebase already answers | 2026-09-04 |
| FW-ENH-004 | Round ≥ 2 explains the previous miss first | `/enhance requests/<file>` with CORRECTION ROUND: 2 and a pointer | B1 states what the previous attempt missed and why, BEFORE any proposal; process-fault misses flagged for `/framework-update` | A new fix proposed with no account of the previous one | 2026-09-04 |
| FW-SUBJ-001 | A generated artifact is a function of its inputs, never of the directory it was built in | `node scripts/theme-build.mjs --out <abs>` run twice with identical `--tokens` and `--out`, once from the framework root and once from `starter/` | Byte-identical output both times; `--check` passes from either directory; the path in the generated header resolves to the real tokens file when followed from the generated file's own directory | A cwd-relative path baked into generated content; `--check` reporting DRIFT ("stale or hand-edited") on a file nothing edited; the framework root and the application rejecting each other's build | 2026-09-10 |
| FW-SUBJ-002 | The gate judges the application where the application actually is | `npm run gate` in the framework repo, and in a scaffolded app | The report states `Application steps ran in <dir>` — `starter` in the framework repo, `.` in an app; G5–G8 execute there and a BLOCKED one names that directory as the place to install | G5–G8 aimed at the framework root, which has no tsconfig and no test scripts; a remediation naming a `package.json` that cannot carry the app's toolchain; a step reporting `-` (never spawned) when its toolchain is present | 2026-09-10 |
| FW-SUBJ-003 | Only a run that verified the whole tree records the tree as verified | Gate a tree; edit a file; run a narrowed gate (`--only`/`--skip`); gate fully again | The narrowed run leaves the fingerprint untouched, so the full run is NOT announced as avoidable; re-gating genuinely unchanged bytes still IS | A narrowed run stamping the tree as gated; the redundancy notice firing on a mandatory run; a test harness writing step logs into the subject's own `.gate-logs/` | 2026-09-10 |
| FW-SUBJ-004 | CI runs the audit set by calling it, not by restating it | Add an audit to `audit:all`; open a pull request | The new audit runs in CI with no edit to `ci/github-actions-ci.yml` | A CI workflow enumerating audits by name — a hand-maintained copy that drifts, leaving an audit unable to fail a pull request | 2026-09-10 |
| FW-PWA-001 | Every scaffolded application is installable with no manual step | `node scripts/new-app.mjs --name acme-invoices`, then `npm run audit:pwa` in the new app | The scaffold writes `public/manifest.webmanifest`, `public/offline.html` and the maskable icons from the app's own tokens; the manifest carries the app's NAME, not the framework's; gate G12 passes with no configuration | A scaffold whose manifest says "Default Framework App"; a declared icon with no file; a PWA that needs a follow-up edit before it can be installed | 2026-09-10 |
| FW-PWA-002 | The installed identity is generated from the token file, never hand-written | Edit `design/tokens.json` (`app.name`, or the semantic token named by `app.themeColorToken`), run `npm run theme:build` | The manifest, the offline page, the launcher icons and `tokens.generated.ts` all follow in one command; `npm run gate` G1 passes | A hand-edited `manifest.webmanifest` (G1 reports it as drift, exactly like a hand-edited stylesheet); a `theme_color` that no longer matches the app | 2026-09-10 |
| FW-PWA-003 | An artifact generated from an app-owned source survives an upgrade | Scaffold an app, rebrand it, run `npm run framework:upgrade --apply` | The generated theme files, manifest, offline page and icons are skipped as expected-divergent; the app's name and palette are untouched | The upgrade listing them under "Auto-apply — pristine, framework changed them" and resetting the app's installed name or compiled palette to the framework's defaults | 2026-09-10 |
| FW-PWA-004 | Half-done PWA support is caught before it ships | Delete any one of: the manifest link, the worker registration, a declared icon file, the offline page | `npm run audit:pwa` (gate G12) names that specific break and blocks; a complete app reports nothing | A gate that passes because the string it looks for appears in a COMMENT; a gate that fires on everything and is therefore switched off | 2026-09-10 |
| FW-PWA-005 | An update is offered, never imposed | Deploy a new version while a session is open | The user is told a version is ready and chooses; `sw.js` calls `skipWaiting()` only on that explicit message | Code swapped under a running session, losing a form in progress; a first install announced as "an update is ready" | 2026-09-10 |
| FW-SUBJ-005 | A check that did not run is BLOCKED at every layer, never PASS | Delete any `.baselines/*-baseline.txt` in an app; run the audit, `npm run audit:all`, and `npm run gate` | The audit exits 3 saying `no baseline at`; `audit:all` labels it `BLKD` and exits 3; the gate step reads BLOCKED with a duration and names the missing baseline | The audit exiting 0 with "INERT" on stderr; the gate recording PASS; `par` calling it FAIL ("your code is broken" about a tree nothing checked) | 2026-09-10 |
| FW-SUBJ-006 | A verdict that never changes is named as a trend | Run `npm run gate` three times over a step that is BLOCKED for its own reason (an absent toolchain, a failed prerequisite) | The third report carries `N consecutive runs` on that step's own line | 24 identical BLOCKED verdicts in the ledger and nothing saying so; a step blocked only by this run's `--only` counted as a trend | 2026-09-10 |
| FW-SUBJ-007 | The version is written once, by the close-out, into both files that state it | `node scripts/close-out.mjs <record> --apply` | `VERSION` and `package.json`'s `version` both equal the record's version; the rest of `package.json` is byte-identical | Two version identities (VERSION 1.31.0 against package.json 1.3.0); a hand-edited bump to one file and not the other | 2026-09-10 |
| FW-SUBJ-008 | A fixture walks the path that failed, not a path that cannot fail | `node scripts/conformance.mjs` with `scripts/lib/lineage.mjs` reverted to before v1.31.0's ownership rule | `diverged` FAILS on both generated-artifact checks; with the current rule it passes | A fixture check that passes under every rule because lineage `--init` routed the file to review — proving nothing with the same green | 2026-09-10 |
| FW-GATE-001 | The reference implementation's toolchain is obtainable | `cd starter && npm install` on a clean checkout | A toolchain appears (`tsc`, `eslint`, `playwright`, `next` in `node_modules/.bin`); `npm run gate` runs G5–G8 with measured durations | `npm install` installing nothing; G5–G8 BLOCKED with a remediation that cannot work; a streak of identical BLOCKED verdicts accepted as an environment fact | 2026-09-10 |
| FW-GATE-002 | The reference implementation passes its own strict settings | `npm run typecheck` and `npm run lint` in `starter/` | Both clean; `tsc-baseline.txt` at zero | A type-error backlog in code that is presented as a worked example; a lint suppression standing in for a fix | 2026-09-10 |
| FW-GATE-003 | The unit tier needs nothing | `npm run test:unit` in a container that cannot serve the app | 113 specs run and pass in seconds; no dev server is started | The unit tier booting the whole application and timing out on it | 2026-09-10 |
| FW-GATE-004 | Every functional spec drives a screen that exists | `npm run test:functional` | 108 of 108 across six projects against the reference screen at `/`; the server is configured by the suite, not by a `.env` file | Specs written against a 404; a project whose engine the CI template never installs | 2026-09-10 |
| FW-CAND-001 | Capture is automatic; promotion is not | `npm run capture -- --rule "<domain-free rule>" --app <name> --apply` | One row parked at `n=1`, newest-first, nothing renumbered. No rule, checklist, pattern, gate or workflow changes | A script appending a framework rule — no rung, no case, no version bump — at machine speed | 2026-09-11 |
| FW-CAND-002 | Even n=2 from a different app does not move the framework | `npm run capture -- --sighting-of CAND-00N --app <other-app> --apply` | The row reads `ELIGIBLE (n=2)` and the output says NOT PROMOTED, naming `/promote` then `/framework-update`. Every governed file byte-identical | The tool writing `PROMOTED` — a verdict that belongs to the human gate | 2026-09-11 |
| FW-CAND-003 | A rule naming a business concept is refused | Capture a rule containing a word from the app's `PRODUCT_LEXICON.md` | Exit 2, the word named, nothing written; `--allow-lexicon-word` excuses that one check and nothing else | A domain rule parked as if general, spending the rule budget a later general rule then cannot | 2026-09-11 |
| FW-CAND-004 | A same-app repeat is not a promotion signal | `--sighting-of` an existing row with its own source app | Stays `n=1`, and says a same-app repeat is a Track C matter — the app's own fix did not hold | One app's recurrence counted as evidence of generality | 2026-09-11 |
| FW-SEED-001 | A stray lockfile in the starter never seeds a scaffolded app | `bash scripts/upgrade.test.sh` — the case PLANTS `starter/package-lock.json`, scaffolds a real app with `new-app.mjs`, then removes the plant | The scaffolded app has no `package-lock.json`; it generates its own on first `npm install` | The framework's resolution, from one machine on one afternoon, copied into every new app — and committed there, because an app commits its lockfile | 2026-09-11 |
| FW-SEED-002 | The seeding case is planted, never assumed absent | Read the case: it writes the lockfile before scaffolding | It fails against the pre-fix `new-app.mjs` — observed, not asserted | A green case that passes because `starter/` had no lockfile to copy, proving only that nothing was there — the vacuity finding, FW-SUBJ-008 | 2026-09-11 |
| FW-CI-001 | The framework's own checks run on a second path namespace | Push to `main`; read the CI run | Two jobs: `gate` on ubuntu, `self-tests-windows` running `audit:all` and `guard:test` on windows with `shell: bash` | A Linux-only CI reporting green through a Windows-only defect class, as it would have through all twelve of RC-012's | 2026-09-11 |
| FW-PATH-001 | A path crossing into JS source resolves on every platform | Run `npm run guard:test` from Git Bash on Windows, and from a POSIX shell | 14/14 on both. `ratchet` 9/9, `theme-build` 11/11, `pwa-baseline` 13/13 — the same verdicts either side of the boundary | `ERR_MODULE_NOT_FOUND` for `C:\c\…`, reported as six failing assertions about a ratchet engine that is in fact correct | 2026-09-11 |
| FW-PATH-002 | The sweep can actually fire | `bash scripts/shpath.test.sh` — case 5 plants a raw-path harness in `$TMP` and sweeps it | The sweep reports the planted site; case 5b confirms it stays quiet on `$(jspath …)` and on a `_js` variable | A sweep that passes because it matched nothing — the clean-looking result of a regex the shell mangled on the way to `grep` | 2026-09-11 |
| FW-PATH-003 | The sweep read the tree it claims to have swept | `bash scripts/shpath.test.sh` — case 4a, before the verdict is trusted | It names the harness count (13) and fails below 8 | "No violations" over a glob that matched zero files — indistinguishable from a clean tree, which is CLAUDE.md rule 5's corollary | 2026-09-11 |
| FW-PATH-004 | `jsurl` is necessary, not decorative | `bash scripts/shpath.test.sh` — case 3 imports the `jspath` form directly | On Windows the bare native path is REJECTED (`ERR_UNSUPPORTED_ESM_URL_SCHEME`); the case fails if it is ever accepted, so the redundancy would be reported rather than assumed | Two functions kept forever because nobody re-checked whether one still earns its place | 2026-09-11 |
| FW-GATE-005 | A data assertion waits for the data | Read any `written` assertion in the functional specs | `expect.poll` on the captured writes; the reference spec's habit 3 says why | A bare `expect` the instant a click or keypress resolves — a race that fails one run in four with no defect | 2026-09-10 |

**Delta 2026-09-04 (v1.4.0):** added FW-INTAKE-001..006, FW-ENH-001..004; updated none; retired none.
**Delta 2026-09-04 (v1.5.0):** added none; updated FW-INTAKE-004, FW-INTAKE-005 (routed-out
classifications now continue in the same run instead of instructing the requester); retired none.
**Delta 2026-09-04 (v1.6.0):** added none; updated FW-INTAKE-001, FW-INTAKE-005, FW-ENH-001
(file-producing classifications also continue in the same run; the field review moves to the
track's first gate, which restates the FIELDS); retired none.
**Delta 2026-09-04 (v1.7.0):** added FW-DESIGN-001..004; updated FW-ENH-002 (the correction
design pass gains its keyboard row and scoped design-quality validation); retired none.
**Delta 2026-09-05 (v1.8.0):** added FW-INTAKE-007 (NEW-APP: initialization before Track A;
scaffolds now seed `requests/`); updated none; retired none.
**Delta 2026-09-05 (v1.9.0):** added FW-MODE-001..004 (run modes: auto checkpoints with an
ASSUMPTIONS ledger, hard stops, scoped-scale combined artifact, confirm unchanged); updated
none; retired none.
**Delta 2026-09-05 (v1.10.0):** added FW-ADVISOR-001..003 (the product-advisor pass: research
+ context lenses + three-tier triage + ignored list; consolidated hard stop; custom answers
bind); updated none; retired none.
**Delta 2026-09-05 (v1.11.0):** added FW-LIB-001..003 (component library: lookup before build,
baseline contribute-back, rule of three for the rest); updated none; retired none.
**Delta 2026-09-05 (v1.12.0):** added FW-REQ-001..003 (usage profile drives hierarchy;
subtraction evidence; unknowns asked); updated FW-ENH-002 (the correction design pass gains
its subtraction row — six rows). Retired none.
**Delta 2026-09-05 (v1.13.0):** added FW-SPEED-001..003 (reading budget; audit-cited evidence;
stage timings); updated none; retired none.
**Delta 2026-09-05 (v1.15.0):** added FW-SPEED-004..005 (review matrix: proportional, parallel
agent spawning; compact close-out output); updated none; retired none.
**Delta 2026-09-05 (v1.17.0):** added FW-LIST-001..002 (CP-23 standard list controls in design
and corrections); updated FW-ENH-002 (seven rows); retired none.
**Delta 2026-09-06 (v1.18.0):** added FW-DASH-001..002 (CP-24 analytics: config-driven
dashboards, question-per-section, cost-metric marking); updated none; retired none.
**Delta 2026-09-06 (v1.19.0):** added FW-MICRO-001..004 (the micro lane: entry test, G8
verification, round>=2 refusal, mid-run promotion); updated none; retired none.
**Delta 2026-09-06 (v1.20.0):** added FW-FANOUT-001..004 (validated parallel build: plan check,
collision block, lane discipline, review-after-build); updated none; retired none.
**Delta 2026-09-06 (v1.21.0):** added FW-COMP-001..003 (reuse-first decision in framework-update:
reuse, refine-the-shared-one, contribute, park); updated none; retired none.
**Delta 2026-09-06 (v1.22.0):** added FW-LEAK-001, FW-EDIT-001..002 (fixture-leak ratchet; CP-25
edit parity and the unchanged-save no-op); updated none; retired none.
**Delta 2026-09-08 (v1.23.0):** added FW-SPEED-006..009 (the gate measures and names its own
cost; a never-run step has no duration; verification proportional to scale; the never-scales
floor); **updated FW-SPEED-003 in place** — it gains a rung for the gate stage after three
versions as prose, and now states plainly that the other four stages are narrator-reported
debt (RC-008). Retired none.
**Delta 2026-09-08 (v1.24.0):** added FW-LOG-001..004 (the run log: measured durations, the
refusal to invent a start, marked back-fills, closed vocabulary and parseable rows); updated
none; retired none.
**Delta 2026-09-08 (v1.25.0):** added FW-STAGE-001..002, FW-PAR-001, FW-PLAN-001..002,
FW-CO-001..002 (stage breakdown; concurrent checks; the executable review matrix; the
write-once close-out); updated none; retired none.
**Delta 2026-09-08 (v1.26.0):** added FW-DEL-001..002 (CP-26 delete semantics as a declared
contract, asserted by round trip), FW-VERIFY-001 (verification is per commit; the gate names an
avoidable run) and FW-CLASS-001..002 (the request pre-sorter, and its refusal to guess);
updated none; retired none.
**Delta 2026-09-08 (v1.27.0):** added none; updated none; **retired FW-CLASS-001..002** — the
request pre-sorter is withdrawn. It classified a single sentence while a real chat carries
several requests at once, so its input was ambiguous exactly where a wrong route is most
expensive, and it never showed a measured saving. IDs are not reused.
**Delta 2026-09-08 (v1.28.0):** added FW-AUDIT-001..005 (CP-27 the audit trail: the seven
facts, actor honesty, redaction, set-diffing, and a read-only view that reuses CP-23 and
CP-21); updated none; retired none.
**Delta 2026-09-10 (v1.30.0):** added FW-SUBJ-001..004 (RC-009 — a checker must describe
its subject, not its own invocation: cwd-invariant generated artifacts, the gate's
application subtree, a narrowed run's fingerprint, and CI calling `audit:all` rather than
copying it); updated none; retired none.
**Delta 2026-09-10 (v1.31.0):** added FW-PWA-001..005 (CP-30 — every generated application is a
PWA: identity and colours generated from the token file, the installability gate G12, generated
artifacts surviving an upgrade, and an update the user chooses); updated none; retired none.
**Delta 2026-09-10 (v1.32.0):** added FW-SUBJ-005..008 (RC-010 — the three-valued contract
reaches the ratchet: a missing baseline is BLOCKED at every layer; the gate reads its own
ledger for a streak; the close-out writes the version identity; a fixture must walk the path
that failed); updated none; retired none.
**Delta 2026-09-10 (v1.33.0):** added FW-GATE-001..005 (RC-011 — the starter declares its
toolchain, so G5–G8 execute; the reference implementation type-checks, lints, and its functional
specs drive a screen that exists; data assertions wait for the data); updated none; retired none.
**Delta 2026-09-11 (v1.34.0):** added FW-PATH-001..004 (RC-012 — a path crossing from the shell
into JavaScript source is data, not argv; the sweep that holds the class shut is proven to read
the tree and proven to fire); updated none; retired none.
**Delta 2026-09-11 (v1.35.0):** added FW-SEED-001..002, FW-CI-001 (RC-013 — a decision recorded
only in a comment, in a file nothing ran; the scaffolder now refuses to seed a lockfile, and CI
runs the framework's own checks on a second path namespace); updated none; retired none.
**Delta 2026-09-11 (v1.36.0):** added FW-CAND-001..004 (the capture tool: parking a lesson is
automatic, promoting one is not, and the suite's load-bearing case asserts the ABSENCE of a
capability rather than the presence of one); updated none; retired none.
